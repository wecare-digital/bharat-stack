{
  "jobs": [
    {
      "functionLocation": "backend/pinger.js",
      "functionName": "dailySelfHit",
      "description": "Ping SEO/Discovery endpoints (every 4 hours)",
      "executionConfig": { "cronExpression": "0 */4 * * *" }
    }
  ]
}
