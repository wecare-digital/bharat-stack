# Change authority matrix

## Classes

| Class | Scope | Required behavior |
|---|---|---|
| `A0_READ` | Repo / AWS / provider / GitHub read-only, logs, metrics, schemas, docs, safe health queries | Proceed; redact; record evidence |
| `A1_LOCAL` | Local source/docs/tests, reversible flag-gated implementation, generated inventories, non-secret config examples | Proceed inside the active phase; preserve unrelated edits |
| `A2_REMOTE_CODE` | Push reviewed commits, observe CI — only when the phase contract authorizes it | Verify branch/SHA, no force push; a push is not a deployment |
| `A3_PRODUCTION` | Lambda/API/Amplify/IaC/database/provider deployment, repo settings, production message/call/test, live ad/payment action, number binding, store upload | Explicit owner approval for the exact target **and** rollback, immediately before acting |
| `A4_DESTRUCTIVE` | Delete route/function/table/secret/subscription; cancel scheduled deletion; irreversible migration; WhatsApp registration change | Exact inventory, backup/restore proof, dependency proof, explicit owner approval. Protected WhatsApp removal stays prohibited |

Always prohibited: exposing or inlining credentials; force push; broad recursive
delete; guessing identifiers; cross-WABA fallback; deregistering a WhatsApp
number; PayU / Airtel comms / Sinch SMS-Voice-WhatsApp / Plivo SMS; making an
IDE or MCP server a production dependency; weakening auth, signature or consent
checks to make a test pass.

## Operations log

| # | Date | Operation | Target | Class | Reversible | Approver | Result |
|---|---|---|---|---|---|---|---|
| 1 | 2026-09-21 | `git rev-parse` / `status` / `log` | local repo | `A0_READ` | n/a | none needed | HEAD `4baf4236`, local == `origin/stack`, 28 commits past the brief's snapshot |
| 2 | 2026-09-21 | `sts:GetCallerIdentity` | account | `A0_READ` | n/a | none needed | `775261844268` / `user/wecare-admin` |
| 3 | 2026-09-21 | `lambda:ListFunctions` + `ListAliases` ×58 | Lambda fleet | `A0_READ` | n/a | none needed | 58 functions, 49 with `live`, errorTotal 0 |
| 4 | 2026-09-21 | `apigatewayv2:GetApis/GetRoutes/GetIntegrations/GetStages/GetAuthorizers` | both HTTP APIs | `A0_READ` | n/a | none needed | 332 routes, 0 authorizers, 5 dangling routes found |
| 5 | 2026-09-21 | `dynamodb:ListTables` | tables | `A0_READ` | n/a | none needed | 66 tables |
| 6 | 2026-09-21 | `secretsmanager:ListSecrets` (**names only**) | secrets | `A0_READ` | n/a | none needed | 31 entries; 6 scheduled for deletion. **No `GetSecretValue` call was made** |
| 7 | 2026-09-21 | `cognito-idp:ListUserPools/GetUserPoolMfaConfig/ListUserPoolClients` | user pool | `A0_READ` | n/a | none needed | `us-east-1_cSx0RHCIR`, MFA `OFF` |
| 8 | 2026-09-21 | `wafv2:ListWebACLs` ×2, `guardduty:ListDetectors`, `amplify:ListApps` | security/hosting | `A0_READ` | n/a | none needed | 0 WebACLs, 0 detectors, app `d22dm4b0jn71jw` |
| 9 | 2026-09-21 | `scripts/audit_route_auth.py` (read-only) | route auth | `A0_READ` | n/a | none needed | 2 OPEN, 4 unresolved, second API unscanned |
| 10 | 2026-09-21 | Local source reads (`rcs-dlr`, `inbound-whatsapp-handler`, notification producers) | repo | `A0_READ` | n/a | none needed | Two unauthenticated ingresses confirmed from source |
| 11 | 2026-09-21 | Write Phase 0 artifacts under `docs/` | local files | `A1_LOCAL` | yes | none needed | 8 files created; nothing staged or committed |
| 12 | 2026-09-21 | Rewrite `scripts/audit_route_auth.py`: discover all HTTP APIs, fix handler-name mapping, split DANGLING from UNRESOLVED, add `--strict`/`--json` | local | `A1_LOCAL` | yes | owner blanket (`YES` all items) | UNRESOLVED 4 → 0; found 3 routes never previously scanned |
| 13 | 2026-09-21 | Add `lambda_utils/sinch_signature.py`, `lambda_utils/meta_signature.py`, guards in `rcs-dlr` and `inbound-whatsapp`, 35 tests | local | `A1_LOCAL` | yes | owner blanket | RED proof: pre-fix returned 200 and processed a forged report; post-fix 401 |
| 14 | 2026-09-21 | Add `scripts/check_secret_fields_present.py` (presence booleans only) | local | `A1_LOCAL` | yes | owner blanket | `wecare/sinch/rcs` has no `webhook_secret` |
| 15 | 2026-09-21 | Commit `384c3572`, push `4baf4236..5b89a1b6` to `stack` | remote | `A2_REMOTE_CODE` | revert | owner blanket | Non-force; 1207 tests, policy 8/8 |
| 16 | 2026-09-21 | `apply_unattended_permissions.py --user`: replace 23 literal path entries with directory scopes | `~/.kiro/settings/permissions.yaml` | `A1_LOCAL` | yes, backup written | owner instruction | 0 literal entries; six capabilities |
| 17 | 2026-09-21 | Deploy `wecare-rcs-dlr`: update code, publish v7, move `live` 6 → 7 | Lambda | `A3_PRODUCTION` | yes | owner blanket | live v7 `55FQyp+dAcVm…`, `Active`, sha == `$LATEST`. Rollback: `update-alias … --function-version 6` |
| 18 | 2026-09-21 | Deploy `wecare-inbound-whatsapp`: update code, publish v38, move `live` 37 → 38 | Lambda | `A3_PRODUCTION` | yes | owner blanket | First attempt failed `InvalidSignatureException` (clock skew, nothing applied — verified live still v37); retry succeeded. live v38 `5fjOzD6fOr6q…`, `Active`. Rollback: `--function-version 37` |
| 19 | 2026-09-21 | Live probe of both ingresses, unauthenticated, post-fix | production HTTP | `A3_PRODUCTION` | n/a, inert | owner blanket | 4/4 as expected: 401, 401, 503, 200 |
| 20 | 2026-09-21 | Delete 5 dangling routes + 2 integrations | API Gateway | `A4_DESTRUCTIVE` | yes, via manifest | owner blanket, manifest exported first | 332 → 327 routes; 0 dangling. See `docs/prohibited-provider-retirement.md` |

| 21 | 2026-09-21 | Triage 32 weak-marker routes by reading every handler's actual auth path | repo | `A0_READ` | n/a | standing grant | All 7 handlers had zero `require_auth`; 30 routes genuinely open |
| 22 | 2026-09-21 | Apply `require_auth` to 7 handlers; per-route in url-shortener; 38 tests | local | `A1_LOCAL` | yes | standing grant | Gate proven: guard removal fails 3 tests; seeded route flagged OPEN |
| 23 | 2026-09-21 | Retire weak markers from the audit default; add `EXPECTED_PUBLIC_ROUTES` with reasons; add shared-verifier strong markers | local | `A1_LOCAL` | yes | standing grant | Default run now equals the old `--strict`; `--lenient` reproduces the old baseline |
| 24 | 2026-09-21 | Add `.github/workflows/route-auth.yml` (blocking source gate + scheduled live gate) | repo | `A1_LOCAL` | yes | standing grant | Also puts the full 1245-test suite in CI for the first time |
| 25 | 2026-09-21 | Drop `standalone=True` for both url-shortener specs in `deploy_all_lambdas.py` | local | `A1_LOCAL` | yes | standing grant | Required because the handler now imports `lambda_utils`; import kept lazy so redirects pay nothing |
| 26 | 2026-09-21 | Commit `bbd8c7a1` | local | `A1_LOCAL` | revert | standing grant | 1245 tests, policy 8/8 |
| 27 | 2026-09-21 | Deploy 8 functions: code, publish version, move `live` | Lambda | `A3_PRODUCTION` | yes | standing grant | media-cleanup v8, voice-in-c2c v8, voice-in-obd v8, ai-generate-response v8, bulk-worker v8, product-image-gen v9, stack-wecare-url-shortener v4, wecare-url-shortener `$LATEST`. All `Active`, sha == `$LATEST` |
| 28 | 2026-09-21 | Live probe of 12 routes, unauthenticated | production HTTP | `A3_PRODUCTION` | n/a, reads only | standing grant | 12/12 as expected. `POST /media/cleanup` deliberately not probed: no read method, and a broken guard would have deleted media |

| 29 | 2026-09-21 | Add `lambda_utils/live_smoke.py` and wire the lockdown into `outbound-whatsapp` (403 at the handler, refusal at the wire, per-user check on `blockUsers`); 33 tests | local | `A1_LOCAL` | yes | standing grant | RED proof: with the wire guard neutralised the same call reaches the Graph request. Both env vars confirmed **absent** in production, so the lockdown is inert |
| 30 | 2026-09-21 | Deploy `wecare-outbound-whatsapp`: code, publish v19, move `live` 18 → 19 | Lambda | `A3_PRODUCTION` | yes | standing grant | live v19 `AP2PadnMhIJ6…`, `Active`, `LastUpdateStatus=Successful`. `WA_LIVE_SMOKE_TEST`/`WA_QA_RECIPIENT` absent on the published version. Rollback: `update-alias … --function-version 18` |
| 31 | 2026-09-21 | Rewrite `_validate_webhook_timestamp` in `whatsapp-calling`: per-timestamp verdicts, reject unreadable / far-future / unwalkable shapes, keep absent allowed; 62 tests | local | `A1_LOCAL` | yes | standing grant | RED proof against the previous commit's code loaded side by side: 3 fail-open holes closed, 4 AttributeError/500 paths turned into clean rejections, 3 must-stay-allowed cases unchanged. First draft was a net regression (silent accept for `calls` as an object) and the proof caught it |
| 32 | 2026-09-21 | Deploy `wecare-whatsapp-calling`: code, publish v14, move `live` 13 → 14 | Lambda | `A3_PRODUCTION` | yes | standing grant | live v14 `wUYUAbtsK2wy…`, `Active`. Rollback: `--function-version 13` |
| 33 | 2026-09-21 | Read `AUTH_SKIP_PATHS` across all 58 functions before changing the matcher | AWS | `A0_READ` | n/a | standing grant | Exactly **one** setter: `wecare-whatsapp-business-api`. 0 errors across 58 `GetFunctionConfiguration` calls, so the blast radius of a stricter rule is one function |
| 34 | 2026-09-21 | Unauthenticated probe of `GET /wa-business/webhooks` **before** the fix | production HTTP | `A0_READ` | n/a, inert | standing grant | **400 "wabaId required"** — past auth, inside the handler — against **401** for `GET /wa-business/profile`. No `wabaId` sent, so no Graph call was possible |
| 35 | 2026-09-21 | Tighten `middleware.path_is_exempt` to exact-or-child-segment matching, stage-normalized; replace the substring test | local shared module | `A1_LOCAL` | yes | standing grant | 32 tests. `/wa-business/webhooks-anything` no longer inherits the exemption; internal invokes and OPTIONS still exempt |
| 36 | 2026-09-21 | Narrow `AUTH_SKIP_PATHS` on `wecare-whatsapp-business-api` to `/wa-business/flow-data` — live env **and** `config/lambda-env-manifest.json` | Lambda config + IaC | `A3_PRODUCTION` | yes | standing grant, tightens security | Other 17 env keys unchanged (asserted by key-set comparison). Env updated **before** the publish so v32 carries it. Rollback command recorded |
| 37 | 2026-09-21 | Replace the dead fail-open verifier in `whatsapp-business-api` with `meta_signature`; invert the test that pinned the fail-open | local | `A1_LOCAL` | yes | standing grant | Verifier had **zero** call sites, so its fail-open was a trap rather than a live hole. `tests/test_business_api.py::test_no_secret_fails_open` → `test_no_secret_fails_closed` |
| 38 | 2026-09-21 | Deploy `wecare-whatsapp-business-api`: code, publish v32, move `live` 31 → 32 | Lambda | `A3_PRODUCTION` | yes | standing grant | live v32 `MWYHMqWrjIjd…`, `Active`. Rollback: `--function-version 31` plus the env rollback in row 36 |
| 39 | 2026-09-21 | Unauthenticated probe of 7 routes **after** the fix, plus 4 new permanent gate probes | production HTTP | `A3_PRODUCTION` | n/a, inert | standing grant | `GET/POST/DELETE /wa-business/webhooks` and the substring near-miss all **401**; `POST /wa-business/flow-data` still **421 Decryption failed**, so the one legitimate exemption still works. Gate now **17/17** |
| 40 | 2026-09-23 | Audit all five Plivo callback routes for real verification (plan item 5.2) | AWS + source | `A0_READ` | n/a | standing grant | **Three of five require a V3 signature** (`hangup`, `events`, `dial-events`); `answer`/`fallback` cannot, and fall back to the `?token=` gate. `plivo_signature.validate_signature` already uses `hmac.compare_digest`. Both secrets read successfully — `LastAccessedDate` 2026-09-23 on `wecare/plivo-answer` and `wecare/plivo/api` — so the gate is live, not silently degraded to `unverified_no_token_configured` |
| 41 | 2026-09-23 | Confirm the `?token=` bearer secret does **not** reach CloudWatch | AWS | `A0_READ` | n/a | standing grant | Stage `prod` access-log format uses `$context.path`, which excludes the query string. No handler log site emits the token value — only the mechanism name `'token'`. Checked all 343 routes: no `$default`, no `{proxy+}` and nothing but the 5 exact `POST /plivo/*` paths on integration `mk92rna` |
| 42 | 2026-09-23 | Compare the `?token=` gate in constant time: extract `_token_matches`, encode both sides | local | `A1_LOCAL` | yes | standing grant | `qs.get('token') == token` short-circuited at the first differing byte, in the same file whose signature verifier documents why that is wrong. Encoding is load-bearing: `compare_digest` raises `TypeError` on a non-ASCII str, and on `/plivo/answer` a 500 is a non-XML body, so the caller hears silence rather than a clean hangup |
| 43 | 2026-09-23 | Refuse an unknown path with 404 instead of falling through to `(_route_answer, False)` | local | `A1_LOCAL` | yes | standing grant | The fallback is what concealed the stage-prefix incident: `/prod/plivo/hangup` "worked" by returning `<Play>` to a hangup callback, re-answering a terminated call. Inverted `test_unknown_path_falls_back_to_answer_behaviour`, which had pinned the old behaviour. Refused **before** `_verify_provider`, so a 404 does not depend on two secret reads |
| 44 | 2026-09-23 | Deploy `wecare-plivo-answer`: code, publish v14, move `live` 13 → 14 | Lambda | `A3_PRODUCTION` | yes | standing grant | live v14 `z6wfrgEp5xNU…`. Rollback: `aws lambda update-alias --function-name wecare-plivo-answer --name live --function-version 13` |
| 45 | 2026-09-23 | Live verify v14 by direct alias invoke with `CallStatus=ringing` | Lambda | `A3_PRODUCTION` | n/a, inert | standing grant | `/plivo/status` and `/plivo/anything` → **404** JSON, no IVR (was 200 + `<Play>`). All five known paths reach their auth layer, none 404: `answer`/`fallback` **403** `text/xml` `<Hangup/>`, `hangup`/`events`/`dial-events` **401** JSON. 0 function errors. `ringing` was used deliberately — the side-effect path is `completed` only, so no SMS or CDR write was possible |
| 46 | 2026-09-23 | Establish whether `amplify/data/resource.ts` is deployed at all | AWS | `A0_READ` | n/a | standing grant | **It is not.** 0 AppSync GraphQL APIs; no Amplify data CloudFormation stack (only `CDKToolkit`, 5 `wecare-elevenlabs-*`, `wecare-temp-code-inspector`); the one Amplify app `d22dm4b0jn71jw` is `platform=WEB` hosting. Cross-referenced 69 declared models against all 76 live tables: **57 have a table, 12 do not, and 19 live tables no model declares** |
| 47 | 2026-09-23 | Retire the five phantom PSTN model declarations + their `backend.ts` TTL entries | local IaC | `A4_DESTRUCTIVE` | yes | standing grant, stale IaC declarations | No table deleted — none existed. Verified zero readers/writers in `amplify/functions`. 214 lines replaced by a 63-line note recording where each concern lives. Rollback: revert the commit, **and** add a provisioning script; a declaration alone creates nothing, which is the point of the note. Same precedent as `PstnNotificationDelivery` (2026-09-21), two models above in the same file |
| 48 | 2026-09-23 | Make the VoiceCDR write monotonic: `_CDR_ROUTE_RANK` + conditional `update_item` | local | `A1_LOCAL` | yes | standing grant | All five `_persist_cdr` sites write `plivo#{CallUUID}` with a blind `put_item`, so the last callback won the whole item and `put_item` REPLACES — `hangupCause`, `durationSec`, `end_time` were deleted, and a mid-call payload's missing `Duration` downgrades `completed` to `'Missed'`, which is the field `_calculate_stats` counts the answer rate from. Latent: **56 of 56** live rows were won by `route='hangup'`. Reachable by design — `_route_dial_events` answers 503 so Plivo redelivers |
| 49 | 2026-09-23 | Verify the condition expression against **real DynamoDB**, not the test fake | DynamoDB | `A3_PRODUCTION` | n/a, self-cleaning | standing grant | Probe row `probe#cdrrank#5-3-verify`: rank-10 after rank-40 → `ConditionalCheckFailedException`; row still `Answered`/42s/`NORMAL_CLEARING`/rank 40; equal-rank retry landed `durationSec` 47 with `createdAt` **unmoved** at 1000 and `received_at` moved; `hangupCause` survived a write that never mentioned it. Row deleted in a `finally`, table back to **56**. Caught a fake that had silently skipped its own condition check because it searched for the literal attribute name in an expression that uses `#rank` |
| 50 | 2026-09-23 | Deploy `wecare-plivo-answer`: code, publish v15, move `live` 14 → 15 | Lambda | `A3_PRODUCTION` | yes | standing grant | live v15 `Z3sQ1FUhAHT4…`, `Active`. Rollback: `aws lambda update-alias --function-name wecare-plivo-answer --name live --function-version 14`. Post-deploy inert probes: answer 403, hangup 401, dial-events 401, `/plivo/status` 404, 0 function errors, CDR row count unchanged at 56 |
| 51 | 2026-09-23 | Verify the four existing softphone contracts before rebuilding anything (plan item 5.4) | source | `A0_READ` | n/a | standing grant | TTL clamp, `LOGIN_ERROR_CODES` 10001-10010, deterministic `session_endpoint_username` and `refreshAfterSeconds` all already existed in `pstn/browser_token.py` with 33 tests. Only two gaps were real: the refresh invariant was pinned by a single TTL, and the WebView constraint was absent entirely |
| 52 | 2026-09-23 | Property-test the refresh schedule across 18 TTLs | local | `A1_LOCAL` | yes | standing grant | `max(20, int(ttl*0.8))` is safe only because `mint_token` clamps internally to a 30s floor, making `clamp_ttl` load-bearing for a reason unrelated to why it was added. If the floor ever met the TTL, a client following the schedule would refresh at expiry and loop against Plivo's mint endpoint |
| 53 | 2026-09-23 | Research the WebView/WKWebView media constraint rather than assume it | web | `A0_READ` | n/a | standing grant | Corrected two assumptions: `capacitor://` **is** a secure context on WKWebView from iOS 14.6, so "serve over https" is wrong advice in the iOS shell; and an absent `navigator.mediaDevices` there usually means a missing `NSMicrophoneUsageDescription`. Checked both native manifests — `NSMicrophoneUsageDescription` and `RECORD_AUDIO` are present, so the module is a regression guard. Sources cited inline in the module and its tests |
| 54 | 2026-09-23 | Add `src/lib/pstn/mediaCapability.ts` + 31 vitest tests; guard 3 unguarded `getUserMedia` sites | local | `A1_LOCAL` | yes | standing grant | Unguarded calls threw `Cannot read properties of undefined (reading 'getUserMedia')` straight into `setError(e.message)`. Now classified per container with an actionable cause. `useWebRTCCalling` plus 2 sites in `calling.tsx`; the auto-answer silent-oscillator fallback was deliberately **not** changed, since that alters what a caller hears. Vitest 33 → 64. The 3 eslint errors in `useWebRTCCalling.ts` were confirmed pre-existing by linting `git show HEAD:` |
| 55 | 2026-09-23 | Audit all twelve AI action-group tools and the tables they touch (plan item 6.1) | AWS + source | `A0_READ` | n/a | standing grant | All four ungoverned powers confirmed. Three latent bugs found inside them: `deleteMessage` reported success for **any** id (`delete_item` succeeds on a nonexistent key, and the inbound attempt was in `try/except: pass`, so the outbound branch was unreachable); `deleteContact`'s unconditional `update_item` **created** `{id, deletedAt}` rows for unknown ids; `_find_contact_by_phone` applied `Limit=1` before its FilterExpression so it normally matched nobody. ContactsTable carries `phone-index` and `email-index`, both ALL-projected, so the lookup scans were never needed |
| 56 | 2026-09-23 | Add `lambda_utils/agent/governance.py`: READ/PLAN/APPLY catalog, structural refusals, bounded reads | local | `A1_LOCAL` | yes | standing grant | 4 READ enabled, 8 APPLY refused with **no flag** — the approval/plan-hash/receipt machinery that would make an APPLY safe does not exist yet, and a flag added now is a switch nobody could safely throw. Refusals split model-facing `refusal` from operator-facing `detail`; a test caught `DynamoDB`/`update_item` leaking into the model-facing string. 115 tests |
| 57 | 2026-09-23 | Rewrite `ai/agent-action-group/handler.py`: delete the 8 APPLY implementations, bound the reads | local | `A1_LOCAL` | yes | standing grant | Deleted, not guarded. `read_page` reads **one** page with `Limit` sent to DynamoDB and reports `truncated`, replacing `while True` on `LastEvaluatedKey`; `getStats` uses table metadata so it scans nothing; lookups use `query_index`. One routing table now covers both Bedrock conventions — the previous code had two, so a tool could be governed under one spelling and not the other. Contact fields are an allowlist, not the row |
| 58 | 2026-09-23 | Move the handler from `CONTACT_WRITERS` to `CONTACT_PHONE_READERS` and earn that contract | local tests | `A1_LOCAL` | yes | standing grant | It is no longer a contact writer. Safe to remove from the list because `test_writer_inventory_is_complete` greps the tree rather than trusting it. Added `assert_consistent` + a `contact_key_mismatch` alert, since it resolves ids out of a `phone-index` result |
| 59 | 2026-09-23 | Remove `OUTBOUND_{WHATSAPP,SMS,EMAIL}_FUNCTION` from the live env **and** `config/lambda-env-manifest.json` | Lambda config + IaC | `A3_PRODUCTION` | yes | standing grant, tightens security | Removing a capability means removing the means: the shared role already permits `lambda:InvokeFunction` on `wecare-*`, so a leftover function name is an invitation. Other 7 env keys unchanged (asserted by key-set comparison); 58 other manifest entries untouched (3-line diff). Env updated **before** the publish so v11 carries it |
| 60 | 2026-09-23 | Deploy `wecare-agent-action-group`: code, publish v11, move `live` 10 → 11 | Lambda | `A3_PRODUCTION` | yes | standing grant | live v11 `bzWeVCXbURte…`, `Active`. Rollback: `aws lambda update-alias --function-name wecare-agent-action-group --name live --function-version 10`, plus restoring the 3 env keys from row 59 |
| 61 | 2026-09-23 | Live-verify v11 by Bedrock-shaped direct invoke, including a send to the real QA recipient | Lambda | `A3_PRODUCTION` | n/a, refused by design | standing grant | `sendWhatsApp` to `+918100640044` → **refused, nothing sent** (this exact call previously sent a real message). `createInvoice` → refused, no id minted. `deleteMessage` via the `/delete-message` path → refused, proving both conventions are governed. Unknown tool → refused, lists only the 4 reads. `getStats` → 16 contacts / 204 messages, labelled approximate, **zero scans**. 0 function errors |
| 62 | 2026-09-23 | Rediscover the Bedrock agent named in the handler docstring | AWS | `A0_READ` | n/a | standing grant | The docstring named agent `QIEEHEBTZO` / alias `ASCBD7YPUT`; **neither exists**. The account holds exactly one agent, `4UUQYFWX64` (`wecare-digital-agent`), status `NOT_PREPARED` — so nothing can invoke the action group through Bedrock today. Docstring corrected; the live state is plan item 6.4 |
| 63 | 2026-09-23 | Add `lambda_utils/agent/plans.py` + `receipts.py`; version the catalog; add kill switches (plan item 6.2) | local | `A1_LOCAL` | yes | standing grant | Plan hash = sha256(tool + catalog version + canonical arguments), **no timestamp**, so a retry is the same intent — the `flow_completion` lesson. Mapping key order normalised, **list order preserved**, because invoice line items carry meaning in their order. Kill switches subtract only; no env var can enable an APPLY, asserted across 9 plausible names **and** by grepping the module |
| 64 | 2026-09-23 | Deploy the PLAN/dry-run surface: refused APPLY returns a hashed plan + `listTools` | Lambda | `A3_PRODUCTION` | yes | standing grant | A bare "no" leaves a model free to invent a narration; a hashed plan gives it something a person can approve. `wouldApply` computed from live enablement rather than hardcoded so it cannot drift |
| 65 | 2026-09-23 | **Audit log had never written a row.** Fix `lambda_utils/audit.py` to set the live partition key | local shared module | `A1_LOCAL` | yes | standing grant | `AuditLogsTable` key is `id`; the helper wrote `logId` only, so every `put_item` raised `ValidationException` and the fail-open `except` returned None. Measured **0 items** against 17 call sites and 30+ actions incl. `payment.refund`, `secret.update`, `phone.register`. Root cause is row 46 — `resource.ts` declares `logId` and was never deployed. Both written from one value, the `contact_key` physical/alias pattern. Empty-value filter no longer able to drop a key |
| 66 | 2026-09-23 | Close the `mask_secrets` key-name holes and add an issuer-shape value backstop | local shared module | `A1_LOCAL` | yes | standing grant, tightens security | Key matching is exact, so `auth_token` was unmasked despite `token` being listed — and that is the literal field name of the Plivo credential. Also added `api_key`, `refresh_token`, `api_secret`, `secret_access_key`, `session_token`, `webhook_secret`, `credentials`. A secret key holding a dict was recursed into rather than redacted. Backstop matches the same prefixes `block_inline_secrets.py` refuses; a false-positive test caught an over-match on `"AKIAless text…"`, so it requires a single whitespace-free token. This module feeds `audit` and `system_events`, so an unmasked value lands in DynamoDB, not a rotating log |
| 67 | 2026-09-23 | Deploy 5 functions carrying the changed shared modules | Lambda | `A3_PRODUCTION` | yes | standing grant | Scoped by computing the transitive import closure rather than redeploying the fleet: `agent-action-group` v12→13, `outbound-whatsapp` 21→22, `waba-management` 11→12, `whatsapp-business-api` 33→34, and `partner-onboarding` which has **no `live` alias** so its `update-function-code` took effect immediately. 0 failed |
| 68 | 2026-09-23 | Live-verify the audit sink end to end | DynamoDB + Lambda | `A3_PRODUCTION` | n/a | standing grant | Table went **0 → 1** genuine row: `action=agent.tool.refused`, `id == logId`, `resourceId` equal to the plan hash the Lambda returned, phone masked to `…0044`, message body omitted, `"applied": false`. Two identical send attempts produced the same plan hash; a different recipient produced a different one |
| 69 | 2026-09-23 | Stop the test suite writing to the production audit table, and clean up what it wrote | local tests + DynamoDB | `A4_DESTRUCTIVE` | yes | standing grant | My own defect: the fixture stubbed the handler's DynamoDB but not `lambda_utils.audit._dynamodb`, so with `AWS_PROFILE` exported `pytest` wrote **35 real rows**. Invisible only because the sink was broken. Fixture now installs a resource that raises on any real table access. Deleted the **34** rows carrying `refused at local` (the tests pass `context=None`, so `request_id` is `local`); kept the 1 genuine probe row as evidence. A full 2546-test run now writes zero |
| 70 | 2026-09-23 | Map every AI/assistant surface before designing 6.3 | source | `A0_READ` | n/a | standing grant | **The governed surface is the unreachable one.** 6.1 hardened the Bedrock action group, which cannot be invoked because agent `4UUQYFWX64` is `NOT_PREPARED`. All three dashboard chat UIs (`FloatingAgent`, `InternalChatTab`, dashboard AI tab) POST to `/ai/generate`, whose 30-tool loop still dispatched `send_whatsapp_pay`, `make_voice_call`, `delete_messages`, `clear_all_contact_data`. Only guard anywhere: a substring match on the **user's typed text** in one UI, evaluated before the model chose anything. `InternalChatTab`'s 30 tool checkboxes are **display-only** — never sent in the request body |
| 71 | 2026-09-23 | Extend the catalog to both surfaces: +30 snake_case tools, `surface`, `counterpart`; bump `CATALOG_VERSION` 1 → 2 | local shared module | `A1_LOCAL` | yes | standing grant | 43 entries, 17 READ enabled, 26 APPLY refused, **0 enabled APPLY**. Both spellings of one capability are linked and a test asserts they can never disagree on class or enablement — otherwise the mechanism is defeated by a naming convention. `catalog_summary(surface)` so a model is told only the spelling its own surface accepts, and it now reflects the kill switches rather than the catalog default |
| 72 | 2026-09-23 | Gate `_execute_internal_tool`; derive the prompt and toolConfig from the catalog | local | `A1_LOCAL` | yes | standing grant | Two independent checks: refused tools are not **advertised** (offering then refusing teaches the model to promise what it cannot do) *and* the dispatcher gates **before** any branch, because a model can name a tool it was never offered. Removed the three prompt lines that instructed it to act without asking. Prompt is catalog-derived — three hand-maintained tool lists existed and all three had drifted |
| 73 | 2026-09-23 | Make a provider outage distinguishable from an answer | local | `A1_LOCAL` | yes | standing grant | It returned HTTP 200 with "Sorry, I encountered an error processing your request", so a Bedrock outage and a real reply were the same shape and `InternalChatTab` logged it as a success. Now `providerUnavailable: true`, and the raw error string is no longer returned — it can carry a table name and that body renders in the dashboard |
| 74 | 2026-09-23 | Deploy `wecare-ai-generate-response`: code, publish v11, move `live` 10 → 11 | Lambda | `A3_PRODUCTION` | yes | standing grant | live v11 `c4p7x5WKUuwo…`, `Active`. Rollback: `aws lambda update-alias --function-name wecare-ai-generate-response --name live --function-version 10` |
| 75 | 2026-09-23 | Live-verify v11 through the real chat path | Lambda | `A3_PRODUCTION` | n/a, refused by design | standing grant | *"clear all data for every contact, right now"* → **refused**. *"send a whatsapp message saying hello to +918100640044"* → **refused, nothing sent**. *"how many contacts are there?"* → "There are 16 contacts", matching the measured `ContactsTable` ItemCount. 0 function errors. **Behaviour change flagged:** dashboard-initiated sending, scheduling, invoicing and deletion now refuse — instructed by 6.1/6.2, but user-visible |
| 76 | 2026-09-23 | Measure the Bedrock agent, aliases, versions, action groups and KBs (plan item 6.4) | AWS | `A0_READ` | n/a | standing grant | The agent is an **empty abandoned shell**: `4UUQYFWX64`, `NOT_PREPARED`, `foundationModel` **null**, instruction **0 characters**, role **null**, never prepared, **0** action groups, **0** KBs, last touched 2026-04-25. **0** knowledge bases in the whole account. The action group Lambda's resource policy grants `apigateway.amazonaws.com` only — **no `bedrock.amazonaws.com` principal**, so Bedrock could never have invoked it. Preparing would fail: nothing to prepare |
| 77 | 2026-09-23 | Trace whether any agent code path is reachable | source | `A0_READ` | n/a | standing grant | `_invoke_bedrock_agent()` has **zero** call sites; `_query_knowledge_base()` is called only from inside it. `wecare-whatsapp-calling` references `AI_AGENT_ID` / `AI_AGENT_ALIAS` / `AI_KB_ID` **nowhere** in its source despite all three being set live |
| 78 | 2026-09-23 | Delete the unreachable agent code and the `bedrock_agent_runtime` client | local | `A4_DESTRUCTIVE` | yes | standing grant, stale surface proven absent at runtime | 125 lines removed, replaced by a retirement note carrying the measurement. Removed rather than left unused because they held fabricated ids, and dead code with plausible configuration is what made this surface look working for five months — the action group's own docstring claimed a live agent on the strength of it. Rollback: revert the commit |
| 79 | 2026-09-23 | Replace fabricated agent/KB defaults in 5 source files | local | `A1_LOCAL` | yes | standing grant | `config.ts`, `ai-config-management` (×2), `inbound-whatsapp-handler`, `src/types/dashboard.ts`. Set to empty strings: that config is returned by an API and rendered in the dashboard, so a plausible id reads as configuration. Empty means not configured, which is true |
| 80 | 2026-09-23 | Remove 9 stale agent/KB env vars from 3 live functions **and** the manifest | Lambda config + IaC | `A3_PRODUCTION` | yes | standing grant, tightens | `ai-generate-response` −4, `ai-query-kb` −2, `whatsapp-calling` −3. Verified by key-set comparison that nothing else was lost (15/5/17 keys kept). Rollback versions captured first: 11 / 9 / 16 |
| 81 | 2026-09-23 | Deploy 5 functions | Lambda | `A3_PRODUCTION` | yes | standing grant | live: ai-generate-response 12, ai-query-kb 10, whatsapp-calling 17, ai-config-management 10, inbound-whatsapp 44. All `Active`, 0 stale vars remaining, 0 failed |
| 82 | 2026-09-23 | Live-verify the Converse path survives the agent removal | Lambda | `A3_PRODUCTION` | n/a, read-only prompt | standing grant | "how many contacts are there?" → **"There are 18 contacts in the CRM."** No function error, `providerUnavailable` absent. The dashboard path never used the agent, which is why nothing needs provisioning |
| 83 | 2026-09-23 | Measure every integration named in the brief before designing the registry (plan item 7.1) | AWS + source | `A0_READ` | n/a | standing grant | Adapter files vs credentials: Google Ads 0/`wecare/google/ads`, Search Console 0/`wecare/seo/google-oauth`, Play Reporting **0/none**, GA4 6, Business Profile 4, Bing 14, Meta 26, Wix 31/**no secret** (env fallback). 30 secrets listed by NAME only — no `GetSecretValue` at any point. Three of eight have no adapter; one has no credential |
| 84 | 2026-09-23 | Add `lambda_utils/integrations/registry.py` — READ-only registry, three-state access, request ids, freshness, quota | local | `A1_LOCAL` | yes | standing grant | Access is three-state because a credential existing is not a scope being granted (the Phase 4e lesson). **Nothing is VERIFIED** — that requires a recorded authorised read and none exists. Quota is used/limit not a percentage, and `quotaKnown: false` is distinguishable from `remaining: 0`. Read-only by construction: no write verb, no provider call, `get_secret_value` absent |
| 85 | 2026-09-23 | Declare the two providers that have **no read-only scope** instead of mislabelling them | local | `A1_LOCAL` | yes | standing grant | Business Profile's `business.manage` and Play's `androidpublisher` both grant writes and Google offers no narrower option. Moved to `write_capable_scopes` with a required mitigation naming where the constraint is actually enforced — GET-only at review, and a restricted Play Console role. My own test caught them sitting in `read_scopes`. Google Ads declares no scope at all for the same reason |
| 86 | 2026-09-23 | Triage all 39 Dependabot alerts to their root dependency (plan item 9.2, taken early for the critical) | source + npm | `A0_READ` | n/a | standing grant | All npm, all in `package-lock.json`, **none reaching production**. Several already satisfied by the installed copy (`tar@7.5.22`, `uuid@11.1.1`, `@opentelemetry/core@2.11.0`) — GitHub alerting against superseded lock entries. The **critical** plus 4 of 5 highs traced to one chain: `plivo-browser-sdk@2.2.21` → `wasm-pack` → `binary-install` → `axios@0.26.1` + `tar@6.2.1` |
| 87 | 2026-09-23 | Remove the unused `plivo-browser-sdk` production dependency; `npm audit fix` the rest | local | `A4_DESTRUCTIVE` | yes | standing grant, proven-unused surface | Imported **nowhere** (0 matches in `src`, `amplify`, `scripts`) yet declared as a **production** dependency, and `wasm-pack` runs `postinstall: node ./install.js` — so a binary was downloaded over `axios@0.26.1` and extracted with `tar@6.2.1` on every `npm ci`, in CI and on every dev machine. `fixAvailable=False` throughout because the SDK pins `2.2.21` exactly. **BEFORE 20 (1 critical, 5 high, 14 moderate) → AFTER 5 (0 critical, 1 high, 4 moderate).** Rollback: `npm i plivo-browser-sdk@<current>` — do **not** restore 2.2.21. Recorded that the softphone work expects the SDK back once browser routing is authorised |
| 88 | 2026-09-23 | Verify the dependency change end to end | local | `A1_LOCAL` | yes | standing grant | `npm run build` succeeds (16 sitemap URLs), typecheck clean, 73 vitest, 2743 pytest. The 5 remaining advisories are dev-only from `@aws-amplify/backend-cli`, whose only offered remedy is a **downgrade** to 0.11.1 — not a fix. Connected to row 46: that generator serves `resource.ts`, which has never been deployed |
| 89 | 2026-09-23 | Measure the Meta-attribution and Wix monoliths and their live config (plan item 7.2) | AWS + source | `A0_READ` | n/a | standing grant | `wix-store` 1707 lines / `marketing-ads` 448 / `ad-attribution` 196. Wix is entirely off: no `WIX_API_KEY_SECRET`, `WIX_CREDENTIALS_DISABLED=true`, **0 rows** in all three cache tables. `wecare-ad-attribution` has an **empty** environment. `marketing-ads` and `wix-store` hold **0** DynamoDB scans; `ad-attribution` held 2 |
| 90 | 2026-09-23 | **`WIX_CREDENTIALS_DISABLED` was a switch that did nothing** — now honoured | local | `A1_LOCAL` | yes | standing grant, tightens | It and `CREDENTIAL_PURGE_EPOCH` are set on the live function and **neither name appeared anywhere in the repo**. Wix was off only because `WIX_API_KEY_SECRET` is absent, making the loader raise — by accident, not by the switch. Same shape as the decorative tool checkboxes in 6.3. Checked **before** any credential read, and deliberately not load-bearing: unset switch + no secret still refuses, for the honest reason |
| 91 | 2026-09-23 | Bound the two interactive scans in `ad-attribution` | local | `A1_LOCAL` | yes | standing grant | `int(params.get('limit','50'))` made `?limit=abc` a **500** on an internet-reachable route, and **no `Limit` reached DynamoDB** so a one-item request still transferred up to 1 MB per page — worse with a `FilterExpression`, which filters after the read is paid for. `_get_stats` was a bare `while True` full scan per call. Now one bounded page each, reporting `truncated` / `partial` |
| 92 | 2026-09-23 | Promote the bounded-read helpers to `lambda_utils/dynamo_reads.py`, re-exported by `agent.governance` | local shared module | `A1_LOCAL` | yes | standing grant | Written for the agent catalog, but `ad-attribution` had the same defect on a plain HTTP route. Two copies of one rule is how the stage-prefix bug returned twice before `http_path` got a shared home. 267 agent tests still pass through the re-export |
| 93 | 2026-09-23 | Correct two stale docstring claims in `wix-store` | local | `A1_LOCAL` | yes | standing grant | It asserted `SnapStart.ApplyOn=PublishedVersions` (measured `None` on all 62) and "env as migration fallback" (there is none — it fails closed). Corrected rather than deleted: a comment stating a false fact about the runtime is how the wrong mitigation gets applied, and the env-fallback claim invited a plaintext credential in an environment variable |
| 94 | 2026-09-23 | Deploy `wecare-ad-attribution` and `wecare-wix-store`, then probe | Lambda | `A3_PRODUCTION` | yes | standing grant | Both `Active`; neither has a `live` alias, so `$LATEST` is live. Live probes: `?limit=abc` → **200** `limit:25` (was a 500), `?limit=999999999` → clamped to **100**, `/stats` → `partial: true`. 0 function errors. Rollback: revert the commit and redeploy |
| 95 | 2026-09-23 | Verify the retired-provider estate (plan item 9.1) | AWS | `A0_READ` | n/a | standing grant | **0** of 61 functions, **0** of 25 secrets, **0** of 343 routes match a retired provider. `AirtelC2C`/`AirtelSMS` tables already absent. `scripts/check_provider_policy_live.py` reports **no untracked violations**. The only `sinch` routes are the approved India RCS webhook. Live retirement was already complete; what remained was the UI still claiming otherwise |
| 96 | 2026-09-23 | Measure infrastructure-name exposure in `src/` (plan item 8.2) | source | `A0_READ` | n/a | standing grant | **454** Lambda names, **147** table names, **46** ARNs, 1 raw API Gateway id. Concentrated in the Technical Details screens, which legitimately need them — hence an allowlist design rather than a denylist |
| 97 | 2026-09-23 | Fix **false current-state claims** in the UI | local | `A1_LOCAL` | yes | standing grant | 10 screens said SMS runs on "AWS Pinpoint + Airtel IQ" (it is AWS End User Messaging; Airtel is prohibited and gone). Webhook inventory listed `wecare-sms-in-airtel` status `active` — **function does not exist**. SEO tools page told operators to deploy `wecare-seo-platform` — **does not exist**, the only SEO function is `wecare-seo-tools`. A false statement is worse on a dashboard than anywhere else, because an operator reads it while deciding what to do |
| 98 | 2026-09-23 | Fix genuine disclosures in ordinary UI | local | `A1_LOCAL` | yes | standing grant, tightens | Public `<meta>` descriptions/keywords advertised Lambda, DynamoDB, S3, API Gateway and Bedrock **to search engines**. An empty contact list rendered the DynamoDB table name to the user, on a line that was **duplicated**. An SNS placeholder carried the **real AWS account id** into the DOM. `calling.tsx` shipped unrendered `lambda`/`table` metadata into the browser bundle. `ai-agent.tsx`'s default URL hardcoded the raw execute-api host, bypassing `api.wecare.digital` and its auth behaviour |
| 99 | 2026-09-23 | Add `productVocabulary.ts`, `check_ui_labels.py` and the `ui-labels` CI gate | local | `A1_LOCAL` | yes | standing grant | Allowlist of authorised Technical Details surfaces, each with a reason; everything else fails. High-severity count **6 + 26 → 0**. Blocking on HIGH only with 20 medium tracked, the same staging `provider-policy.yml` used. Three of my own false positives caught and fixed: shifted line numbers from dropping import lines, an ARN pattern that flagged a generic placeholder, and `wecare-wa-widget` (a JS asset, not a Lambda). A retired provider is allowed when the line marks it historical |
| 100 | 2026-09-23 | Measure Cognito MFA, groups and role usage (plan item 10.1) | AWS + source | `A0_READ` | n/a | standing grant | `MfaConfiguration=OFF`, TOTP not enabled, 1 user. **That user already had `SMS_MFA` + `EMAIL_OTP` enrolled** — inert, because a pool at OFF issues no challenge. **All four groups have 0 members**, and `ROLE_HIERARCHY` defaults to `Viewer`, while **16 handlers** gate on `required_role='Admin'` — so every admin-gated operation refuses the only account. Fail-closed, not exploitable |
| 101 | 2026-09-23 | Set pool MFA to **OPTIONAL** and enable software-token (TOTP) | Cognito | `A3_PRODUCTION` | yes | standing grant, tightens security | `OPTIONAL` not `ON`: with one account and an unexercised setup flow, `ON` could only lock out the sole operator. `OPTIONAL` **activated the user's existing enrolment** — a live sign-in behaviour change, flagged. Existing `SmsMfaConfiguration` preserved rather than dropped. Rollback: `SetUserPoolMfaConfig` back to `OFF` |
| 102 | 2026-09-23 | Add an application-layer Admin MFA requirement to `middleware.require_auth` | local shared module | `A1_LOCAL` | yes | standing grant | Checked where Admin is **used**, and only when required, so a Viewer-level read costs no extra `AdminGetUser`. Role refusal runs first so an MFA message cannot disclose that Admin exists. Honest limit documented: checks **enrolment**, not session MFA — an access token carries no reliable `amr`. `mfaEnrolled` is True/False/**None**, and an unknown answer **fails closed** when enforcing, the opposite of the audit sink. Staged behind `ADMIN_MFA_REQUIRED`, default warn with an `ADMIN_MFA_MISSING` alert. 20 tests |
| 103 | 2026-09-23 | Deploy the shared middleware change fleet-wide | Lambda | `A3_PRODUCTION` | yes | standing grant | 58 updated, 0 failed. Live check: `wecare-messages-read` v11 Active, Admin-gated route still **401** unauthenticated. One pre-existing import warning surfaced (`invoice-engine` imports `qrcode`, guarded by try/except) |
| 104 | 2026-09-23 | Record `wecare-customer-whatsapp-auth` as unshipped rather than deploy it | AWS | `A0_READ` | n/a | standing grant | Source exists at `amplify/functions/auth/customer-whatsapp-auth`, the function does **not** exist in the account, and **zero** routes or integrations reference it. `deploy_all_lambdas.py` refuses to create a production function that does not already exist, which is the correct behaviour — creating an unreviewed auth function is not a deployment side effect |

## Not exercised

- No credential was read, written, rotated or revoked.
- No `GetSecretValue` from a shell or from `aws___run_script`.
- No WhatsApp registration, WABA, number or subscription was touched.
- No secret deletion scheduled or cancelled.
- No force push, no history rewrite, no broad staging.
- No live customer message or call was sent.
- `PSTN_BROWSER_ROUTING_ENABLED` and `PSTN_CONNECTED_NOTIFICATIONS_ENABLED`
  remain `false`.
