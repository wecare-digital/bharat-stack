# Change authority matrix

## Classes

| Class | Scope | Required behavior |
|---|---|---|
| `A0_READ` | Repo / AWS / provider / GitHub read-only, logs, metrics, schemas, docs, safe health queries | Proceed; redact; record evidence |
| `A1_LOCAL` | Local source/docs/tests, reversible flag-gated implementation, generated inventories, non-secret config examples | Proceed inside the active phase; preserve unrelated edits |
| `A2_REMOTE_CODE` | Push reviewed commits, observe CI — only when the phase contract authorizes it | Verify branch/SHA, no force push; a push is not a deployment |
| `A3_PRODUCTION` | Lambda/API/Amplify/IaC/database/provider deployment, repo settings, production message/call/test, live ad/payment action, number binding, store upload | Explicit owner approval for the exact target **and** rollback, immediately before acting |
| `A4_DESTRUCTIVE` | Delete route/function/table/secret/subscription; cancel scheduled deletion; irreversible migration; WhatsApp registration change | Exact inventory, backup/restore proof, dependency proof, explicit owner approval. Protected WhatsApp removal stays prohibited |

Always prohibited: exposing or inlining credentials; force push; broad recursive
delete; guessing identifiers; cross-WABA fallback; deregistering a WhatsApp
number; PayU / Airtel comms / Sinch SMS-Voice-WhatsApp / Plivo SMS; making an
IDE or MCP server a production dependency; weakening auth, signature or consent
checks to make a test pass.

## Operations log

| # | Date | Operation | Target | Class | Reversible | Approver | Result |
|---|---|---|---|---|---|---|---|
| 1 | 2026-09-21 | `git rev-parse` / `status` / `log` | local repo | `A0_READ` | n/a | none needed | HEAD `4baf4236`, local == `origin/stack`, 28 commits past the brief's snapshot |
| 2 | 2026-09-21 | `sts:GetCallerIdentity` | account | `A0_READ` | n/a | none needed | `775261844268` / `user/wecare-admin` |
| 3 | 2026-09-21 | `lambda:ListFunctions` + `ListAliases` ×58 | Lambda fleet | `A0_READ` | n/a | none needed | 58 functions, 49 with `live`, errorTotal 0 |
| 4 | 2026-09-21 | `apigatewayv2:GetApis/GetRoutes/GetIntegrations/GetStages/GetAuthorizers` | both HTTP APIs | `A0_READ` | n/a | none needed | 332 routes, 0 authorizers, 5 dangling routes found |
| 5 | 2026-09-21 | `dynamodb:ListTables` | tables | `A0_READ` | n/a | none needed | 66 tables |
| 6 | 2026-09-21 | `secretsmanager:ListSecrets` (**names only**) | secrets | `A0_READ` | n/a | none needed | 31 entries; 6 scheduled for deletion. **No `GetSecretValue` call was made** |
| 7 | 2026-09-21 | `cognito-idp:ListUserPools/GetUserPoolMfaConfig/ListUserPoolClients` | user pool | `A0_READ` | n/a | none needed | `us-east-1_cSx0RHCIR`, MFA `OFF` |
| 8 | 2026-09-21 | `wafv2:ListWebACLs` ×2, `guardduty:ListDetectors`, `amplify:ListApps` | security/hosting | `A0_READ` | n/a | none needed | 0 WebACLs, 0 detectors, app `d22dm4b0jn71jw` |
| 9 | 2026-09-21 | `scripts/audit_route_auth.py` (read-only) | route auth | `A0_READ` | n/a | none needed | 2 OPEN, 4 unresolved, second API unscanned |
| 10 | 2026-09-21 | Local source reads (`rcs-dlr`, `inbound-whatsapp-handler`, notification producers) | repo | `A0_READ` | n/a | none needed | Two unauthenticated ingresses confirmed from source |
| 11 | 2026-09-21 | Write Phase 0 artifacts under `docs/` | local files | `A1_LOCAL` | yes | none needed | 8 files created; nothing staged or committed |
| 12 | 2026-09-21 | Rewrite `scripts/audit_route_auth.py`: discover all HTTP APIs, fix handler-name mapping, split DANGLING from UNRESOLVED, add `--strict`/`--json` | local | `A1_LOCAL` | yes | owner blanket (`YES` all items) | UNRESOLVED 4 → 0; found 3 routes never previously scanned |
| 13 | 2026-09-21 | Add `lambda_utils/sinch_signature.py`, `lambda_utils/meta_signature.py`, guards in `rcs-dlr` and `inbound-whatsapp`, 35 tests | local | `A1_LOCAL` | yes | owner blanket | RED proof: pre-fix returned 200 and processed a forged report; post-fix 401 |
| 14 | 2026-09-21 | Add `scripts/check_secret_fields_present.py` (presence booleans only) | local | `A1_LOCAL` | yes | owner blanket | `wecare/sinch/rcs` has no `webhook_secret` |
| 15 | 2026-09-21 | Commit `384c3572`, push `4baf4236..5b89a1b6` to `stack` | remote | `A2_REMOTE_CODE` | revert | owner blanket | Non-force; 1207 tests, policy 8/8 |
| 16 | 2026-09-21 | `apply_unattended_permissions.py --user`: replace 23 literal path entries with directory scopes | `~/.kiro/settings/permissions.yaml` | `A1_LOCAL` | yes, backup written | owner instruction | 0 literal entries; six capabilities |
| 17 | 2026-09-21 | Deploy `wecare-rcs-dlr`: update code, publish v7, move `live` 6 → 7 | Lambda | `A3_PRODUCTION` | yes | owner blanket | live v7 `55FQyp+dAcVm…`, `Active`, sha == `$LATEST`. Rollback: `update-alias … --function-version 6` |
| 18 | 2026-09-21 | Deploy `wecare-inbound-whatsapp`: update code, publish v38, move `live` 37 → 38 | Lambda | `A3_PRODUCTION` | yes | owner blanket | First attempt failed `InvalidSignatureException` (clock skew, nothing applied — verified live still v37); retry succeeded. live v38 `5fjOzD6fOr6q…`, `Active`. Rollback: `--function-version 37` |
| 19 | 2026-09-21 | Live probe of both ingresses, unauthenticated, post-fix | production HTTP | `A3_PRODUCTION` | n/a, inert | owner blanket | 4/4 as expected: 401, 401, 503, 200 |
| 20 | 2026-09-21 | Delete 5 dangling routes + 2 integrations | API Gateway | `A4_DESTRUCTIVE` | yes, via manifest | owner blanket, manifest exported first | 332 → 327 routes; 0 dangling. See `docs/prohibited-provider-retirement.md` |

| 21 | 2026-09-21 | Triage 32 weak-marker routes by reading every handler's actual auth path | repo | `A0_READ` | n/a | standing grant | All 7 handlers had zero `require_auth`; 30 routes genuinely open |
| 22 | 2026-09-21 | Apply `require_auth` to 7 handlers; per-route in url-shortener; 38 tests | local | `A1_LOCAL` | yes | standing grant | Gate proven: guard removal fails 3 tests; seeded route flagged OPEN |
| 23 | 2026-09-21 | Retire weak markers from the audit default; add `EXPECTED_PUBLIC_ROUTES` with reasons; add shared-verifier strong markers | local | `A1_LOCAL` | yes | standing grant | Default run now equals the old `--strict`; `--lenient` reproduces the old baseline |
| 24 | 2026-09-21 | Add `.github/workflows/route-auth.yml` (blocking source gate + scheduled live gate) | repo | `A1_LOCAL` | yes | standing grant | Also puts the full 1245-test suite in CI for the first time |
| 25 | 2026-09-21 | Drop `standalone=True` for both url-shortener specs in `deploy_all_lambdas.py` | local | `A1_LOCAL` | yes | standing grant | Required because the handler now imports `lambda_utils`; import kept lazy so redirects pay nothing |
| 26 | 2026-09-21 | Commit `bbd8c7a1` | local | `A1_LOCAL` | revert | standing grant | 1245 tests, policy 8/8 |
| 27 | 2026-09-21 | Deploy 8 functions: code, publish version, move `live` | Lambda | `A3_PRODUCTION` | yes | standing grant | media-cleanup v8, voice-in-c2c v8, voice-in-obd v8, ai-generate-response v8, bulk-worker v8, product-image-gen v9, stack-wecare-url-shortener v4, wecare-url-shortener `$LATEST`. All `Active`, sha == `$LATEST` |
| 28 | 2026-09-21 | Live probe of 12 routes, unauthenticated | production HTTP | `A3_PRODUCTION` | n/a, reads only | standing grant | 12/12 as expected. `POST /media/cleanup` deliberately not probed: no read method, and a broken guard would have deleted media |

| 29 | 2026-09-21 | Add `lambda_utils/live_smoke.py` and wire the lockdown into `outbound-whatsapp` (403 at the handler, refusal at the wire, per-user check on `blockUsers`); 33 tests | local | `A1_LOCAL` | yes | standing grant | RED proof: with the wire guard neutralised the same call reaches the Graph request. Both env vars confirmed **absent** in production, so the lockdown is inert |
| 30 | 2026-09-21 | Deploy `wecare-outbound-whatsapp`: code, publish v19, move `live` 18 → 19 | Lambda | `A3_PRODUCTION` | yes | standing grant | live v19 `AP2PadnMhIJ6…`, `Active`, `LastUpdateStatus=Successful`. `WA_LIVE_SMOKE_TEST`/`WA_QA_RECIPIENT` absent on the published version. Rollback: `update-alias … --function-version 18` |
| 31 | 2026-09-21 | Rewrite `_validate_webhook_timestamp` in `whatsapp-calling`: per-timestamp verdicts, reject unreadable / far-future / unwalkable shapes, keep absent allowed; 62 tests | local | `A1_LOCAL` | yes | standing grant | RED proof against the previous commit's code loaded side by side: 3 fail-open holes closed, 4 AttributeError/500 paths turned into clean rejections, 3 must-stay-allowed cases unchanged. First draft was a net regression (silent accept for `calls` as an object) and the proof caught it |
| 32 | 2026-09-21 | Deploy `wecare-whatsapp-calling`: code, publish v14, move `live` 13 → 14 | Lambda | `A3_PRODUCTION` | yes | standing grant | live v14 `wUYUAbtsK2wy…`, `Active`. Rollback: `--function-version 13` |
| 33 | 2026-09-21 | Read `AUTH_SKIP_PATHS` across all 58 functions before changing the matcher | AWS | `A0_READ` | n/a | standing grant | Exactly **one** setter: `wecare-whatsapp-business-api`. 0 errors across 58 `GetFunctionConfiguration` calls, so the blast radius of a stricter rule is one function |
| 34 | 2026-09-21 | Unauthenticated probe of `GET /wa-business/webhooks` **before** the fix | production HTTP | `A0_READ` | n/a, inert | standing grant | **400 "wabaId required"** — past auth, inside the handler — against **401** for `GET /wa-business/profile`. No `wabaId` sent, so no Graph call was possible |
| 35 | 2026-09-21 | Tighten `middleware.path_is_exempt` to exact-or-child-segment matching, stage-normalized; replace the substring test | local shared module | `A1_LOCAL` | yes | standing grant | 32 tests. `/wa-business/webhooks-anything` no longer inherits the exemption; internal invokes and OPTIONS still exempt |
| 36 | 2026-09-21 | Narrow `AUTH_SKIP_PATHS` on `wecare-whatsapp-business-api` to `/wa-business/flow-data` — live env **and** `config/lambda-env-manifest.json` | Lambda config + IaC | `A3_PRODUCTION` | yes | standing grant, tightens security | Other 17 env keys unchanged (asserted by key-set comparison). Env updated **before** the publish so v32 carries it. Rollback command recorded |
| 37 | 2026-09-21 | Replace the dead fail-open verifier in `whatsapp-business-api` with `meta_signature`; invert the test that pinned the fail-open | local | `A1_LOCAL` | yes | standing grant | Verifier had **zero** call sites, so its fail-open was a trap rather than a live hole. `tests/test_business_api.py::test_no_secret_fails_open` → `test_no_secret_fails_closed` |
| 38 | 2026-09-21 | Deploy `wecare-whatsapp-business-api`: code, publish v32, move `live` 31 → 32 | Lambda | `A3_PRODUCTION` | yes | standing grant | live v32 `MWYHMqWrjIjd…`, `Active`. Rollback: `--function-version 31` plus the env rollback in row 36 |
| 39 | 2026-09-21 | Unauthenticated probe of 7 routes **after** the fix, plus 4 new permanent gate probes | production HTTP | `A3_PRODUCTION` | n/a, inert | standing grant | `GET/POST/DELETE /wa-business/webhooks` and the substring near-miss all **401**; `POST /wa-business/flow-data` still **421 Decryption failed**, so the one legitimate exemption still works. Gate now **17/17** |
| 40 | 2026-09-23 | Audit all five Plivo callback routes for real verification (plan item 5.2) | AWS + source | `A0_READ` | n/a | standing grant | **Three of five require a V3 signature** (`hangup`, `events`, `dial-events`); `answer`/`fallback` cannot, and fall back to the `?token=` gate. `plivo_signature.validate_signature` already uses `hmac.compare_digest`. Both secrets read successfully — `LastAccessedDate` 2026-09-23 on `wecare/plivo-answer` and `wecare/plivo/api` — so the gate is live, not silently degraded to `unverified_no_token_configured` |
| 41 | 2026-09-23 | Confirm the `?token=` bearer secret does **not** reach CloudWatch | AWS | `A0_READ` | n/a | standing grant | Stage `prod` access-log format uses `$context.path`, which excludes the query string. No handler log site emits the token value — only the mechanism name `'token'`. Checked all 343 routes: no `$default`, no `{proxy+}` and nothing but the 5 exact `POST /plivo/*` paths on integration `mk92rna` |
| 42 | 2026-09-23 | Compare the `?token=` gate in constant time: extract `_token_matches`, encode both sides | local | `A1_LOCAL` | yes | standing grant | `qs.get('token') == token` short-circuited at the first differing byte, in the same file whose signature verifier documents why that is wrong. Encoding is load-bearing: `compare_digest` raises `TypeError` on a non-ASCII str, and on `/plivo/answer` a 500 is a non-XML body, so the caller hears silence rather than a clean hangup |
| 43 | 2026-09-23 | Refuse an unknown path with 404 instead of falling through to `(_route_answer, False)` | local | `A1_LOCAL` | yes | standing grant | The fallback is what concealed the stage-prefix incident: `/prod/plivo/hangup` "worked" by returning `<Play>` to a hangup callback, re-answering a terminated call. Inverted `test_unknown_path_falls_back_to_answer_behaviour`, which had pinned the old behaviour. Refused **before** `_verify_provider`, so a 404 does not depend on two secret reads |
| 44 | 2026-09-23 | Deploy `wecare-plivo-answer`: code, publish v14, move `live` 13 → 14 | Lambda | `A3_PRODUCTION` | yes | standing grant | live v14 `z6wfrgEp5xNU…`. Rollback: `aws lambda update-alias --function-name wecare-plivo-answer --name live --function-version 13` |
| 45 | 2026-09-23 | Live verify v14 by direct alias invoke with `CallStatus=ringing` | Lambda | `A3_PRODUCTION` | n/a, inert | standing grant | `/plivo/status` and `/plivo/anything` → **404** JSON, no IVR (was 200 + `<Play>`). All five known paths reach their auth layer, none 404: `answer`/`fallback` **403** `text/xml` `<Hangup/>`, `hangup`/`events`/`dial-events` **401** JSON. 0 function errors. `ringing` was used deliberately — the side-effect path is `completed` only, so no SMS or CDR write was possible |
| 46 | 2026-09-23 | Establish whether `amplify/data/resource.ts` is deployed at all | AWS | `A0_READ` | n/a | standing grant | **It is not.** 0 AppSync GraphQL APIs; no Amplify data CloudFormation stack (only `CDKToolkit`, 5 `wecare-elevenlabs-*`, `wecare-temp-code-inspector`); the one Amplify app `d22dm4b0jn71jw` is `platform=WEB` hosting. Cross-referenced 69 declared models against all 76 live tables: **57 have a table, 12 do not, and 19 live tables no model declares** |
| 47 | 2026-09-23 | Retire the five phantom PSTN model declarations + their `backend.ts` TTL entries | local IaC | `A4_DESTRUCTIVE` | yes | standing grant, stale IaC declarations | No table deleted — none existed. Verified zero readers/writers in `amplify/functions`. 214 lines replaced by a 63-line note recording where each concern lives. Rollback: revert the commit, **and** add a provisioning script; a declaration alone creates nothing, which is the point of the note. Same precedent as `PstnNotificationDelivery` (2026-09-21), two models above in the same file |
| 48 | 2026-09-23 | Make the VoiceCDR write monotonic: `_CDR_ROUTE_RANK` + conditional `update_item` | local | `A1_LOCAL` | yes | standing grant | All five `_persist_cdr` sites write `plivo#{CallUUID}` with a blind `put_item`, so the last callback won the whole item and `put_item` REPLACES — `hangupCause`, `durationSec`, `end_time` were deleted, and a mid-call payload's missing `Duration` downgrades `completed` to `'Missed'`, which is the field `_calculate_stats` counts the answer rate from. Latent: **56 of 56** live rows were won by `route='hangup'`. Reachable by design — `_route_dial_events` answers 503 so Plivo redelivers |
| 49 | 2026-09-23 | Verify the condition expression against **real DynamoDB**, not the test fake | DynamoDB | `A3_PRODUCTION` | n/a, self-cleaning | standing grant | Probe row `probe#cdrrank#5-3-verify`: rank-10 after rank-40 → `ConditionalCheckFailedException`; row still `Answered`/42s/`NORMAL_CLEARING`/rank 40; equal-rank retry landed `durationSec` 47 with `createdAt` **unmoved** at 1000 and `received_at` moved; `hangupCause` survived a write that never mentioned it. Row deleted in a `finally`, table back to **56**. Caught a fake that had silently skipped its own condition check because it searched for the literal attribute name in an expression that uses `#rank` |
| 50 | 2026-09-23 | Deploy `wecare-plivo-answer`: code, publish v15, move `live` 14 → 15 | Lambda | `A3_PRODUCTION` | yes | standing grant | live v15 `Z3sQ1FUhAHT4…`, `Active`. Rollback: `aws lambda update-alias --function-name wecare-plivo-answer --name live --function-version 14`. Post-deploy inert probes: answer 403, hangup 401, dial-events 401, `/plivo/status` 404, 0 function errors, CDR row count unchanged at 56 |
| 51 | 2026-09-23 | Verify the four existing softphone contracts before rebuilding anything (plan item 5.4) | source | `A0_READ` | n/a | standing grant | TTL clamp, `LOGIN_ERROR_CODES` 10001-10010, deterministic `session_endpoint_username` and `refreshAfterSeconds` all already existed in `pstn/browser_token.py` with 33 tests. Only two gaps were real: the refresh invariant was pinned by a single TTL, and the WebView constraint was absent entirely |
| 52 | 2026-09-23 | Property-test the refresh schedule across 18 TTLs | local | `A1_LOCAL` | yes | standing grant | `max(20, int(ttl*0.8))` is safe only because `mint_token` clamps internally to a 30s floor, making `clamp_ttl` load-bearing for a reason unrelated to why it was added. If the floor ever met the TTL, a client following the schedule would refresh at expiry and loop against Plivo's mint endpoint |
| 53 | 2026-09-23 | Research the WebView/WKWebView media constraint rather than assume it | web | `A0_READ` | n/a | standing grant | Corrected two assumptions: `capacitor://` **is** a secure context on WKWebView from iOS 14.6, so "serve over https" is wrong advice in the iOS shell; and an absent `navigator.mediaDevices` there usually means a missing `NSMicrophoneUsageDescription`. Checked both native manifests — `NSMicrophoneUsageDescription` and `RECORD_AUDIO` are present, so the module is a regression guard. Sources cited inline in the module and its tests |
| 54 | 2026-09-23 | Add `src/lib/pstn/mediaCapability.ts` + 31 vitest tests; guard 3 unguarded `getUserMedia` sites | local | `A1_LOCAL` | yes | standing grant | Unguarded calls threw `Cannot read properties of undefined (reading 'getUserMedia')` straight into `setError(e.message)`. Now classified per container with an actionable cause. `useWebRTCCalling` plus 2 sites in `calling.tsx`; the auto-answer silent-oscillator fallback was deliberately **not** changed, since that alters what a caller hears. Vitest 33 → 64. The 3 eslint errors in `useWebRTCCalling.ts` were confirmed pre-existing by linting `git show HEAD:` |
| 55 | 2026-09-23 | Audit all twelve AI action-group tools and the tables they touch (plan item 6.1) | AWS + source | `A0_READ` | n/a | standing grant | All four ungoverned powers confirmed. Three latent bugs found inside them: `deleteMessage` reported success for **any** id (`delete_item` succeeds on a nonexistent key, and the inbound attempt was in `try/except: pass`, so the outbound branch was unreachable); `deleteContact`'s unconditional `update_item` **created** `{id, deletedAt}` rows for unknown ids; `_find_contact_by_phone` applied `Limit=1` before its FilterExpression so it normally matched nobody. ContactsTable carries `phone-index` and `email-index`, both ALL-projected, so the lookup scans were never needed |
| 56 | 2026-09-23 | Add `lambda_utils/agent/governance.py`: READ/PLAN/APPLY catalog, structural refusals, bounded reads | local | `A1_LOCAL` | yes | standing grant | 4 READ enabled, 8 APPLY refused with **no flag** — the approval/plan-hash/receipt machinery that would make an APPLY safe does not exist yet, and a flag added now is a switch nobody could safely throw. Refusals split model-facing `refusal` from operator-facing `detail`; a test caught `DynamoDB`/`update_item` leaking into the model-facing string. 115 tests |
| 57 | 2026-09-23 | Rewrite `ai/agent-action-group/handler.py`: delete the 8 APPLY implementations, bound the reads | local | `A1_LOCAL` | yes | standing grant | Deleted, not guarded. `read_page` reads **one** page with `Limit` sent to DynamoDB and reports `truncated`, replacing `while True` on `LastEvaluatedKey`; `getStats` uses table metadata so it scans nothing; lookups use `query_index`. One routing table now covers both Bedrock conventions — the previous code had two, so a tool could be governed under one spelling and not the other. Contact fields are an allowlist, not the row |
| 58 | 2026-09-23 | Move the handler from `CONTACT_WRITERS` to `CONTACT_PHONE_READERS` and earn that contract | local tests | `A1_LOCAL` | yes | standing grant | It is no longer a contact writer. Safe to remove from the list because `test_writer_inventory_is_complete` greps the tree rather than trusting it. Added `assert_consistent` + a `contact_key_mismatch` alert, since it resolves ids out of a `phone-index` result |
| 59 | 2026-09-23 | Remove `OUTBOUND_{WHATSAPP,SMS,EMAIL}_FUNCTION` from the live env **and** `config/lambda-env-manifest.json` | Lambda config + IaC | `A3_PRODUCTION` | yes | standing grant, tightens security | Removing a capability means removing the means: the shared role already permits `lambda:InvokeFunction` on `wecare-*`, so a leftover function name is an invitation. Other 7 env keys unchanged (asserted by key-set comparison); 58 other manifest entries untouched (3-line diff). Env updated **before** the publish so v11 carries it |
| 60 | 2026-09-23 | Deploy `wecare-agent-action-group`: code, publish v11, move `live` 10 → 11 | Lambda | `A3_PRODUCTION` | yes | standing grant | live v11 `bzWeVCXbURte…`, `Active`. Rollback: `aws lambda update-alias --function-name wecare-agent-action-group --name live --function-version 10`, plus restoring the 3 env keys from row 59 |
| 61 | 2026-09-23 | Live-verify v11 by Bedrock-shaped direct invoke, including a send to the real QA recipient | Lambda | `A3_PRODUCTION` | n/a, refused by design | standing grant | `sendWhatsApp` to `+918100640044` → **refused, nothing sent** (this exact call previously sent a real message). `createInvoice` → refused, no id minted. `deleteMessage` via the `/delete-message` path → refused, proving both conventions are governed. Unknown tool → refused, lists only the 4 reads. `getStats` → 16 contacts / 204 messages, labelled approximate, **zero scans**. 0 function errors |
| 62 | 2026-09-23 | Rediscover the Bedrock agent named in the handler docstring | AWS | `A0_READ` | n/a | standing grant | The docstring named agent `QIEEHEBTZO` / alias `ASCBD7YPUT`; **neither exists**. The account holds exactly one agent, `4UUQYFWX64` (`wecare-digital-agent`), status `NOT_PREPARED` — so nothing can invoke the action group through Bedrock today. Docstring corrected; the live state is plan item 6.4 |
| 63 | 2026-09-23 | Add `lambda_utils/agent/plans.py` + `receipts.py`; version the catalog; add kill switches (plan item 6.2) | local | `A1_LOCAL` | yes | standing grant | Plan hash = sha256(tool + catalog version + canonical arguments), **no timestamp**, so a retry is the same intent — the `flow_completion` lesson. Mapping key order normalised, **list order preserved**, because invoice line items carry meaning in their order. Kill switches subtract only; no env var can enable an APPLY, asserted across 9 plausible names **and** by grepping the module |
| 64 | 2026-09-23 | Deploy the PLAN/dry-run surface: refused APPLY returns a hashed plan + `listTools` | Lambda | `A3_PRODUCTION` | yes | standing grant | A bare "no" leaves a model free to invent a narration; a hashed plan gives it something a person can approve. `wouldApply` computed from live enablement rather than hardcoded so it cannot drift |
| 65 | 2026-09-23 | **Audit log had never written a row.** Fix `lambda_utils/audit.py` to set the live partition key | local shared module | `A1_LOCAL` | yes | standing grant | `AuditLogsTable` key is `id`; the helper wrote `logId` only, so every `put_item` raised `ValidationException` and the fail-open `except` returned None. Measured **0 items** against 17 call sites and 30+ actions incl. `payment.refund`, `secret.update`, `phone.register`. Root cause is row 46 — `resource.ts` declares `logId` and was never deployed. Both written from one value, the `contact_key` physical/alias pattern. Empty-value filter no longer able to drop a key |
| 66 | 2026-09-23 | Close the `mask_secrets` key-name holes and add an issuer-shape value backstop | local shared module | `A1_LOCAL` | yes | standing grant, tightens security | Key matching is exact, so `auth_token` was unmasked despite `token` being listed — and that is the literal field name of the Plivo credential. Also added `api_key`, `refresh_token`, `api_secret`, `secret_access_key`, `session_token`, `webhook_secret`, `credentials`. A secret key holding a dict was recursed into rather than redacted. Backstop matches the same prefixes `block_inline_secrets.py` refuses; a false-positive test caught an over-match on `"AKIAless text…"`, so it requires a single whitespace-free token. This module feeds `audit` and `system_events`, so an unmasked value lands in DynamoDB, not a rotating log |
| 67 | 2026-09-23 | Deploy 5 functions carrying the changed shared modules | Lambda | `A3_PRODUCTION` | yes | standing grant | Scoped by computing the transitive import closure rather than redeploying the fleet: `agent-action-group` v12→13, `outbound-whatsapp` 21→22, `waba-management` 11→12, `whatsapp-business-api` 33→34, and `partner-onboarding` which has **no `live` alias** so its `update-function-code` took effect immediately. 0 failed |
| 68 | 2026-09-23 | Live-verify the audit sink end to end | DynamoDB + Lambda | `A3_PRODUCTION` | n/a | standing grant | Table went **0 → 1** genuine row: `action=agent.tool.refused`, `id == logId`, `resourceId` equal to the plan hash the Lambda returned, phone masked to `…0044`, message body omitted, `"applied": false`. Two identical send attempts produced the same plan hash; a different recipient produced a different one |
| 69 | 2026-09-23 | Stop the test suite writing to the production audit table, and clean up what it wrote | local tests + DynamoDB | `A4_DESTRUCTIVE` | yes | standing grant | My own defect: the fixture stubbed the handler's DynamoDB but not `lambda_utils.audit._dynamodb`, so with `AWS_PROFILE` exported `pytest` wrote **35 real rows**. Invisible only because the sink was broken. Fixture now installs a resource that raises on any real table access. Deleted the **34** rows carrying `refused at local` (the tests pass `context=None`, so `request_id` is `local`); kept the 1 genuine probe row as evidence. A full 2546-test run now writes zero |
| 70 | 2026-09-23 | Map every AI/assistant surface before designing 6.3 | source | `A0_READ` | n/a | standing grant | **The governed surface is the unreachable one.** 6.1 hardened the Bedrock action group, which cannot be invoked because agent `4UUQYFWX64` is `NOT_PREPARED`. All three dashboard chat UIs (`FloatingAgent`, `InternalChatTab`, dashboard AI tab) POST to `/ai/generate`, whose 30-tool loop still dispatched `send_whatsapp_pay`, `make_voice_call`, `delete_messages`, `clear_all_contact_data`. Only guard anywhere: a substring match on the **user's typed text** in one UI, evaluated before the model chose anything. `InternalChatTab`'s 30 tool checkboxes are **display-only** — never sent in the request body |
| 71 | 2026-09-23 | Extend the catalog to both surfaces: +30 snake_case tools, `surface`, `counterpart`; bump `CATALOG_VERSION` 1 → 2 | local shared module | `A1_LOCAL` | yes | standing grant | 43 entries, 17 READ enabled, 26 APPLY refused, **0 enabled APPLY**. Both spellings of one capability are linked and a test asserts they can never disagree on class or enablement — otherwise the mechanism is defeated by a naming convention. `catalog_summary(surface)` so a model is told only the spelling its own surface accepts, and it now reflects the kill switches rather than the catalog default |
| 72 | 2026-09-23 | Gate `_execute_internal_tool`; derive the prompt and toolConfig from the catalog | local | `A1_LOCAL` | yes | standing grant | Two independent checks: refused tools are not **advertised** (offering then refusing teaches the model to promise what it cannot do) *and* the dispatcher gates **before** any branch, because a model can name a tool it was never offered. Removed the three prompt lines that instructed it to act without asking. Prompt is catalog-derived — three hand-maintained tool lists existed and all three had drifted |
| 73 | 2026-09-23 | Make a provider outage distinguishable from an answer | local | `A1_LOCAL` | yes | standing grant | It returned HTTP 200 with "Sorry, I encountered an error processing your request", so a Bedrock outage and a real reply were the same shape and `InternalChatTab` logged it as a success. Now `providerUnavailable: true`, and the raw error string is no longer returned — it can carry a table name and that body renders in the dashboard |
| 74 | 2026-09-23 | Deploy `wecare-ai-generate-response`: code, publish v11, move `live` 10 → 11 | Lambda | `A3_PRODUCTION` | yes | standing grant | live v11 `c4p7x5WKUuwo…`, `Active`. Rollback: `aws lambda update-alias --function-name wecare-ai-generate-response --name live --function-version 10` |
| 75 | 2026-09-23 | Live-verify v11 through the real chat path | Lambda | `A3_PRODUCTION` | n/a, refused by design | standing grant | *"clear all data for every contact, right now"* → **refused**. *"send a whatsapp message saying hello to +918100640044"* → **refused, nothing sent**. *"how many contacts are there?"* → "There are 16 contacts", matching the measured `ContactsTable` ItemCount. 0 function errors. **Behaviour change flagged:** dashboard-initiated sending, scheduling, invoicing and deletion now refuse — instructed by 6.1/6.2, but user-visible |
| 76 | 2026-09-23 | Measure the Bedrock agent, aliases, versions, action groups and KBs (plan item 6.4) | AWS | `A0_READ` | n/a | standing grant | The agent is an **empty abandoned shell**: `4UUQYFWX64`, `NOT_PREPARED`, `foundationModel` **null**, instruction **0 characters**, role **null**, never prepared, **0** action groups, **0** KBs, last touched 2026-04-25. **0** knowledge bases in the whole account. The action group Lambda's resource policy grants `apigateway.amazonaws.com` only — **no `bedrock.amazonaws.com` principal**, so Bedrock could never have invoked it. Preparing would fail: nothing to prepare |
| 77 | 2026-09-23 | Trace whether any agent code path is reachable | source | `A0_READ` | n/a | standing grant | `_invoke_bedrock_agent()` has **zero** call sites; `_query_knowledge_base()` is called only from inside it. `wecare-whatsapp-calling` references `AI_AGENT_ID` / `AI_AGENT_ALIAS` / `AI_KB_ID` **nowhere** in its source despite all three being set live |
| 78 | 2026-09-23 | Delete the unreachable agent code and the `bedrock_agent_runtime` client | local | `A4_DESTRUCTIVE` | yes | standing grant, stale surface proven absent at runtime | 125 lines removed, replaced by a retirement note carrying the measurement. Removed rather than left unused because they held fabricated ids, and dead code with plausible configuration is what made this surface look working for five months — the action group's own docstring claimed a live agent on the strength of it. Rollback: revert the commit |
| 79 | 2026-09-23 | Replace fabricated agent/KB defaults in 5 source files | local | `A1_LOCAL` | yes | standing grant | `config.ts`, `ai-config-management` (×2), `inbound-whatsapp-handler`, `src/types/dashboard.ts`. Set to empty strings: that config is returned by an API and rendered in the dashboard, so a plausible id reads as configuration. Empty means not configured, which is true |
| 80 | 2026-09-23 | Remove 9 stale agent/KB env vars from 3 live functions **and** the manifest | Lambda config + IaC | `A3_PRODUCTION` | yes | standing grant, tightens | `ai-generate-response` −4, `ai-query-kb` −2, `whatsapp-calling` −3. Verified by key-set comparison that nothing else was lost (15/5/17 keys kept). Rollback versions captured first: 11 / 9 / 16 |
| 81 | 2026-09-23 | Deploy 5 functions | Lambda | `A3_PRODUCTION` | yes | standing grant | live: ai-generate-response 12, ai-query-kb 10, whatsapp-calling 17, ai-config-management 10, inbound-whatsapp 44. All `Active`, 0 stale vars remaining, 0 failed |
| 82 | 2026-09-23 | Live-verify the Converse path survives the agent removal | Lambda | `A3_PRODUCTION` | n/a, read-only prompt | standing grant | "how many contacts are there?" → **"There are 18 contacts in the CRM."** No function error, `providerUnavailable` absent. The dashboard path never used the agent, which is why nothing needs provisioning |
| 83 | 2026-09-23 | Measure every integration named in the brief before designing the registry (plan item 7.1) | AWS + source | `A0_READ` | n/a | standing grant | Adapter files vs credentials: Google Ads 0/`wecare/google/ads`, Search Console 0/`wecare/seo/google-oauth`, Play Reporting **0/none**, GA4 6, Business Profile 4, Bing 14, Meta 26, Wix 31/**no secret** (env fallback). 30 secrets listed by NAME only — no `GetSecretValue` at any point. Three of eight have no adapter; one has no credential |
| 84 | 2026-09-23 | Add `lambda_utils/integrations/registry.py` — READ-only registry, three-state access, request ids, freshness, quota | local | `A1_LOCAL` | yes | standing grant | Access is three-state because a credential existing is not a scope being granted (the Phase 4e lesson). **Nothing is VERIFIED** — that requires a recorded authorised read and none exists. Quota is used/limit not a percentage, and `quotaKnown: false` is distinguishable from `remaining: 0`. Read-only by construction: no write verb, no provider call, `get_secret_value` absent |
| 85 | 2026-09-23 | Declare the two providers that have **no read-only scope** instead of mislabelling them | local | `A1_LOCAL` | yes | standing grant | Business Profile's `business.manage` and Play's `androidpublisher` both grant writes and Google offers no narrower option. Moved to `write_capable_scopes` with a required mitigation naming where the constraint is actually enforced — GET-only at review, and a restricted Play Console role. My own test caught them sitting in `read_scopes`. Google Ads declares no scope at all for the same reason |
| 86 | 2026-09-23 | Triage all 39 Dependabot alerts to their root dependency (plan item 9.2, taken early for the critical) | source + npm | `A0_READ` | n/a | standing grant | All npm, all in `package-lock.json`, **none reaching production**. Several already satisfied by the installed copy (`tar@7.5.22`, `uuid@11.1.1`, `@opentelemetry/core@2.11.0`) — GitHub alerting against superseded lock entries. The **critical** plus 4 of 5 highs traced to one chain: `plivo-browser-sdk@2.2.21` → `wasm-pack` → `binary-install` → `axios@0.26.1` + `tar@6.2.1` |
| 87 | 2026-09-23 | Remove the unused `plivo-browser-sdk` production dependency; `npm audit fix` the rest | local | `A4_DESTRUCTIVE` | yes | standing grant, proven-unused surface | Imported **nowhere** (0 matches in `src`, `amplify`, `scripts`) yet declared as a **production** dependency, and `wasm-pack` runs `postinstall: node ./install.js` — so a binary was downloaded over `axios@0.26.1` and extracted with `tar@6.2.1` on every `npm ci`, in CI and on every dev machine. `fixAvailable=False` throughout because the SDK pins `2.2.21` exactly. **BEFORE 20 (1 critical, 5 high, 14 moderate) → AFTER 5 (0 critical, 1 high, 4 moderate).** Rollback: `npm i plivo-browser-sdk@<current>` — do **not** restore 2.2.21. Recorded that the softphone work expects the SDK back once browser routing is authorised |
| 88 | 2026-09-23 | Verify the dependency change end to end | local | `A1_LOCAL` | yes | standing grant | `npm run build` succeeds (16 sitemap URLs), typecheck clean, 73 vitest, 2743 pytest. The 5 remaining advisories are dev-only from `@aws-amplify/backend-cli`, whose only offered remedy is a **downgrade** to 0.11.1 — not a fix. Connected to row 46: that generator serves `resource.ts`, which has never been deployed |
| 89 | 2026-09-23 | Measure the Meta-attribution and Wix monoliths and their live config (plan item 7.2) | AWS + source | `A0_READ` | n/a | standing grant | `wix-store` 1707 lines / `marketing-ads` 448 / `ad-attribution` 196. Wix is entirely off: no `WIX_API_KEY_SECRET`, `WIX_CREDENTIALS_DISABLED=true`, **0 rows** in all three cache tables. `wecare-ad-attribution` has an **empty** environment. `marketing-ads` and `wix-store` hold **0** DynamoDB scans; `ad-attribution` held 2 |
| 90 | 2026-09-23 | **`WIX_CREDENTIALS_DISABLED` was a switch that did nothing** — now honoured | local | `A1_LOCAL` | yes | standing grant, tightens | It and `CREDENTIAL_PURGE_EPOCH` are set on the live function and **neither name appeared anywhere in the repo**. Wix was off only because `WIX_API_KEY_SECRET` is absent, making the loader raise — by accident, not by the switch. Same shape as the decorative tool checkboxes in 6.3. Checked **before** any credential read, and deliberately not load-bearing: unset switch + no secret still refuses, for the honest reason |
| 91 | 2026-09-23 | Bound the two interactive scans in `ad-attribution` | local | `A1_LOCAL` | yes | standing grant | `int(params.get('limit','50'))` made `?limit=abc` a **500** on an internet-reachable route, and **no `Limit` reached DynamoDB** so a one-item request still transferred up to 1 MB per page — worse with a `FilterExpression`, which filters after the read is paid for. `_get_stats` was a bare `while True` full scan per call. Now one bounded page each, reporting `truncated` / `partial` |
| 92 | 2026-09-23 | Promote the bounded-read helpers to `lambda_utils/dynamo_reads.py`, re-exported by `agent.governance` | local shared module | `A1_LOCAL` | yes | standing grant | Written for the agent catalog, but `ad-attribution` had the same defect on a plain HTTP route. Two copies of one rule is how the stage-prefix bug returned twice before `http_path` got a shared home. 267 agent tests still pass through the re-export |
| 93 | 2026-09-23 | Correct two stale docstring claims in `wix-store` | local | `A1_LOCAL` | yes | standing grant | It asserted `SnapStart.ApplyOn=PublishedVersions` (measured `None` on all 62) and "env as migration fallback" (there is none — it fails closed). Corrected rather than deleted: a comment stating a false fact about the runtime is how the wrong mitigation gets applied, and the env-fallback claim invited a plaintext credential in an environment variable |
| 94 | 2026-09-23 | Deploy `wecare-ad-attribution` and `wecare-wix-store`, then probe | Lambda | `A3_PRODUCTION` | yes | standing grant | Both `Active`; neither has a `live` alias, so `$LATEST` is live. Live probes: `?limit=abc` → **200** `limit:25` (was a 500), `?limit=999999999` → clamped to **100**, `/stats` → `partial: true`. 0 function errors. Rollback: revert the commit and redeploy |
| 95 | 2026-09-23 | Verify the retired-provider estate (plan item 9.1) | AWS | `A0_READ` | n/a | standing grant | **0** of 61 functions, **0** of 25 secrets, **0** of 343 routes match a retired provider. `AirtelC2C`/`AirtelSMS` tables already absent. `scripts/check_provider_policy_live.py` reports **no untracked violations**. The only `sinch` routes are the approved India RCS webhook. Live retirement was already complete; what remained was the UI still claiming otherwise |
| 96 | 2026-09-23 | Measure infrastructure-name exposure in `src/` (plan item 8.2) | source | `A0_READ` | n/a | standing grant | **454** Lambda names, **147** table names, **46** ARNs, 1 raw API Gateway id. Concentrated in the Technical Details screens, which legitimately need them — hence an allowlist design rather than a denylist |
| 97 | 2026-09-23 | Fix **false current-state claims** in the UI | local | `A1_LOCAL` | yes | standing grant | 10 screens said SMS runs on "AWS Pinpoint + Airtel IQ" (it is AWS End User Messaging; Airtel is prohibited and gone). Webhook inventory listed `wecare-sms-in-airtel` status `active` — **function does not exist**. SEO tools page told operators to deploy `wecare-seo-platform` — **does not exist**, the only SEO function is `wecare-seo-tools`. A false statement is worse on a dashboard than anywhere else, because an operator reads it while deciding what to do |
| 98 | 2026-09-23 | Fix genuine disclosures in ordinary UI | local | `A1_LOCAL` | yes | standing grant, tightens | Public `<meta>` descriptions/keywords advertised Lambda, DynamoDB, S3, API Gateway and Bedrock **to search engines**. An empty contact list rendered the DynamoDB table name to the user, on a line that was **duplicated**. An SNS placeholder carried the **real AWS account id** into the DOM. `calling.tsx` shipped unrendered `lambda`/`table` metadata into the browser bundle. `ai-agent.tsx`'s default URL hardcoded the raw execute-api host, bypassing `api.wecare.digital` and its auth behaviour |
| 99 | 2026-09-23 | Add `productVocabulary.ts`, `check_ui_labels.py` and the `ui-labels` CI gate | local | `A1_LOCAL` | yes | standing grant | Allowlist of authorised Technical Details surfaces, each with a reason; everything else fails. High-severity count **6 + 26 → 0**. Blocking on HIGH only with 20 medium tracked, the same staging `provider-policy.yml` used. Three of my own false positives caught and fixed: shifted line numbers from dropping import lines, an ARN pattern that flagged a generic placeholder, and `wecare-wa-widget` (a JS asset, not a Lambda). A retired provider is allowed when the line marks it historical |
| 100 | 2026-09-23 | Measure Cognito MFA, groups and role usage (plan item 10.1) | AWS + source | `A0_READ` | n/a | standing grant | `MfaConfiguration=OFF`, TOTP not enabled, 1 user. **That user already had `SMS_MFA` + `EMAIL_OTP` enrolled** — inert, because a pool at OFF issues no challenge. **All four groups have 0 members**, and `ROLE_HIERARCHY` defaults to `Viewer`, while **16 handlers** gate on `required_role='Admin'` — so every admin-gated operation refuses the only account. Fail-closed, not exploitable |
| 101 | 2026-09-23 | Set pool MFA to **OPTIONAL** and enable software-token (TOTP) | Cognito | `A3_PRODUCTION` | yes | standing grant, tightens security | `OPTIONAL` not `ON`: with one account and an unexercised setup flow, `ON` could only lock out the sole operator. `OPTIONAL` **activated the user's existing enrolment** — a live sign-in behaviour change, flagged. Existing `SmsMfaConfiguration` preserved rather than dropped. Rollback: `SetUserPoolMfaConfig` back to `OFF` |
| 102 | 2026-09-23 | Add an application-layer Admin MFA requirement to `middleware.require_auth` | local shared module | `A1_LOCAL` | yes | standing grant | Checked where Admin is **used**, and only when required, so a Viewer-level read costs no extra `AdminGetUser`. Role refusal runs first so an MFA message cannot disclose that Admin exists. Honest limit documented: checks **enrolment**, not session MFA — an access token carries no reliable `amr`. `mfaEnrolled` is True/False/**None**, and an unknown answer **fails closed** when enforcing, the opposite of the audit sink. Staged behind `ADMIN_MFA_REQUIRED`, default warn with an `ADMIN_MFA_MISSING` alert. 20 tests |
| 103 | 2026-09-23 | Deploy the shared middleware change fleet-wide | Lambda | `A3_PRODUCTION` | yes | standing grant | 58 updated, 0 failed. Live check: `wecare-messages-read` v11 Active, Admin-gated route still **401** unauthenticated. One pre-existing import warning surfaced (`invoice-engine` imports `qrcode`, guarded by try/except) |
| 104 | 2026-09-23 | Record `wecare-customer-whatsapp-auth` as unshipped rather than deploy it | AWS | `A0_READ` | n/a | standing grant | Source exists at `amplify/functions/auth/customer-whatsapp-auth`, the function does **not** exist in the account, and **zero** routes or integrations reference it. `deploy_all_lambdas.py` refuses to create a production function that does not already exist, which is the correct behaviour — creating an unreviewed auth function is not a deployment side effect |
| 105 | 2026-09-24 | Confirm push state and Amplify build status | git + AWS | `A0_READ` | n/a | standing grant | All my commits were pushed; local was merely 2 behind another session. Amplify app `d22dm4b0jn71jw`, branch `stack` PRODUCTION auto-build: job **791** (phases 8.2/9.1) **SUCCEED**, job **792** (phase 10.1) RUNNING. Domain `wecare.digital` AVAILABLE with `stack` and `www` subdomains. Also noted: 8 stale `chatgpt/*` DEVELOPMENT branches in Amplify from 2026-09-18 |
| 106 | 2026-09-24 | Measure design drift against the contract in `grahak-os-design.md` | source | `A0_READ` | n/a | standing grant | **`src/lib/design-tokens.ts` has ZERO importers** — 172 lines declaring itself the single source of truth, which is why ~1,500 `border-radius` declarations use **twelve** different values against a contract specifying one (only 151 use 13px). It also *contradicts* the contract: `textSecondary: '#4b5563'` is a colour the contract retires by name, and `lineHeight.tight: 1.15` against the contract's `1.04`. All 6 retired-colour hits in `grahak-os/index.tsx` are **comments** explaining the retirement, so the page itself is clean |
| 107 | 2026-09-24 | **`--warning` and `--info` were both `#1a3a2a`, identical to `--success`** | local | `A1_LOCAL` | yes | standing grant | A warning rendered as a success at all 3 live `var(--warning)` call sites. Replaced with the amber and blue already present in this repo's own `design-tokens.ts` status scale (`status.pending`, `status.sent`) — existing values, not invented ones. A state colour that cannot be told from another state is not a state colour |
| 108 | 2026-09-24 | Retire `#4b5563` from the token file and all 19 call sites | local | `A1_LOCAL` | yes | standing grant | The contract retires it by name: "the only blue-tinted grey in the page's own copy", reading cooler than the neutral body text beside it. 25 live uses of `--text-secondary`. Verified **individually** that all 19 code uses are `color:` on a light surface before substituting `rgba(0,0,0,.54)`, so there is no contrast regression |
| 109 | 2026-09-24 | Collapse the retired slate ramp to `#000` on code panels | local | `A1_LOCAL` | yes | standing grant | `#1e293b` ×4, `#1e1e1e` ×3, `#0f172a` ×1, all code-panel backgrounds; the contract states "Both code panels are `#000`". `#94a3b8` ×1 was button text on a dark surface, replaced with the white the contract already uses on a black panel |
| 110 | 2026-09-24 | Add `scripts/check_design_drift.py` and wire it into the `ui-labels` workflow | local | `A1_LOCAL` | yes | standing grant | Two rules: no retired colour in real CSS, and no two state tokens resolving to the same value. Comments are stripped before matching — `grahak-os/index.tsx` documents all five retirements, and reporting the record of a fix as the defect is a mistake this repo's tests have had to correct three times. Verified: `npm run build` succeeds (125 sitemap URLs), typecheck clean, 73 vitest, 2794 pytest |

| 111 | 2026-09-24 | Measure the Cognito sign-in URL configuration against the live domains | AWS + HTTP | `A0_READ` | n/a | standing grant | **I first concluded login was broken and that was wrong, twice.** (1) `stack.wecare.digital` 301s to the apex, and I assumed the redirect dropped the OAuth `?code=`; probing with `?code=TESTVALUE123` showed the query string **survives** the 301. (2) I read a 403 from `signin.wecare.digital` as a `redirect_uri` rejection; a control probe with `not-registered.example.com` returned the **same** 403 and the same "Sign in" page, so that endpoint does not validate `redirect_uri` at render time and the 403 proves nothing. The configuration is internally consistent: `NEXT_PUBLIC_APP_URL=https://stack.wecare.digital/` is a registered callback. Recorded because a false "it is broken" costs more than the fix would have |
| 112 | 2026-09-24 | Add `https://wecare.digital/` to the web client's callback and logout URLs | Cognito | `A3_PRODUCTION` | yes | standing grant, additive | Purely additive: the apex is where the Amplify domain actually serves, so registering it removes the flow's dependence on a 301 without touching a path that works today. `NEXT_PUBLIC_APP_URL` deliberately **left** at the subdomain — a prior session's comment in `_app.tsx` chose it on purpose, and I cannot test a full OAuth round trip to justify changing it. `UpdateUserPoolClient` resets omitted fields to defaults, so every field was passed back and then re-read: `oauthFlowsIntact`, `scopesIntact`, `authFlowsIntact`, `subdomainStillRegistered`, `localhostStillRegistered` all true. Rollback: remove the apex from both lists |
| 113 | 2026-09-24 | **A vestigial second app client pointed its only callback at a domain we do not own** | Cognito | `A3_PRODUCTION` | yes | standing grant, tightens security | Client `1jrnb80tcvceg7uln9vuoe8va5` ("WECARE.DIGITAL"), created 2026-02-11 02:40 and never modified since, 35 minutes before the real web client — the console quick-start client, superseded and forgotten. Its single callback was `https://d84l1y8p4kdic.cloudfront.net`, the AWS sample distribution, with the `code` flow enabled and the extra `phone` scope. **Zero references** to the client id or that host anywhere in the repo. Disabled `AllowedOAuthFlowsUserPoolClient` and cleared the callback, logout, flow and scope lists, preserving every other field; verified the real web client was untouched in the same read. Rollback: the prior values are recorded in this row |
| 114 | 2026-09-24 | **Credential disclosure: reading that client also disclosed its client secret** | incident | `A0_READ` | n/a | standing grant | `DescribeUserPoolClient` returns `ClientSecret` in cleartext, so a read taken to check redirect URIs put a live confidential-client secret into the session transcript, which persists to `~/.kiro/logs`. The value is not repeated in this row, in the report, or in any file. Blast radius is bounded: the client is now OAuth-disabled, has no callback, `ALLOW_USER_PASSWORD_AUTH` is off, and a client secret alone authenticates nobody — it computes `SECRET_HASH` and still requires valid user credentials. **Cognito cannot rotate an app client secret in place**; the only remedy is deleting the client, which is account-level security and therefore an owner confirmation, not a standing-grant action |
| 115 | 2026-09-24 | Teach `block_catastrophic.py` about secret-bearing *reads* | local guard | `A1_LOCAL` | yes | standing grant, tightens | The guard reasoned about operations that are obviously about secrets, so an ordinary-looking `Describe` sailed through on both the CLI and the MCP route. New `SECRET_BEARING_READS` table, derived into its SDK spelling by the existing `_build_api_table` so the two tiers cannot drift. Blocked, not asked — but the CLI form is **exempt when `--query` provably excludes the secret**: it must exist, descend below the top level (`'UserPoolClient'` alone still returns everything), and not name the field. That exemption is the design: the legitimate question stays one flag away, so nobody has a reason to reach past the guard. No SDK exemption, because the guard sees a script's source and cannot prove what it returns. Self-test **87 → 97** cases |

| 116 | 2026-09-24 | Map the frontend before changing it: two design languages, not one | source | `A0_READ` | n/a | standing grant | The written contract covers `/grahak-os` only. Public pages use `<style jsx>` with literal hex; the ~103 Layout-wrapped inner pages use global CSS plus inline `style={{}}`, a different type root, and a different measure. Header and Footer are rendered centrally in `_app.tsx` for public routes and **not at all** once authenticated, where `Layout.tsx` supplies a sidebar shell instead — so "apply the header and footer style to inner pages" has no shared component to change; the alignment has to happen in the stylesheets |
| 117 | 2026-09-24 | **The hairline rule was inverted on 53 surfaces: lime at rest, dark green on hover** | local | `A1_LOCAL` | yes | standing grant | The contract: "2px means hoverable, 1px means static, and the colour is always `#e5e7eb`", with a hover that swaps the border **to** lime. Seven of the 53 were `select`, `input` and `textarea`, so every dashboard form field wore a ring that reads as permanently focused — the same defect already fixed on the sign-in form, whose note in `_app.tsx` explains why. The fix had landed on the login card and never on the pages behind it. Worst site was `tokens.css`, where `input, select, textarea` is **unscoped** and so painted the public pages too. 49 rewritten; 2px applied **only** where the selector actually has a `:hover` rule, because the weight is a signal and over-applying it is the error |
| 118 | 2026-09-24 | Keep four lime resting borders and the single heavy lime edges | local | `A1_LOCAL` | yes | standing grant | `.msg-bar.success`, `.success-banner`, `.message-bar.success` and `.message-bubble.outbound` are lime with `#1a3a2a` type — contract treatment 1, "our own surfaces, full voice". The lime edges on `.inner-header`, `.inner-footer`, `.sidebar` and the panel headers are kept for the same reason the sign-in card's 4px lime top edge is defended in `_app.tsx`: a single heavy edge on one of our own surfaces reads as brand and cannot be mistaken for focus. What is **not** that pattern is a lime rule repeated under every row of every table, so those 3 went to `#e5e7eb` |
| 119 | 2026-09-24 | **The shipped bundle caught me fixing one file out of nine** | verification | `A0_READ` | n/a | standing grant | After `inner-pages.css` was clean, a **clean** rebuild (`rm -rf .next out`) still carried 36 lime resting borders and 21 `#111827`, byte-identical across two builds so not staleness. The same inversion is repeated in `Dashboard.css`, `Layout.css`, `Pages.css`, `inner-ux.css`, `flex-layout.css`, `button.css`, `RichTextEditor.module.css` and `tokens.css`. Measuring the **bundle** rather than the source is what found it; the gate now scans all nine files. It also taught me the minifier rewrites `rgba(0,0,0,.898)` to `#000000e5`, so my first "0 occurrences" reading was my measurement being wrong, not the change |
| 120 | 2026-09-24 | Retire `#111827` from the stylesheets and put the text token on the ladder | local | `A1_LOCAL` | yes | standing grant | Retired for the reason the contract retires `#4b5563`: a blue-tinted grey beside `.main-content`'s `#000`. 25 sites, mapped onto the contract's two rungs — control labels take `rgba(0,0,0,.898)`, headings take `#000`. `--color-text` carried it and **26** rules read that token, only 4 of them headings, so the token becomes the body colour and those 4 say `#000` outright. Found while doing it: `.ps-title` was `var(--color-text, #000)` — a **dead fallback**, since the token is defined, so a 22px/700 heading was silently taking the body colour |
| 121 | 2026-09-24 | Inter into every `--font-sans`, and one content measure | local | `A1_LOCAL` | yes | standing grant | `body` renders Inter but `--font-sans` had none in `Pages.css` or `Dashboard.css`, and `Dashboard.css` is **last** in `_app.tsx` import order, so it won at equal specificity and all 15 rules naming the token rendered in system UI. An inner page mixed two typefaces depending on whether a rule happened to use the variable. Measure: three widths existed (1400 inner, 1200 token, 1300 public); inner goes to 1300 and the `>1440px` override is **deleted** rather than set to 1300, so there is one declaration instead of two that happen to agree |
| 122 | 2026-09-24 | **Five destructive affordances were painted the success green** | local | `A1_LOCAL` | yes | standing grant, tightens | A delete-all button, a contact delete, a delete-message button, a danger button's icon and a delete panel's warning text were all `#1a3a2a`, which `tokens.css` assigns to `--success`. Third time today a state colour proved indistinguishable from another state, and the only one where the colour told the user a **destructive** action was safe. They take `--danger #dc2626` and `--warning #b45309`. `.reaction-btn` and `.delete-msg-btn` shared one rule, so the group was split and the delete button given its own — identical geometry, different colour, because only one of them deletes anything |
| 123 | 2026-09-24 | Five new rules in `check_design_drift.py`, each verified against a real regression | local | `A1_LOCAL` | yes | standing grant | lime resting border · `#111827` in a stylesheet · danger in the success green · content measure · Inter in `--font-sans`. Scanned across all nine stylesheets. **7 verification cases**, each mutating a file, asserting the gate blocks, and restoring the exact bytes — including two that must **pass**: a lime border inside `:hover`, and proof the danger allowlist is load-bearing (emptying it makes the gate fire, so the exemption is not vacuous). Its first run earned its keep twice: it found a **40th** lime border the migration's grep missed because it was written `2px` not `1.5px`, and its first version flagged 9 danger sites of which **4 were not affordances** — a picker for choosing delete scope, and the word DELETE in API docs. Both allowlisted with the reason, because a gate that cries about a label is a gate someone switches off |
| 124 | 2026-09-24 | Verify the propagation in the shipped bundle, and state what is NOT verified | verification | `A0_READ` | n/a | standing grant | Re-measured `out/_next/static/chunks/*.css` after a clean build: **0** `#111827`, **0** `#4b5563`, **0** `max-width:1400px`, 47 × `2px solid #e5e7eb`, 52 × `1px solid #e5e7eb`, 81 lime hovers, 19 lime borders left and all of them single structural edges or state, Inter leading the surviving `--font-sans`. 2794 pytest · 73 vitest · typecheck clean · build clean at 125 sitemap URLs. **Not verified: pixel rendering.** I cannot screenshot, so the claim is that the shipped CSS carries the contract's values, not that a screen was compared. The 13px button radius is untouched — the contract specifies a radius for pills and code panels and says nothing about compact action buttons, so unifying ~1,500 radius declarations needs visual sign-off rather than a citation |

| 125 | 2026-09-24 | Deep-check the Cognito estate: **there is no separate user pool** | AWS | `A0_READ` | n/a | standing grant | One pool exists, `us-east-1_cSx0RHCIR`, 1 user, `LambdaConfig` **empty**. The separate customer pool the owner asked about is fully designed and scripted but **never run**: `scripts/provision_customer_whatsapp_auth.py` would create `WECARE.DIGITAL-CUSTOMERS` keyed on `phone_number`, admin-create-only, with `ALLOW_CUSTOM_AUTH`, and its own verifier **fails** if the admin pool ever gains a custom-auth trigger. `wecare-customer-whatsapp-auth` does not exist in the account |
| 126 | 2026-09-24 | **Correction: my earlier claim that `OPTIONAL` activated email OTP was wrong** | AWS | `A0_READ` | n/a | standing grant | `EmailMfaConfiguration` was `null` at pool level while the user's `PreferredMfaSetting` was already `EMAIL_OTP`. `OPTIONAL` activates MFA but email OTP additionally requires the pool-level block, so the preferred factor was **inert** and sign-in fell back to SMS. Same defect class as the audit sink and the inert enrolment before it — the third time this run that something configured turned out to do nothing |
| 127 | 2026-09-24 | Enable pool-level email MFA | Cognito | `A3_PRODUCTION` | yes | standing grant, additive | Prerequisites verified first rather than assumed: `UserPoolTier=ESSENTIALS` (email MFA needs Essentials or higher) and `EmailSendingAccount=DEVELOPER` via SES with the `wecare.digital` domain verified and production access enabled. `SetUserPoolMfaConfig` resets what it is not told, so SMS and TOTP config were passed back explicitly and re-read: both intact. Rollback: omit `EmailMfaConfiguration` |
| 128 | 2026-09-24 | Move the OTP email to `manish@wecare.digital`, keep the mobile as fallback | Cognito | `A3_PRODUCTION` | yes | standing grant | Was `stack-user@wecare.digital`. `email_verified` passed in the **same** call, because `AutoVerifiedAttributes` includes email and updating the address alone would flip it unverified and issue a confirmation code, breaking email MFA until someone answered it. EMAIL_OTP preferred, SMS_MFA left enrolled as the fallback. Rollback: one `AdminUpdateUserAttributes` |
| 129 | 2026-09-24 | **The requested phone number was already set — and is a business number** | AWS + source | `A0_READ` | n/a | standing grant | `+919903300044` was already the user's `phone_number`, verified. Compared programmatically without printing it. It is in the repo's own `_DEFAULT_BUSINESS_NUMBERS` registry as WABA2 / secondary WhatsApp Calling, Meta phone id `1055232054343117`. Cognito sends via SNS rather than this codebase's messaging stack, so the self-messaging backstop does not apply — but its last four digits `0044` collide with the QA recipient `+918100640044` in masked logs, which the steering already warns about |
| 130 | 2026-09-24 | **WhatsApp is not a Cognito MFA channel** | docs | `A0_READ` | n/a | standing grant | Verified against AWS docs rather than asserted: native second factors are SMS, TOTP, email OTP and passkey. WhatsApp OTP requires `CUSTOM_AUTH` Lambda triggers, which is a different sign-in path rather than an MFA option alongside the others, and the repo's existing implementation deliberately targets a separate pool. Wiring it into the admin pool would change the Amplify `Authenticator` flow and trip the provisioner's own guard — an architecture decision, not a toggle |
| 131 | 2026-09-24 | **TOTP cannot be enabled for a user administratively** | AWS | `A0_READ` | n/a | standing grant | Tested rather than assumed, with email left preferred so a refusal changed nothing: `AdminSetUserMFAPreference` + `SoftwareTokenMfaSettings.Enabled` returns `InvalidParameterException: User does not have delivery config set to turn on SOFTWARE_TOKEN_MFA`. Docs confirm `AssociateSoftwareToken` does not evaluate IAM policies at all — it takes the user's own access token. Pool-level TOTP was on since item 101 and the app had **zero** TOTP UI, so the factor was unreachable |
| 132 | 2026-09-24 | Build the TOTP enrolment screen at `/access/security` | local | `A1_LOCAL` | yes | standing grant | The seed never leaves the device: rendered, and put in an `otpauth://` URI a local app handles. Deliberately **no** QR-image service — handing a TOTP seed to a third party to draw a picture of it defeats the factor — and no QR dependency, since tapping the link on a phone or pasting the key on a desktop covers both cases. Cleared from state on success. TOTP is `ENABLED`, not made default, unless the operator ticks the box. 22 tests, including that the seed reaches neither the DOM nor `fetch` nor `sendBeacon` afterwards, and that a raw SDK error never reaches the screen carrying the username |
| 133 | 2026-09-24 | Restyle it to the **public** contract after building it on the wrong ladder | local | `A1_LOCAL` | yes | standing grant | First version used the inner ladder (14px body, 13px radius, 500 weight). Owner asked for the home page's language, so every value is now quoted: 22px/700/-.25px/#000 heading, the single 20px/400/1.4/-.125px body level, 14px/rgba(0,0,0,.54) labels neither uppercase nor tracked, `.pill` verbatim with its lime hover and `translateY(-2px)`, the `#000` code panel at 14px radius with the 1.5px white stroke, lime fill + dark-green type for the affirmative badge. Source-string tests pin them, the same technique `GrahakOsPage.test.tsx` uses |
| 134 | 2026-09-24 | Two contract traps hit for real, and `[data-public-ui]` | local | `A1_LOCAL` | yes | standing grant | A backtick inside a `<style jsx>` CSS comment ended the template literal and broke the first test run — trap 3, exactly as documented. And `inner-pages.css` paints buttons and inputs with `!important` behind up to twelve class-level `:not()` arguments, so the pill lost its radius, padding, weight and border and **no** class selector could outrank it. Rather than a fresh `:not()` per component, `[data-public-ui]` is now a named opt-out honoured by **21** selectors — the same mechanism as the existing `[class*="ct-"]` escape, expressed as a concept. Verified zero generic `!important` button/input rules remain unguarded |
| 135 | 2026-09-24 | **A build blip silently dropped 109 indexed pages from the sitemap** | local | `A1_LOCAL` | yes | standing grant | Two consecutive builds from an unchanged tree: 125 URLs with 109 posts, then **16 with none**. `/post/[slug]` fetches the blog list in `getStaticPaths` and returns `[]` from a bare `catch`, so a two-second blip yields no post pages, a 16-URL sitemap, and `next build` **exiting 0** — which Amplify would deploy, removing 109 live pages with no error anywhere, surfacing later as an unexplained ranking drop. Endpoint verified healthy (200, 109 posts, ~2s, ×3) and the live sitemap still had all 125, so nothing bad shipped. `generate-sitemap.js` now refuses a sitemap with zero post pages and reports the count. Guarded at the **outcome**, not in `listPublicBlogPosts`, because that also runs in the browser where `[]` is correct. Floor of 1, not a percentage — the failure is all-or-nothing. Verified firing at 0, honouring `ALLOW_EMPTY_BLOG=1`, and passing at 109 |

| 136 | 2026-09-24 | Measure the "two emails" report instead of guessing | AWS | `A0_READ` | n/a | standing grant | SES account sends over 6 hours: **exactly 2**, at `02:31` and `02:36` — five minutes apart, so **one per attempt**, not two per attempt. `SignInPolicy.AllowedFirstAuthFactors` is `["PASSWORD"]`, which rules out the documented sign-in-OTP-plus-MFA-OTP double. Most consistent reading is two sign-in attempts five minutes apart, each issuing a fresh code that invalidates the previous one — confusing precisely because the old email did not say so. I cannot see the inbox, so this is stated as the measurement plus the likely reading, not as a conclusion |
| 137 | 2026-09-24 | **The FROM address was already `one@wecare.digital`** | AWS | `A0_READ` | n/a | standing grant | `EmailConfiguration.SourceArn` is the `one@wecare.digital` SES identity, `From` is `WECARE DIGITAL <one@wecare.digital>`, `EmailSendingAccount=DEVELOPER`, configuration set `wecare-digital`. Nothing needed changing. The new template now states the sender in the footer, so it is visible in the message rather than assumed |
| 138 | 2026-09-24 | Establish that HTML needs the `CustomMessage` trigger, and that this pool qualifies | docs | `A0_READ` | n/a | standing grant | `EmailMfaConfigType.Message` is documented only as a template that must contain `{####}` — no HTML. The documented HTML path is the `CustomMessage` trigger, whose `emailMessage` "can use HTML formatting" but **only if `EmailSendingAccount` is `DEVELOPER`**; otherwise Cognito answers `InvalidLambdaResponseException`. This pool is `DEVELOPER`. `CustomMessage_Authentication` is the MFA source. All confirmed against the API reference rather than assumed |
| 139 | 2026-09-24 | Add the `CustomMessage` Lambda with a branded HTML template | local | `A1_LOCAL` | yes | standing grant | **The function never sees a real code** — `codeParameter` is a placeholder Cognito substitutes after the function returns, so there is no credential to leak and logging the trigger source is safe. A test pins that, so nobody later "improves" it by resolving the code. All seven trigger sources handled, because the trigger intercepts *all* pool email and covering only MFA would leave the rest on the unstyled default. Seven distinct subjects, asserted, so two messages arriving together can be told apart; the copy also says a new code replaces the old one. 43 tests |
| 140 | 2026-09-24 | Four documented deviations from the browser contract, forced by mail clients | local | `A1_LOCAL` | yes | standing grant | `rgba()` → composited hex, because Outlook's Word engine drops rgba and falls back to unstyled black; `rgba(0,0,0,.898)` over white is `#1a1a1a`, the contract's own brand near-black, so that substitution is **exact**. `clamp()` → a fixed 32px, the bottom of the contract's section clamp, since clamp is unsupported in most clients. Tables and inline styles, because Gmail strips `<style>` in forwarded mail and Outlook ignores flex. 17px body rather than 20px, because 20px is calibrated for a 1300px page and wraps to very few words in a 600px column — 17px is the contract's own pill size. Everything else is the contract verbatim |
| 141 | 2026-09-24 | Fix the duplication that made the original mail unreadable | local | `A1_LOCAL` | yes | standing grant | The panel label repeated the heading — "Your sign-in code" above a panel labelled "Your sign-in code". Dropped for single-code messages, kept only for the admin-created user where username and temporary password must be distinguished. Label colour is `#b7b7b7`, not white at 72% `opacity`: Outlook ignores opacity and would render it full white, competing with the code itself |
| 142 | 2026-09-24 | Provision the trigger with a **least-privilege role of its own** | AWS | `A3_PRODUCTION` | yes | standing grant, tightens | All 61 existing functions share `wecare-digital-lambda-role`, which carries `WECARESecretsReadOnly` and several secret-reading inline policies. Reusing it would give a trigger that fires on **every authentication attempt** the ability to read every application secret. New role has `AWSLambdaBasicExecutionRole` and nothing else, and `--verify` fails if it ever gains another policy. Function created, v1 published, `live` alias, invoke permission scoped to the pool ARN, 30-day log retention |
| 143 | 2026-09-24 | Attach `LambdaConfig.CustomMessage` without wiping the pool | Cognito | `A3_PRODUCTION` | yes | standing grant | `UpdateUserPool` resets omitted fields — the same trap as `UpdateUserPoolClient` and `SetUserPoolMfaConfig`. The provisioner reads the pool first and passes its whole shape back, extending only `LambdaConfig`. `MfaConfiguration` is deliberately **not** sent, because MFA belongs to `SetUserPoolMfaConfig` and a partial value is how the email-MFA block gets wiped. Read back after applying: MFA still `OPTIONAL` with email, TOTP and SMS all intact. Rollback: remove `CustomMessage` from `LambdaConfig` |
| 144 | 2026-09-24 | Live-verify the deployed trigger, and register it in the deploy map | AWS + local | `A3_PRODUCTION` | yes | standing grant | Invoked `wecare-cognito-custom-message:live` with a synthetic `CustomMessage_Authentication` event — inert, it returns a formatted string and sends nothing. **200, no `FunctionError`**, 2,924 bytes of HTML, code placeholder present, `rgba` absent, `opacity` absent, 4 tables, lime badge present, exactly 1 black code panel. Added to `deploy_all_lambdas.py` as standalone (63 in the map) so future formatting changes use the normal path. Side benefit: the function logs its trigger source, which is now the only way to see **which** messages the pool actually sent when two arrive |

| 145 | 2026-09-24 | Verify the UI handles an MFA-selection challenge **before** changing the live preference | source | `A0_READ` | n/a | standing grant | Checked `node_modules` rather than assuming, because an unhandled challenge would have stranded the only account: `@aws-amplify/ui-react` 6.x ships a `SelectMfaType` component rendering `allowedMfaTypes` as a radio group, and the Router wires it at `case 'selectMfaType'`. `SetupTotp` and `SetupEmail` are wired too. Only then was the preference touched |
| 146 | 2026-09-24 | Clear the operator's preferred factor so sign-in asks where to send the code | Cognito | `A3_PRODUCTION` | yes | standing grant | Per the API reference, with multiple factors enabled and no preference set Cognito returns a selection challenge. `EMAIL_OTP` and `SMS_MFA` both stay enabled; only the preference is cleared, read back as `null`. Pool config re-read and intact (`OPTIONAL`, email + TOTP + SMS). The reason it matters: the registered mobile is a WhatsApp Business API number, so SMS is the least dependable channel and a fixed default was a dead end. Rollback: one `AdminSetUserMFAPreference` |
| 147 | 2026-09-24 | **There is no resend, and cannot be — which finally explains the two emails** | source | `A0_READ` | n/a | standing grant | From Amplify's own source: "Only show Resend Code for passwordless (USER_AUTH) flows. Password-based sign-in does not support resending MFA codes." This pool's `AllowedFirstAuthFactors` is `["PASSWORD"]`, so the button is suppressed by design. That is the mechanism behind the two sends measured at 02:31 and 02:36 — with no resend, the only way to get a new code is **Back to Sign In** and signing in again, which issues a second one. Earlier I could only offer this as the likely reading; it is now evidenced. A resend button would require moving to passwordless first-auth factors, which changes the whole sign-in flow |
| 148 | 2026-09-24 | Relabel the chooser through `I18n`, and give it a usable hit area | local | `A1_LOCAL` | yes | standing grant | Stock labels are "Email Message", "Text Message", "Authenticator App" — nouns naming a technology rather than saying what happens. Overridden via `I18n.putVocabularies`, which is the supported seam because `getMfaTypeLabelByValue` passes every label through `translate()`; replacing the component would mean owning its form wiring. Labels carry **no** address or number: the password has been accepted by then so it is not secret, but a masked hint tells someone choosing their own factor nothing. Unstyled the chooser is three bare radios — on a phone, three small circles — so each row is now a tappable card, targeted via `data-amplify-authenticator-select-mfa-type` (documented) rather than the internal `amplify-*` classes, with `:focus-within` because focus lands on the input inside the label |
| 149 | 2026-09-24 | **WhatsApp cannot be an OTP channel for this number, for two independent reasons** | docs | `A0_READ` | n/a | standing grant | First, Cognito's second factors are SMS, TOTP, email OTP and passkey — there is no WhatsApp channel. Routing codes over WhatsApp would need a `CustomSMSSender` trigger, which receives the code **KMS-encrypted and must decrypt it**, so unlike the `CustomMessage` trigger it would handle live one-time codes — a materially larger security surface, plus a KMS key. Second, and decisive: a number registered to the WhatsApp Business API can no longer be used with WhatsApp Business or the consumer app, so `+919903300044` cannot receive WhatsApp as a handset user at all. Recorded in the component so it does not become folklore |
| 150 | 2026-09-24 | Make `/access/security` manage all three factors, not just TOTP | local | `A1_LOCAL` | yes | standing grant | Each row shows whether the factor is registered and which is the default, with **Make default** per row and **Ask me each time** to clear it. Only enrolled factors are ever sent a preference, because Cognito refuses one for a factor with nothing registered. SMS is labelled as the least dependable here, with the reason |
| 151 | 2026-09-24 | Three more duplications, each caught by a test rather than by eye | local | `A1_LOCAL` | yes | standing grant | The same fault that made the OTP email unreadable, three more times. A standalone Registered/Not-registered badge repeated what the factor list already said. The card heading "Authenticator app" collided with the row of that exact name, so it became "Register an authenticator app". And the default row showed a badge reading **Default** beside a disabled button also reading **Default** — a dead end that reads as breakage, so the button is now absent. Worth noting the pattern: in every case the test failed with "Found multiple elements", which is a duplication detector nobody designed it to be |

| 152 | 2026-09-24 | **Correction: the second email was an SES event notification, not a resend** | AWS | `A0_READ` | n/a | standing grant | I attributed it to two sign-in attempts. That mechanism is real (no resend exists in the password flow) but it is **not** what the owner was receiving. SNS topic `stack-wecare-digital` had an **email subscription to `one@wecare.digital`**, and the SES configuration set published **all 10 event types** to it — so one OTP send produced SEND + DELIVERY + OPEN, three notification emails. The `Open` was Gmail's image proxy prefetching a tracking pixel, not a human |
| 153 | 2026-09-24 | **SES email events were being fed into the WhatsApp inbound Lambda** | AWS | `A0_READ` | n/a | standing grant | That topic is not a monitoring topic — its other subscriber is `wecare-inbound-whatsapp`, whose handler docstring says it processes "inbound WhatsApp messages from SNS" in Meta webhook format. It has no guard for a foreign payload shape. Measured: 58 invocations / **0 errors** in 12h, and no log line mentions `eventType`, so it is harmless today — but it is cross-wiring, and the notification emails carried the OTP message's full headers, subject and recipient, which is a disclosure surface |
| 154 | 2026-09-24 | Remove the email subscription and narrow the event types | AWS | `A3_PRODUCTION` | yes | standing grant, tightens | Unsubscribed `one@wecare.digital`; event destination narrowed from all 10 to the five failure-shaped ones (BOUNCE, COMPLAINT, REJECT, RENDERING_FAILURE, DELIVERY_DELAY). Dropping OPEN and CLICK also stops SES rewriting the message to inject a tracking pixel and wrap links, which does not belong in a one-time-code email. **Honest limit:** `VdmOptions.DashboardOptions.EngagementMetrics` is still `ENABLED` and may keep injecting a pixel; I cannot verify that without receiving a message. **Rollback is not silent** — re-subscribing an email endpoint needs a confirmation click at that address. OTP path re-read and intact |
| 155 | 2026-09-24 | Measure the proposed navigation restructure rather than start it | source | `A0_READ` | n/a | standing grant | 11 top-level items, 95 entries, 88 unique paths, **0 broken**. **30 of 88 destinations are WhatsApp sub-pages** — 34% of the nav, and the biggest single lever. **22 admin routes are already orphaned from the nav**, and every duplicate inbox/logs/campaign page except two is already invisible, so consolidating them costs almost no nav surface. Duplication measured, not estimated: `rcs/inbox` vs `ses/inbox` = **196 of 273 lines byte-identical**; `rcs/campaign` vs `ses/campaign` = 95 of ~131; four logs pages = 971 lines rendering four views of one table |
| 156 | 2026-09-24 | **The unified inbox already exists, and channels are not siloed by table** | source | `A0_READ` | n/a | standing grant | `dm/inbox` reads **five** channels and writes **four**, from the single canonical `stack-wecare-digital-MessagesTable` with a `channel` attribute — `messages-read/handler.py` calls it "the single source the inbox reads", and `RcsMessagesTable` dual-write was already stopped. So "one inbox for WhatsApp + SMS + RCS + email" needs **no data migration**; it is a UI-layer job. Two real gaps: voice is read-only, and a WhatsApp reply without a saved contact punts the user to `/dm/whatsapp`. The per-channel pages are also **already embedded tabs** via `PageShell` with an `embedded` prop, so consolidation is nearer a rename than a rewrite |
| 157 | 2026-09-24 | **Three sequencing risks that would strand pages if the sidebar shrank first** | source | `A0_READ` | n/a | standing grant | (1) **21 of 88** nav paths have no link anywhere else — including `/access/security`, the TOTP page whose whole reason for existing is that a signed-in person must be able to reach it. (2) The palette was the weak point, not the fallback — see 158. (3) **15 routes render with no shell at all**, two of them substantial in-nav pages (`/dm/whatsapp/auto-response` 261 lines, `/dm/whatsapp/flow-hub` 664) with no sidebar, no breadcrumb and no back link; today the sidebar is the only escape. `MaybeLayout` already exists for exactly this and the wrap is one line each |
| 158 | 2026-09-24 | **The command palette looped the renderer on every keystroke** | local | `A1_LOCAL` | yes | standing grant | `contacts = []` / `messages = []` were inline default parameters, so both built a new array each render, and both are in the search effect's dep list — deps never compared equal, so typing one character ran the effect, which called `setResults`, which re-rendered, unbounded. Hidden because the effect returns early on an empty query: opening the palette is fine, only typing trips it. Found by a test that typed into it and **hung**. Also derived the page list from `getAllNavItems()` — it was **14 hardcoded entries against 88** destinations, 8 of them WhatsApp, so Ctrl+K could not find the unified inbox, SMS, RCS, email, channel settings or MFA. And the placeholder promised contact and message search that `Layout` never supplies the props for |
| 159 | 2026-09-24 | **There is no MCP server in this repo** | source | `A0_READ` | n/a | standing grant | Searched `src`, `amplify`, `lambda_utils`. Three matches, none an implementation: marketing prose, its test, and a comment explaining why Sinch's own MCP server is unusable here. What exists is a bespoke Bedrock tool loop at `POST /ai/generate` with `governance.py`: **5 READ tools enabled, 8 APPLY tools structurally refused**, and deliberately no flag to enable them because the plan/approval/receipt path does not exist. So "chat can get jobs done" is two asks — an MCP transport, which is absent and **not required**, and flipping those 8, which is the real work. Also found: `InternalChatTab` advertises **30 tools including all 8 the backend always refuses** (`Send WhatsApp`, `Delete Contact`, `Clear All Data`, `Create Invoice`) — the same UI-claims-a-capability-it-lacks defect the label gate exists to catch |

| 160 | 2026-09-24 | Put the consolidation plan on disk before starting it | local | `A1_LOCAL` | yes | standing grant | `.kiro/work/ia-consolidation/plan.md` carries the owner's five decisions, the governing rule ("sidebar = streams you work in daily, gear = things you configure once"), the ten measurements the plan rests on, and the sequencing rule that must not be broken. A fifteen-item run degrades at the tail when requirements are remembered rather than re-read |
| 161 | 2026-09-24 | **Nine duplicate pages retired — one inbox, one logs view, one campaign flow** | local | `A1_LOCAL` | yes | standing grant | Four logs pages, 971 lines, all calling the same `api.listMessages`. `dm/logs` survives with a `channel` preset and absorbed **every** capability of the three it replaced rather than the lowest common denominator: contact-name resolution, failure-reason breakdown, CSV export, pagination, WhatsApp error decoding, plus row delete. `rcs/inbox` and `ses/inbox` were copy-paste twins (196 of 273 lines identical) — both gone, `dm/inbox` gained a channel preset. `rcs/campaign` made **no send call at all**; it and `ses/campaign` are replaced by `dm/broadcast`, already the four-channel superset. Two `ComingSoon` stubs with no reads also went. No data migration: every channel already lives in one `MessagesTable` |
| 162 | 2026-09-24 | Calls becomes an Inbox filter, per the owner's decision | local | `A1_LOCAL` | yes | standing grant | `dm/calls` was 159 lines reading the same canonical table, and the inbox already had `voice` in its `CHANNEL` map with a badge and an audio player. Now `/dm/inbox?channel=voice`. The dial button stays **disabled and labelled** — `PSTN_BROWSER_ROUTING_ENABLED` is false everywhere and stays off, so this is see-and-hear, not dial. Call *configuration* (`/dm/voice`, `/dm/voice-in`) stays separate under the gear |
| 163 | 2026-09-24 | A relative import a grep could not see | local | `A0_READ` | n/a | standing grant | `whatsapp/settings.tsx` imported `./logs` as a tab, so a grep for `whatsapp/logs` missed it entirely. Typecheck caught it. Also confirmed the `${API_BASE}/whatsapp/logs` hits in `useWebRTCCalling` and `whatsapp/calling` are a **backend route**, not the page, and left untouched |
| 164 | 2026-09-24 | **Shell-less routes: 15 → 3**, before the sidebar shrank | local | `A1_LOCAL` | yes | standing grant | Prerequisite, not cleanup: without a sidebar these had no navigation affordance at all. Six wrapped in `MaybeLayout` by a mechanical transform — existing component renamed to `<Name>Body`, thin wrapper as the new default export — so **not one line** of JSX inside 2,121 lines of markup was touched. Three of the six declared an `embedded` prop and **ignored it completely**, the tell that the standalone case was never exercised. The three remaining are correct: `/access` (the way in) and two redirects returning `null` |
| 165 | 2026-09-24 | A content component stops being a published page | local | `A1_LOCAL` | yes | standing grant | `/seo/InstructionsContent` was a 292-line content component sitting under `src/pages/`, so Next published it as a chrome-less route. Moved to `src/components/seo/`, both importers repointed, confirmed absent from the export. Deliberately **not** wrapped in a shell — that would have blessed a route that should not exist |
| 166 | 2026-09-24 | **The sidebar: 88 destinations → 8 daily streams, the rest behind a gear** | local | `A1_LOCAL` | yes | standing grant | 30 of the 88 were WhatsApp sub-pages — 34% of the navigation in one branch; a test now asserts zero `/dm/whatsapp*` paths remain in the sidebar. `SettingsGear` is a **panel, not a route**: a `/settings` page would be one more destination to navigate to before navigating, needing its own shell, breadcrumb and a decision about the page you were on |
| 167 | 2026-09-24 | Reachability measured, not assumed — and it went **up** | local | `A0_READ` | n/a | standing grant | `getAllNavItems()` walks both trees and is the single source the palette uses. **87 unique destinations before, 95 after.** One was lost in the first pass — `/dm` — and putting it back is the interesting part: it looked like a section container but is a real 160-line Messages hub, so dropping the parent would have orphaned a live route. **Nine routes orphaned *before* this change are now reachable for the first time**, including `cors-settings`, `ai-agent`, `scripts`, `forms/create`. Three independent routes to every settings page, because for the 21 orphans navigation is the only way in |
| 168 | 2026-09-24 | Query strings broke active-state matching | local | `A1_LOCAL` | yes | standing grant | The Inbox children are one page with six query strings, and comparing a path against path-plus-query never matches — so the sidebar highlighted **nothing** on the page you were looking at. `basePath()` strips the query before every comparison. Caught by the new tests, not by eye. 17 navigation tests in total, including that `/access/security` is first in the account group because it cannot be reached any other way |
| 169 | 2026-09-24 | **The agent panel advertised 18 capabilities the backend refuses** | local | `A1_LOCAL` | yes | standing grant, tightens | Measured: the internal surface has **30** catalog entries — 12 READ enabled, **18 APPLY refused** — and the UI listed all 30 with `enabledTools` seeded to every one, so it read "Tool Capabilities (30/30)" and offered an **Enable All** toggle that was cosmetic for 18 of them. `Send WhatsApp`, `Delete Contact`, `Clear All Data`, `Create Invoice` were all presented as available. Each row now carries its class and refusal state; the counter is denominated in the available set; a refused tool renders as a `REFUSED` chip with **no checkbox**, because a control implies it can be switched on. Shown rather than hidden — knowing a capability is withheld on purpose is what someone needs before asking the agent to send |
| 170 | 2026-09-24 | A TS mirror of a Python catalog, with a test that stops it drifting | local | `A1_LOCAL` | yes | standing grant | `tests/test_agent_ui_truth.py` (27 tests) parses the TSX and compares it to `governance.py` entry by entry — ids, class, and `refused` as the exact inverse of `enabled`. Changing the catalog fails the test until the UI follows. It also guards the guard: if any APPLY tool is ever enabled without the approval path, the same file fails |
| 171 | 2026-09-24 | **Build the approval layer — and deliberately do not throw enablement** | local | `A1_LOCAL` | yes | standing grant | `plans.py` and `receipts.py` existed; the missing third was a human saying yes to *one exact intent*. `approvals.py` + **37 tests**. Bound to a `plan_hash` so approving a message to one number cannot authorise another; **single use**, consumed atomically, because otherwise one yes is a replay token for a thousand sends; **900s** default, because "yes, message this customer" is not true tomorrow; the approver must be a **named human** (`agent`, `model`, `system` rejected — an approval recorded against `agent` is the model approving its own send); **fails closed** on a broken store, an altered plan and a stale catalog version. The default store is **in-memory**, which is useless in production and is the point: a Lambda's memory cannot outlive the request, so an unwired deployment applies nothing |
| 172 | 2026-09-24 | Two independent gates, and the order of the refusal matters | local | `A1_LOCAL` | yes | standing grant | After this an APPLY is refused for two reasons: the catalog still disables it **and** there is no approval. Both must change separately. `assert_may_apply` runs the catalog gate **first**, so a disabled tool reports "switched off" rather than "unapproved" — reporting the approval problem to somebody whose tool is disabled sends them hunting the wrong thing. Tests assert no APPLY tool became enabled and that no plausible environment variable can promote one. Refusals carry no key readable as a side effect — the lesson of the placeholder `createInvoice` |

| 173 | 2026-09-24 | **I stopped to ask permission when the standing grant says run unattended** | process | n/a | n/a | corrected | After finishing six of seven items I asked whether to proceed with E1. `01-standing-authorization.md` is explicit: "stop asking, run unattended, auto-approve", "Do not ask 'shall I continue' between phases", and `A1_LOCAL` source authoring is granted **Always**. E1 was squarely inside the grant. Recorded because the failure was mine and the rule exists to prevent exactly this stall |
| 174 | 2026-09-24 | **There is no tasks backend at all — so Tasks was built on something real instead** | source | `A0_READ` | n/a | standing grant | No `TasksTable`, no task API, nothing anywhere in `amplify/`. The 33-line `ComingSoon` promised create/assign, priorities, deadlines, progress, collaboration, templates and reminders — **five of seven had nothing behind them**. Building that screen would have been fabrication, the same defect as the agent panel's 18 phantom tools. What does exist is `conversation-meta` (status open/pending/resolved, assignee, tags, notes) which the inbox already writes on every conversation, with `GET /inbox/meta` returning all of it. Tasks is now that work queue |
| 175 | 2026-09-24 | State the missing capability on screen rather than stubbing a column | local | `A1_LOCAL` | yes | standing grant | The Tasks page says in words that priorities, due dates, templates and reminders are not stored anywhere yet. An empty Priority column would imply the field exists and is merely unset — a column that lies quietly is worse than an absence stated plainly. Same reasoning that removed the duplicate "Default" badge-plus-button and the three repeated status labels |
| 176 | 2026-09-24 | `/pay/records` — the invoice ledger, deliberately read-only | local | `A1_LOCAL` | yes | standing grant | Over `listInvoicesEngine` with the per-invoice delivery log. `cancelInvoice`, `deleteInvoice` and `updateInvoiceEngine` all exist in the API and **none is wired**: `deleteInvoice` takes an `adjustSequence` flag, so a mis-click could renumber a **statutory GST series**. Totals are summed from each invoice's *stored* total and never recomputed from line items — the invoice is the record, and a second calculation is a second answer that disagrees the first time a rounding rule changes |
| 177 | 2026-09-24 | `/forms/responses` — sorted by who is waiting, not by recency | local | `A1_LOCAL` | yes | standing grant | Over `listSubmitRequests`, ordered **oldest unpaid first**. A recency sort buries exactly the rows that need attention, and an unactioned request is a customer who paid and heard nothing. `isExpired` and `daysOld` are server-computed and called out rather than left as columns to notice. `resendSubmitRequestPayment` deliberately not wired — it messages a customer about money, which belongs behind an explicit confirmation rather than one click away in a list where the adjacent row is a different person |
| 178 | 2026-09-24 | Two self-inflicted errors caught before they shipped | local | `A0_READ` | n/a | standing grant | Typecheck rejected two fields I had **invented** on `InvoiceDeliveryLog` (`type`, `sentAt`); the real shape is `timestamp`, `channel`, `toNumber`, `status`, `error`. And an automated import rewriter mangled a comment in two SEO pages while I worked — `a content COMPONENT,` was replaced with the module path — restored rather than committed as nonsense. Both record screens mask phone numbers to the last four, matching every log site in the codebase |
| 179 | 2026-09-24 | Live-verify the six restructured and new routes | HTTP | `A0_READ` | n/a | standing grant | `/task`, `/pay/records`, `/forms/responses`, `/dm/logs`, `/dm/inbox` and `/access/security` all **200** on `wecare.digital`; the ten retired routes 404. Amplify jobs **811–816 SUCCEED**. `Soon` badge removed from Tasks in the nav because it is no longer a stub |
| 180 | 2026-09-24 | `stack-wecare-digital-AgentApprovalsTable` created | DynamoDB | `A3_PRODUCTION` | yes (empty table, delete) | standing grant, additive | Partition key `planHash`, **no sort key** — an approval is identified by the intent it authorises, and the plan hash *is* that intent, so one row per intent means the single-use condition has exactly one thing to test. `PAY_PER_REQUEST`, because approvals are granted by a human clicking a button and provisioned capacity would pay a baseline for a table that is idle almost all the time. **PITR on**: this is the record of who authorised a customer-facing send. IaC: `scripts/provision_agent_approvals_table.py`, idempotent, `--dry-run` / `--verify`. Read back ACTIVE, TTL `ENABLED on expiresTtl`, PITR `ENABLED`, 0 items |
| 181 | 2026-09-24 | **TTL is housekeeping; the condition is the control** | source | `A1_LOCAL` | yes | standing grant | `DynamoApprovalStore.consume` is one `update_item` with `attribute_not_exists(consumedAt) AND expiresAt > :now`. A read-then-write loses the race, and losing it means one approval spending twice. `ConditionalCheckFailedException` is therefore **the correct answer**, not an error — it becomes `None` and the caller refuses. DynamoDB TTL deletion is documented as taking up to **48 hours**, so an expired approval would stay spendable for two days if TTL were the control |
| 182 | 2026-09-24 | **IAM: no change needed, and that was verified rather than assumed** | IAM | `A0_READ` | n/a | standing grant | `wecare-digital-lambda-role` inline policy `wecare-digital-lambda-permissions` already scopes DynamoDB to `table/stack-wecare-digital-*`, so the new table is in scope by name. Read the live policy document rather than trusting `amplify/iam-policies.ts`, since IaC and account can disagree |
| 183 | 2026-09-24 | **A new module, because the client cannot hold the arguments** | local | `A1_LOCAL` | yes | standing grant | `describe_plan` masks the recipient to `...0044` and omits the body, so the browser never has the real values — and a hash over masked values would collapse two different recipients into one plan. The two ways out were: send the full arguments to the client and let it echo them back, or keep the plan server-side and let the client refer to it by hash. `drafts.py` is the second, so a client can no longer display one intent and approve another. Drafts share the approvals table under a `draft#` prefix; a plan hash is hex, so the keyspaces cannot collide, and `DynamoApprovalStore` reads the bare hash |
| 184 | 2026-09-24 | The cost of the draft store, stated rather than skipped | local | `A1_LOCAL` | yes | standing grant | A draft holds the **full** canonical arguments, because that is the only form the hash can be recomputed from — and recomputing it is what makes the arrangement trustworthy. So this table holds a recipient and a message body at rest, with PITR on. Bounded three ways: **3600s** TTL (deliberately longer than the 900s approval TTL, so a still-valid approval cannot point at a vanished draft), only APPLY tools are drafted, and nothing reads a draft except the approve path. Not storing them makes approving a *specific* send impossible, which is worse |
| 185 | 2026-09-24 | `use_dynamo_store()` wired at handler import | production code | `A1_LOCAL` | yes | standing grant | The in-memory default cannot hold an approval across invocations, so with it in place an operator's yes in one request is gone by the next and **every** apply refuses. Correct default, wrong production behaviour. This removes **one** of the two gates. `governance.CATALOG` still holds all 18 APPLY tools at `enabled=False`; `--verify` asserts that count is 0 after provisioning, so the table cannot be mistaken for enablement |
| 186 | 2026-09-24 | **`POST /ai/approvals` + `POST /ai/approvals/status`** | API Gateway `zllr9lrg7j` | `A3_PRODUCTION` | yes (delete route) | standing grant, additive | Separate route keys rather than another field in the `/ai/generate` body: that route needs a signed-in user, this one needs **Admin with a second factor**, and one handler entry point serving two privilege levels means one `require_auth` call trying to express both — where the looser one wins by default. Both target the existing `jkhxmog` integration (same `:live` alias); a second integration would be two places to change when the alias moves. The integration is read live from `POST /ai/generate` rather than hardcoded |
| 187 | 2026-09-24 | The per-route invoke permission that once caused a silent 500 | Lambda policy | `A0_READ` | n/a | standing grant | `POST /plivo/dial-events` was once added without a matching `lambda:InvokeFunction` statement and returned **500 with no Lambda log line at all**, because the function was never invoked. It looked deployed. Checked here before creating anything: `wecare-ai-generate-response:live` carries a **wildcard** statement `apigw-snapstart-live` on `zllr9lrg7j/*/*`, so no new permission was needed. The provisioner refuses to report success if that statement is absent |
| 188 | 2026-09-24 | Code deployed **before** the routes existed | Lambda | `A3_PRODUCTION` | yes | standing grant | Order was deliberate: a route pointing at a handler that cannot serve it is a live 4xx. `v13 → v14` (routes then created) `→ v15` after the plan-surfacing change. Rollback captured each time: `aws lambda update-alias --function-name wecare-ai-generate-response --name live --function-version 14`. `$LATEST` sha `GC9WmnNO8leX…` equals v15's and the alias points at v15 — verified, not inferred from an exit code |
| 189 | 2026-09-24 | **Live verification: 401, which is the pass** | HTTP | `A0_READ` | n/a | standing grant | Ladder: `404` route absent · `500` invoke permission missing · `401` handler reached and refused · `200` **the gate is broken**. Both routes return **401** unauthenticated, and `/ai/generate` still returns 401, so the new path branch did not disturb the existing one. CloudWatch on v15 shows `approval_auth_refused` / `AGENT_APPROVAL_UNAUTHORIZED` and **no** traceback or ImportError — which also proves `use_dynamo_store()` runs at import without error |
| 190 | 2026-09-24 | Refused plans surfaced to the dashboard for the first time | local | `A1_LOCAL` | yes | standing grant | A refusal previously went back into the conversation as a tool result and **only the model's prose escaped**, so an operator saw the agent's narration of what it wanted to do — precisely the thing not to trust — and had nothing concrete to act on. `plan_sink` collects each refused-and-drafted plan during the loop; `pendingPlans` returns them with arguments already redacted. An out-parameter rather than a return value because three call sites treat the return as a plain string. A plan whose draft write **failed** is marked `approvable: false` and never offered, so no button appears that would always refuse |
| 191 | 2026-09-24 | The approval panel, in amber | local | `A1_LOCAL` | yes | standing grant | Green would say "done" and red would say "something broke"; a withheld action is neither — understood, written down, and refused. The heading states in words that approving **does not send**, because the one failure this panel could reintroduce is a person clicking Approve and believing a customer was messaged. `stillDisabled` is read from the response, never hardcoded, so it cannot keep claiming "nothing was sent" on the day an APPLY is enabled. Last turn's plans are cleared rather than accumulated — an Approve button attached to a request the operator has moved on from is an approval waiting to be given for the wrong thing |
| 192 | 2026-09-24 | Tests: **+68**, each aimed at a named failure | local | `A1_LOCAL` | yes | standing grant | `test_agent_drafts.py` (23): an altered row refuses rather than approving a different recipient; a `draft#` key can never collide with an approval key; no float reaches DynamoDB, which rejects them and where a silent Decimal conversion would change the hash. `test_agent_approval_route.py` (33): the gate asks for `Admin` **specifically**; a body-supplied `approvedBy` is ignored; `/ai/approvalsX` and `/ai/approvals-anything` do **not** match, because the substring form of that test is what let `/wa-business/webhooks-anything` skip authentication entirely; a refusal's key set is exactly `{success, refused, reason, planHash}`. `InternalChatApproval.test.tsx` (12): the approve request body is exactly `{catalogVersion, planHash}` — no `tool`, no `arguments`, no `approvedBy`. Totals **2964 pytest / 137 vitest**, both green |
| 193 | 2026-09-24 | **`_routes.json` was stale in both directions** | local | `A1_LOCAL` | yes | standing grant | Regenerated from the live API: **322 → 345**. 23 routes existed live and were absent from the file (`/crm/*`, `/partners/*`, `/pstn/*`, `/site-language/*`), and **30 were in the file and absent live** — including `GET /messages/{messageId}`, `GET /push/devices`, `POST /docs/scrape` and `GET /whatsapp-voice/language-config`, all of which `scripts/register_task12_routes.py` claims to register. Verified individually against `GetRoutes` before believing it. There is now exactly **one** HTTP API where the earlier snapshot recorded two. Sorted now, so future diffs mean something |
| 194 | 2026-09-24 | Gaps found while measuring, recorded not fixed | — | `A0_READ` | n/a | standing grant | **MEDIUM** — `wecare-push-notifications` and `wecare-docs-scraper` have **routeCount 0**: every route `register_task12_routes.py` claims for them is absent from the live API, so both are reachable only by direct invoke. **MEDIUM** — 25 routes still have an **unqualified** integration, bypassing the version/alias model. **LOW** — 12 log groups have no retention. **INFORMATIONAL** — the security baseline in `00-current-owner-overrides.md` (332 routes, 2 HTTP APIs) is stale, exactly as its own execution rule warns |

| 195 | 2026-09-24 | **Dependabot: 1 high + 1 moderate → 0 vulnerabilities** | local | `A1_LOCAL` | yes (revert the override) | standing grant | The push surfaced them, so they were read rather than assumed: `mysql2 <3.22.0` *Auth Plugin Downgrade to mysql_clear_password Leaks Plaintext Credentials* (**high**) and `csv-parse <7.0.2` *prototype replacement reachable via the columns path* (moderate). Both are transitive under `@aws-amplify/backend-cli`, a **devDependency**, reached only by `ampx generate schema-from-database` — which this project cannot use, since `amplify/data/resource.ts` has no SQL data source. Fixed with `overrides`, the pattern already in `package.json` for `lodash`, `fast-xml-parser` and `immutable@3` |
| 196 | 2026-09-24 | Pinning to the advisory's patched version was **not enough** | local | `A1_LOCAL` | yes | standing grant | `mysql2` pinned to the advisory's `3.22.0` first, and `npm audit` then reported a *different* moderate advisory — an **unbounded zlib inflate** decompression-bomb DoS affecting `<=3.23.0`, which the Dependabot list had not mentioned. Pinned to `3.24.4` instead. Worth recording as a method note: the advisory's `first_patched_version` answers one CVE, not "is this package now clean". `npm audit` now reports **0 vulnerabilities**; typecheck, 137 vitest and the build all re-run green after the tree changed |

| 197 | 2026-09-24 | **25 routes were invoking `$LATEST`; now 6, and the 6 are deliberate** | API Gateway + Lambda | `A3_PRODUCTION` | yes | standing grant, hardens | All 25 belonged to three functions. Two costs to an unqualified integration: **no rollback** (moving an alias back is one call; undoing an `update-function-code` means rebuilding a package that may no longer exist) and **no atomic cutover** (`$LATEST` changes the instant the upload finishes, mid-flight requests included). `wecare-partner-onboarding` → published v10, alias `live`; `wecare-marketing-ads` → v9. Behaviour-preserving by construction: the integration was already serving `$LATEST`, so the alias starts at exactly what production was running. `scripts/provision_live_alias.py`, with a **recorded baseline** — `GET /partners/me` was 401 before and is 401 after, `OPTIONS /marketing-ads` 200 before and after. A probe with no baseline is not evidence |
| 198 | 2026-09-24 | `wecare-seo-tools` **excluded**, and the exclusion is the safer answer | — | `A0_READ` | n/a | standing grant | `scripts/deploy_seo_tools.py` contains **no alias handling at all**. Giving it a `live` alias would mean every future deploy updates `$LATEST`, reports success, and never reaches production — the exact "it looked shipped" failure `ensure_plivo_dial_events_route.py` was written about. The alias comes after its deployer learns to move one. Named in the script so the exclusion cannot be mistaken for an oversight |
| 199 | 2026-09-24 | The alias permission that a function policy does not cover | Lambda policy | `A3_PRODUCTION` | yes | standing grant, additive | Measured before touching anything: `wecare-marketing-ads` had exactly one statement, scoped to the **unqualified** function. A function-level resource policy does not authorise an invoke of an alias, so repointing the integration first would have given API Gateway AccessDenied — surfaced as **500 with no Lambda log line**, because the function is never called. Permission added before the integration moved; that order is not negotiable |
| 200 | 2026-09-24 | **12 log groups never expired; now 0** | CloudWatch Logs | `A3_PRODUCTION` | no (a deleted log event is gone) | standing grant, tightens | 13 of 67 `wecare-*` groups had `retentionInDays` unset, which means never. Execution logs hold masked numbers, message ids and request ids, so keeping them forever was a data-retention decision made by **absence** rather than by anyone. Set to **30 days** — the value this codebase already codifies in `provision_cognito_custom_message.py` and `provision_customer_whatsapp_auth.py`, and already on 42 of 67 groups. The 12 at 90 days show no category logic (a mix of `automation-rules`, `docs-scraper`, `payu-webhook`, `url-shortener`) so they read as incidental and were **left alone**: shortening a longer retention is the only genuinely destructive move available, and the script refuses it. Nothing was lost — the largest affected group held 363 KB, all of it days old, so no existing event is old enough for a 30-day policy to reach. That is timing, not luck, and the script says so. The audit trail is not here: `lambda_utils.audit` writes to `AuditLogsTable` with its own TTL |
| 201 | 2026-09-24 | **Three dashboard pages called routes that did not exist — and hid it** | measurement | `A0_READ` | n/a | standing grant | `/dm/docs` calls `GET /docs/sources`, `POST /docs/sources`, `POST /docs/scrape`, `GET /docs/changelog`; `/dm/push` calls `GET /push/devices`, `POST /push/send`; `src/api/client.ts` calls `GET`+`PUT /whatsapp-voice/language-config`. **All eight absent from the live API.** Both pages swallow the 404: `/dm/docs` wraps its reads in `.catch(() => [])` and renders an empty source list and empty changelog as though that were the truth, `/dm/push` checks `res.ok` and shows nothing. Three screens that look functional and cannot work — the same shape as the agent panel's 18 phantom tools |
| 202 | 2026-09-24 | The handlers were already written for these routes | source | `A0_READ` | n/a | standing grant | Not a feature being added. `docs-scraper` carries an explicit `ROUTE_ACTIONS` map keyed on the exact strings `GET /docs/sources`, `POST /docs/sources`, `POST /docs/scrape`, `GET /docs/changelog`. `push-notifications` dispatches on the `devices` and `send` segments and requires **Admin** for both. `whatsapp-voice` handles `language-config` for GET and PUT and documents it in its own docstring. Only the wiring was missing. `scripts/register_task12_routes.py` claims to register most of them, so either it never ran against this API or they were lost with the second HTTP API that no longer exists |
| 203 | 2026-09-24 | **8 routes created; all three pages can reach their handlers** | API Gateway | `A3_PRODUCTION` | yes (delete route) | standing grant, additive | `scripts/provision_missing_ui_routes.py`. `whatsapp-voice` reused its existing `:live` integration; `push-notifications` got a `live` alias (v9) because it is in `deploy_all_lambdas.py`, so `snapstart_publish.py` — which finds targets by looking for the alias — starts publishing and moving it immediately. `docs-scraper` got an **unqualified** integration deliberately: it is `PackageType=Image` and ships via `.github/workflows/docs-scraper-deploy.yml`, which runs `update-function-code` and nothing else. Four routes that work on `$LATEST` beat four routes that do not exist, and that is why `routesTargetingLatest` went **25 → 2 → 6** rather than to zero. No OPTIONS routes: the API carries an API-level `CorsConfiguration`, so preflight never reaches a route lookup. All three probes **401**, and `/dm/docs`, `/dm/push`, `/dm/voice` serve 200 |
| 204 | 2026-09-24 | What was **not** verified about those three pages | — | `A0_READ` | n/a | standing grant | The routes exist and refuse anonymous callers — that is proven. That each page now *displays data* is **not** proven: all three handlers require `Admin`, and the pool's only user is in no group, so an authenticated round trip needs the owner action below. Recorded rather than claimed |
| 205 | 2026-09-24 | Inventory after this run | AWS | `A0_READ` | n/a | standing grant | 62 functions · **1** HTTP API · **353** routes · 77 tables · `live` alias on **56** (was 53). Anomalies: `routesToAbsentFunction` **0**, `noRoutesNoEventSourceNoTraffic` **0**, `zeroInvocations7d` **0**, `logsNeverExpire` **0**, `noLiveAliasButHasRoutes` **2** (both deliberate), `routesTargetingLatest` **6** (both deliberate sets). Amplify jobs **819, 820 SUCCEED** |

| 206 | 2026-09-24 | **WAF cannot protect an HTTP API — that reshapes the whole target** | research | `A0_READ` | n/a | standing grant | Verified against AWS documentation rather than recalled: WAF's protected resource types are a CloudFront distribution, an API Gateway **REST** API, an ALB, an AppSync GraphQL API, a **Cognito user pool**, an App Runner service, an **Amplify application** and a Verified Access instance. `zllr9lrg7j` is an HTTP API (apigatewayv2), so **no web ACL can ever be attached to it**. A service limit, not a decision. The owner override's "WAF must be implemented and live-verified" is therefore met on the two surfaces that support it, and the API keeps the protection it already has: handler-level `require_auth`, provider signature verification, and per-route invoke permissions |
| 207 | 2026-09-24 | **`wecare-amplify-waf` — CLOUDFRONT scope, blocking** | WAF | `A3_PRODUCTION` | yes (`disassociate_web_acl`) | standing grant, tightens | Rate limit 2000/5min per IP, plus `AmazonIpReputationList`, `KnownBadInputsRuleSet` and `CommonRuleSet` all in **BLOCK**. Two web ACLs were needed rather than one because the scopes are incompatible: Amplify requires a CloudFront-scope ACL created in us-east-1 and AWS states a regional ACL is not usable with Amplify. Blocking is defensible here precisely because the app is a **static Next.js export** — GETs for HTML, JS and images, no form posts, no bodies worth inspecting — so the managed groups have almost nothing legitimate to false-positive on |
| 208 | 2026-09-24 | **`wecare-cognito-waf` — REGIONAL, rate limit blocks, managed rules COUNT** | WAF | `A3_PRODUCTION` | yes | standing grant, tightens | Deliberately configured **differently**, and making it uniform would have been the error. The real threat to a sign-in endpoint is credential stuffing, which the rate-based rule (1000/5min per IP) answers directly. Putting an untuned Core rule set in BLOCK in front of managed login risks refusing a legitimate sign-in — and the only account in this pool belongs to the person who would then be locked out of fixing it. AWS's own Cognito guidance is to test and tune before enforcing, so `CommonRuleSet` and `AmazonIpReputationList` are in COUNT until the logs are read |
| 209 | 2026-09-24 | COUNT is only honest if the counts are readable | CloudWatch Logs | `A3_PRODUCTION` | yes | standing grant, additive | Logging is part of the same change rather than a follow-up, because a rule set counting into nothing is the audit sink that failed open wearing a new costume. `aws-waf-logs-wecare-amplify` and `aws-waf-logs-wecare-cognito`, both at the house 30-day retention |
| 210 | 2026-09-24 | **Live enforcement proven, not asserted** | HTTP + CloudWatch | `A0_READ` | n/a | standing grant | A Log4Shell probe (`${jndi:ldap://…}`) in a query parameter returns **403** while `/` returns **200**. Inert by construction: the string is matched and dropped and nothing in this stack interprets JNDI. CloudWatch confirms the attribution — `AWS/WAFV2` records **2 BlockedRequests against `AWSManagedRulesKnownBadInputsRuleSet`** and 11 AllowedRequests. A method note: CloudFront-scope ACLs publish metrics **without** a `Region` dimension, so the first query returned 0 and looked like a dead WAF; `list_metrics` showed the real dimension sets. 17 of 18 site probes return 200 (the 18th is a hashed Next.js chunk path that legitimately 404s — a WAF block is 403, not 404) |
| 211 | 2026-09-24 | Cognito enforcement: what was proven and what was not | CloudWatch | `A0_READ` | n/a | standing grant | **56 AllowedRequests, 0 Blocked** on the Cognito ACL — proof it is inspecting live traffic and refusing nothing legitimate. The rate-based rule is **not** exercised, and deliberately so: tripping it means firing thousands of requests at our own sign-in endpoint, which is a denial of service against ourselves. The COUNT rules block nothing yet. `--verify` prints both of those as "not verified" rather than letting a green line imply otherwise |
| 212 | 2026-09-24 | **A 403 on managed login that was NOT the WAF** | measurement | `A0_READ` | n/a | standing grant | `signin.wecare.digital/login` returned 403 right after the Cognito ACL was attached, which is exactly the shape of a WAF lockout. Ruled out on evidence, not reasoning: `get_sampled_requests` returned **0 samples** across every rule, the Cognito API still answered `InitiateAuth` with a 400, and the default `*.auth.us-east-1.amazoncognito.com` domain returned **302** for the identical query string. Route 53 was also correct — `signin` aliases exactly to the pool's own `d2cjxje0em4hm6.cloudfront.net`, and hitting that distribution directly gave the same 403. The 403 was pre-existing |
| 213 | 2026-09-24 | **Root cause: the client had no managed login branding** | Cognito | `A3_PRODUCTION` | yes (delete branding) | standing grant, additive | `DescribeManagedLoginBrandingByClient` returned `ResourceNotFoundException`. The custom domain serves **managed login**, which requires a branding style; the default domain falls back to the classic hosted UI, which is why one worked and the other did not. Created with `--use-cognito-provided-values` (branding `7631a405…`). `signin.wecare.digital/login` **403 → 200**, default domain 200, and `InitiateAuth` still 400 — so the SRP path the app actually uses was never touched. This was already logged on the architecture page as recommendation **R28**, but as "no fallback if DNS or the certificate expires"; the real fault was neither DNS nor the certificate |
| 214 | 2026-09-24 | `ADMIN_MFA_REQUIRED` **not** flipped, and that is the correct answer | — | n/a | n/a | blocked on owner | Flipping it would satisfy a checkbox and make the system refuse the first Admin ever created — the exact failure `_admin_mfa_required()`'s own comment predicts ("whoever hit that would turn the check off rather than enrol"). The precondition is an Admin who exists and has a factor. `_has_enrolled_mfa` accepts **any** factor, and the sole user already has `EMAIL_OTP` + `SMS_MFA`, so the only thing missing is **group membership** — and granting a human the Admin role is a privilege widening, which is not in the standing grant's table. Unblock: `aws cognito-idp admin-add-user-to-group --user-pool-id us-east-1_cSx0RHCIR --username wecare.digital --group-name Admin`, then set `ADMIN_MFA_REQUIRED=true` |
| 215 | 2026-09-24 | Recurring cost created, stated rather than buried | billing | `A3_PRODUCTION` | yes | standing grant | Two web ACLs at roughly $5/month each plus about $1/month per rule — on the order of **$17/month** before request charges, and Amplify's WAF integration may add a per-app fee at GA. Recorded because a recurring charge introduced without being named is how a bill becomes a surprise |

| 216 | 2026-09-24 | **REGRESSION I INTRODUCED: the inbox got stuck on a channel filter** | local | `A1_LOCAL` | yes | standing grant, bug fix | Owner reported the inner-page inbox and WhatsApp messages not showing. Cause was mine, from `269d45ed`. All six sidebar Inbox entries are the **same route** `/dm/inbox` differing only by query string, so Next keeps the component mounted across them and React state survives the navigation — and my `?channel=` effect only ever *set* the filter: `if ( q && q in CHANNEL ) setChannelFilter( q )`. Going `?channel=email` → "All channels" left `channelFilter` on `email`, and email has **zero rows**, so the page that promises every channel rendered "No conversations" while the API returned 244 messages every 15 seconds. Fixed to `setChannelFilter( q && q in CHANNEL ? q : 'ALL' )`, guarded on `router.isReady` so a deep link is not reset during the pre-hydration pass when `router.query` is still empty |
| 217 | 2026-09-24 | `dm/logs` never had the bug, and the difference is the lesson | source | `A0_READ` | n/a | standing grant | The logs view derives its channel with `useMemo` over `[channel, router.query.channel]`, so clearing the query recomputes to `undefined` automatically. The inbox used `useState` plus a one-way effect because its selector has to stay **user-changeable** — a preset, not a lock. Derived state was unavailable, so the reset had to be explicit, and I omitted it. Only two files read `router.query.channel`; both are now correct |
| 218 | 2026-09-24 | **Diagnosis order: I cleared four suspects before touching code** | measurement | `A0_READ` | n/a | standing grant | The newest change was a **blocking WAF**, so it was checked first: `get_sampled_requests` over 3 hours returned **4 blocked requests, all my own Log4Shell probes**, and zero on the Cognito ACL. Then the backend — 6 hours of `wecare-messages-read` logs showed **305 successful reads, 0 timeouts, p95 852 ms**, with `channel='ALL'` returning **244** and `channel='WHATSAPP'` returning **183** *at the moment the owner was looking at an empty page*. Then `loadData`'s other half, since `Promise.all` fails as a unit: `wecare-contacts` had **0 error markers, p95 459 ms**. Then the channel casing, which is the obvious suspect and was **not** the fault — writes are lowercase (`message_store.VALID_CHANNELS`), the handler uppercases on read, `client.ts` uppercases again, and the inbox lowercases before comparing. Only after all four did the client-side filter become the answer |
| 219 | 2026-09-24 | A silent empty list is the reason this was hard to see | local | `A1_LOCAL` | yes | standing grant | `apiCall` returns `null` on any non-ok response and `listMessages` turns that into `[]`, so `loadData`'s `catch` never runs and `toast.error('Failed to load inbox')` never appears. That means an auth failure, a timeout and a real empty table are **the same screen**. It was not the cause today, but it is why the cause was invisible, so the empty state now names the channel, reports how many messages are sitting on the others, and offers a one-click "Show all channels". `No conversations` alone was indistinguishable from a failed load |
| 220 | 2026-09-24 | Tests that are genuinely RED before they are green | local | `A1_LOCAL` | yes | standing grant | `src/test/InboxChannelFilter.test.tsx`, **15 tests**. Verified by reverting the fix and re-running: **3 fail** on the old code — "does NOT stay stuck on a channel after the query is removed", "an empty channel does not permanently hide everything else", and the source assertion banning the one-way form. The remaining 12 pin what must not break while fixing it: a hub prop outranks a stale URL, an unknown channel falls back to ALL rather than hiding everything, `?channel=` is not reset before `router.isReady`, and the UPPERCASE-vs-lowercase comparison filters in both directions. Totals **152 vitest** (was 137) / **2964 pytest** |
| 221 | 2026-09-24 | `VOICE` was missing from two places that claimed to enumerate channels | Lambda + types | `A3_PRODUCTION` | yes | standing grant, additive | `Message.channel` in `client.ts` was typed `'WHATSAPP' \| 'SMS' \| 'EMAIL' \| 'RCS'` and the handler whitelist matched it, while `message_store.VALID_CHANNELS` has included `voice` since calls began writing breadcrumb rows — and `/dm/inbox?channel=voice` is now the Calls destination rendering those rows with a badge and an audio player. Harmless today because the whitelist builds a filter that `_read_from_messages_table` never consults, but a list that silently disagrees with the canonical set bites whoever first relies on it. `wecare-messages-read` v11 → **v12**, rollback `--function-version 11`. Verified live: all four `/messages` variants return 401, zero errors, and reads returning **253** and climbing as new messages arrive |

| 222 | 2026-09-24 | **`#111827` retired from inline styles: 129 → 0** | local | `A1_LOCAL` | yes | standing grant | The last deferred colour job, explicitly parked by `check_design_drift.py`'s own docstring: "#111827 still appears in inline React style objects in dozens of pages. Those are a much larger, separate job." Measured: **129 live uses across 23 files** (comments stripped), concentrated in `dashboard/index.tsx` (49), `whatsapp/calling.tsx` (26), `whatsapp/ai-agent.tsx` (13), `design-reference.tsx` (8), `dm/index.tsx` (6). Replaced **context-aware, not blind**: 125 text → `#1a1a1a` (`tokens.css --text`), **2 code/log panel backgrounds → `#000`** (`SystemTab.tsx:528`, `whatsapp/migration.tsx:213` — both monospace log panels with light type, the contract's "both code panels are #000"), 1 greyscale rung → `rgba(0,0,0,.95)`. The other eight retired colours needed no work — verified **0 live uses**, comments only |
| 223 | 2026-09-24 | **The single source of truth was contradicting the thing it claimed to mirror** | local | `A1_LOCAL` | yes | standing grant | `design-tokens.ts` opens by declaring itself authoritative and says it MIRRORS `tokens.css`. It did not: `text` was `#111827` against `--text: #1a1a1a`, and `grey900` was `#111827` against `--color-grey-900: rgba(0,0,0,.95)`. That file has **14 importers**, so `colors.text` resolved to a retired blue-tinted grey in 14 files while every rule reading the CSS variable resolved correctly — **two primary text colours on one screen, decided by which system a component happened to read**. An earlier note recorded this file as having *zero* importers, which is what let the contradiction sit |
| 224 | 2026-09-24 | Two new drift rules, both proven RED first | local | `A1_LOCAL` | yes | standing grant | `#111827` promoted into `RETIRED_COLOURS`, which scans all of `src/**.{tsx,ts,css}` rather than stylesheets only, and the narrower `scan_retired_in_styles` **deleted** — two rules for one colour is two places to get an exemption wrong. Added `scan_token_mirror`, which compares five `design-tokens.ts` keys against their CSS variables. It has to composite before comparing, because the contract writes body text as `rgba(0,0,0,.898)` in one file and `#1a1a1a` in another and **those are the same colour over white, exactly** — a naive string compare would report agreement as drift and push someone to "fix" it into real disagreement. Verified by injection: a reverted token value fires `token_mirror_mismatch`, and an inline style in `dashboard/index.tsx:2164` fires `retired_colour` — something the old stylesheet-only rule could never have caught |
| 225 | 2026-09-24 | `tests/test_design_drift_tokens.py` — 33 tests on the arithmetic | local | `A1_LOCAL` | yes | standing grant | The gate is only as good as `normalise_colour`, so that is unit-tested in both directions: `rgba(0,0,0,.898)` == `#1a1a1a`, `rgba(0,0,0,.95)` == `#0d0d0d`, `rgba(0,0,0,.54)` == `#757575`, shorthand hex expands, fully-opaque `rgb()` needs no compositing, retired values pass through **unchanged** so they cannot be normalised into looking legitimate, and an unresolvable `var(--text)` is returned rather than guessed. Plus the regression itself, reconstructed in a tmp dir, and its inverse — two spellings of one colour must **not** trip it |
| 226 | 2026-09-24 | **All three steering "Known open issues" were already resolved** | measurement | `A0_READ` | n/a | standing grant | Checked before fixing, and none was outstanding work. **`.why-section`** does not exist anywhere in `src/` any more, so the entry described a removed class; verified the general case instead by brace-parsing the styled-jsx of all three public pages with `@media` flattened — **every** centred container declares a `max-width`. **`overflow-x:clip`** already ships on `grahak-os` as a `hidden` base plus an `@supports (overflow-x:clip)` upgrade in *separate rules*, because two declarations on one rule lets a minifier drop the first; `/` and `/vayulok` never needed it, their only `overflow:hidden` being a rotating-word mask and an sr-only utility. **The empty-body issue** is fixed — the `if ( !mounted ) return null;` is gone. All three entries corrected in the steering file with the measurements rather than left reading "Still open" |
| 227 | 2026-09-24 | The static export does reach a non-JS crawler | HTTP/build | `A0_READ` | n/a | standing grant | Measured on the built export by stripping `<script>` from `<body>` and counting the remainder: `index.html` **2,501 chars** + JSON-LD, `grahak-os/index.html` **2,448** + JSON-LD with five `h2`s, `vayulok/index.html` **489** + JSON-LD. **Recorded as still open, reclassified:** vayulok's 489 is its genuine content — one `h1` and no `h2` at all — so it is a content gap, not a rendering one, and writing marketing copy is outside this work |
| 228 | 2026-09-24 | **UI label violations 20 → 0, and the gate now blocks on them** | local | `A1_LOCAL` | yes | standing grant, tightens | Three different problems wearing one label. **8 reworded**, because they were genuine copy defects: the agent greeting said "powered by Amazon Bedrock" (which model answers is not a decision an operator makes), a button tooltip read "Get AI Suggestion (Bedrock)" instead of "Suggest a rewrite", and the contacts error told the reader to *redeploy a Lambda and check CloudWatch* — neither action available to them, so it now says what happened, that nothing was changed, and offers the one thing in reach. **11 authorised** with a stated reason, because the infrastructure names ARE the subject: the SEO architecture content, the cost-controls service list (renaming those would leave an operator unable to match a line to a line on the bill), and the WABA migration screen — where naming Secrets Manager is the **by-reference form `secret-handling.md` asks for**, telling an operator where to look without printing the value |
| 229 | 2026-09-24 | I made the exemption too loose and caught it before it shipped | local | `A0_READ` | n/a | standing grant | `Cognito: {` inside `Amplify.configure()` is an SDK-mandated object key, not copy anybody reads — a real false positive, and authorising all of `_app.tsx` would have stopped checking the MFA chooser labels, which are real UI text. My first exemption was `[A-Za-z0-9 ]+:\s*\{`, which **also swallows `<p>DynamoDB: {count} rows</p>`**, where the brace opens a JSX interpolation. That would have turned a precision fix into a hole. Now anchored to the whole line — a config key is alone on its line, a label is not — with 6 unit cases asserting both the exemption and its limits |
| 230 | 2026-09-24 | The medium allowance withdrawn, on its own stated terms | CI | `A1_LOCAL` | yes | standing grant, tightens | `check_ui_labels.py --gate` blocked on HIGH only while 20 medium violations stood — the staging `provider-policy.yml` used, advisory until the count reaches zero then flip, because a gate that fails on day one for something nobody will fix this week gets switched off. The count is zero, so the allowance has outlived its purpose: leaving it means the 21st violation landing silently under a line reading **"GATE PASSED"**, which is the exact shape of defect this phase has spent its time removing. `--strict` kept as a no-op alias. Verified the gate fails on a reintroduced medium violation, and the `MODE:` comment in `ui-labels.yml` updated so CI documents what it actually does |
| 231 | 2026-09-24 | Gates after this work | — | `A0_READ` | n/a | standing grant | **2997 pytest** (was 2964, +33) · **152 vitest** · typecheck clean · build **125 sitemap URLs** (109 blog posts) · `check_design_drift.py --gate` OK · `check_ui_labels.py --gate` OK **at every severity** · `check-provider-policy.sh` OK · `verify_secret_hook.py` 26/26 · `block_catastrophic.py --self-test` 97/97 |
| 232 | 2026-09-24 | **`apiCall` collapsed every failure to `null`, everywhere** | local | `A1_LOCAL` | yes | standing grant | Entry 219 named this as the reason the inbox bug was invisible and fixed only the inbox's empty state. The mechanism itself is fixed now. The implementation became `apiCallResult<T>` returning a discriminated union with a typed `ApiFailure` — **9 kinds**, status, and a `retryable` flag — while `apiCall` stayed as a two-line wrapper so all **~309** internal call sites and every exported wrapper signature are untouched. `collectApiFailures()` recovers the reason for the list wrappers that return `[]` for both empty and broken. The inbox consumes it: a 401 says the session expired and offers a reload, everything else names the fault and offers Retry **only when retrying could work**. A failed 15s poll deliberately does not blank a working list — losing a visible thread list to a transient blip is worse than slightly stale data. **Honest limit: only the inbox consumes it.** Every other list page still shows `[]` for both cases, and `ErrorState.tsx` is still imported by one page, which is `design-reference.tsx` |
| 233 | 2026-09-24 | There is no `/login` route, so "sign in again" could not link anywhere | source | `A0_READ` | n/a | standing grant | Checked before writing the 401 remedy. Auth is the Amplify `Authenticator` wrapping every protected page in `_app.tsx`; there is no sign-in route to send anyone to. A reload remounts it and re-runs the session check, so that is the action offered. A `<Link href="/login">` would have been a dead link presented as a fix |
| 234 | 2026-09-24 | **`amplify/data/resource.ts` reads like infrastructure and is a document** | local | `A1_LOCAL` | yes | standing grant | The HIGH gap carried from 5.3. Measured: **0 AppSync APIs**, 64 models declared, **77 live tables**. Not one model had ever been materialised — every live table came from CDK or a `provision_*.py` script under a different naming scheme — while the header claimed "41 Tables with PAY_PER_REQUEST billing mode" and the comments cite physical names like `stack-wecare-digital-ContactsTable`, which makes it read as a description of the live tables. The header now states what the file is and the trap it sets: Amplify would **not adopt** the live tables if this were deployed, it would create 58 empty ones beside them. It cannot simply be deleted either — `backend.ts` uses `backend.data.resources.stacks['data']` as the CDK anchor for the URL shortener, the queues, the alarms and the SEO resources, so the declaration is load-bearing as a stack anchor even though its models are inert |
| 235 | 2026-09-24 | Six phantom models removed, and five silent no-op TTL policies with them | local | `A1_LOCAL` | yes | standing grant | `SmsAws`, `AirtelSMS`, `AirtelC2C`, `RcsMessages`, `AdminActionLog`, `ProviderDriftSnapshot` — each declared a table that does not exist. `AdminActionLog` was the purest case: the declaration was its **only** mention anywhere in the repo, and the live admin trail is `AuditLog` on `AuditLogsTable`. `ProviderDriftSnapshot` was the deferred item 9.2, waiting for a single retired-provider sweep. Their `TTL_CONFIG` entries in `backend.ts` went too: the `if ( table )` guard meant four of eight retention policies in that block were configuring **nothing** while reading as though they were. 16 entries remain, all with tables. `RateLimitTracker` was **cleared rather than removed** — it is the one that looks like a phantom and is not, living on `RateLimitTable` with the "Tracker" dropped |
| 236 | 2026-09-24 | A model-to-table gate, and the duplication I removed from it | local | `A1_LOCAL` | yes | standing grant | `scripts/check_data_model_drift.py`, gated and snapshot-backed so CI runs it without AWS credentials. It carries an **explicit** model → table map rather than a rule, because the sibling script refuses this comparison for a stated reason: an earlier attempt assumed `Model` → `stack-wecare-digital-Model` and produced **61 false positives**. My own first draft also tried to check code references and produced **6 false positives** — a regex stopping at the hyphen turns `stack-wecare-digital-bulk-queue` into a missing table called "bulk". `audit_data_model_drift.py` already owns that question and classifies queues and functions properly, so the duplication came out. Two gates, two questions, neither guessing |
| 237 | 2026-09-24 | **`NOTIF-STORE-004` cleared: 5 referenced-but-absent tables → 0** | Lambda source | `A1_LOCAL` | yes | standing grant | Registry item recorded as `DISCOVERED` with its gate pending "once cleared". `audit_data_model_drift.py --gate` now exits 0. The sharpest find was **`WIX_STORE_FUNCTION`**, defaulting to `stack-wecare-digital-wix-store` — a Lambda name written with the DynamoDB **table** prefix, matching nothing in the account. Verified against the live configuration that neither `wecare-service-api` nor `wecare-whatsapp-business-api` sets that variable, so the broken default is what actually ran and **every** Wix invoke from either raised `ResourceNotFoundException`. Also removed three dead `SmsAwsTable` constants and the `system-cleanup` registry entry that raised `ResourceNotFound` on every run — the same fault its own neighbouring comment describes for the Airtel entries |
| 238 | 2026-09-24 | **A deleted store now answers 410, not 500** | Lambda source | `A1_LOCAL` | yes | standing grant | Both Airtel tables were deleted on 2026-09-20, and `voice-in/c2c/handler.py` carried a comment asserting the opposite — "it holds real historical records that must stay readable". Five call sites read that table and the function took **39 invocations in 30 days**, so these were live 500s rather than dead code; `lambda_utils/comms/legacy_history.py` exists solely to read the other one and is reachable from a live `sms-aws` route. New `lambda_utils/retired_store` distinguishes the cases: **500 says "we failed, retry"; 410 says "this existed and was intentionally removed"**, and returning the first for the second is how a closed decision gets reopened as an incident. It matches `ResourceNotFoundException` on the **error code**, because botocore synthesises the class at runtime and catching `ClientError` would also swallow throttling and access-denied, which must keep surfacing as faults |
| 239 | 2026-09-24 | The asymmetry in `counts_by_provider`, which is the point of that file | Lambda source | `A1_LOCAL` | yes | standing grant | Three `legacy_history` paths report `storeAbsent`; `get_message` deliberately collapses to `None`, because for a single-id lookup "gone" and "no such row" are the same answer and there is nothing for the caller to do differently. But `counts_by_provider` must **not** report `total: 0, exact: True` — that output is the checksum a deletion decision is justified against, so "zero rows, exactly" would read as evidence that there was never anything to lose, which is the opposite of what happened. `purge()` reports `deleted: 0, alreadyAbsent: True` rather than claiming a deletion it did not perform, and still refuses without its typed confirmation — the absence check must not short-circuit the destructive-intent guard |
| 240 | 2026-09-24 | **A repeat order could mark the wrong invoice paid** | Lambda source | `A1_LOCAL` | yes | standing grant, bug fix | `_mark_invoice_paid_by_phone_and_amount` matched on `abs(float(total) - rupees) < 0.02` — an epsilon of 0.02 under a comment claiming ±₹0.01, so a ₹100.00 invoice matched a ₹100.01 payment — and then resolved multiple matches with `if len(candidates) >= 1`, sorting by `createdAt` and marking the **oldest** paid. Two pending invoices for one phone with the same total is an ordinary repeat order, and there that marked the wrong invoice paid **and left the real one outstanding**: one payment, two wrong rows, nothing downstream able to detect it. Now an exact integer-paise comparison, and **>1 candidate refuses to guess** — an unreconciled payment is recoverable because someone can still look at it; a wrongly-reconciled one is not. Three log lines in that function were also printing the **full phone number** against a codebase that masks to the last four everywhere else |
| 241 | 2026-09-24 | A uuid was being issued as a GST invoice number | Lambda source | `A1_LOCAL` | yes | standing grant, bug fix | `_get_next_invoice_number` fell back to `WD-PAY-TEMP-<uuid>` when the sequence counter was unreachable. Rule 46(b) requires a consecutive series for the financial year, so that was not a degraded invoice number but an **invalid** one — returned as an ordinary success, at the exact moment the system had lost track of what the next number should be. It raises now, and `create_invoice` answers **503 with `retryable: true`** and writes nothing: 503 rather than 500 because the payload is fine and the counter is not, and nothing written because a number outside the series cannot be reassigned once it has gone to a customer |
| 242 | 2026-09-24 | **Five ways to accept a payment unverified, recorded as one** | Lambda source | `A1_LOCAL` | yes | standing grant | Meta's own guidance is "must not rely solely on the status of the transaction provided in the webhook", and the code held that as a **bool initialised `True`**. So a capture never checked — because the outbound row had no `paymentConfigName`, or because the lookup call failed — was indistinguishable in the logs and on the invoice from one Meta had confirmed; "which captures did we accept without verifying?" had no answer. Now a four-value outcome. Mapping it out also found a **fifth** path nobody had listed: a 200 with an **empty `payments` array** fell straight through as verified, which is the easiest to miss because it sits on the success branch of the HTTP call. Accepting-unverified now logs at **error** level, which is what an alarm can count |
| 243 | 2026-09-24 | The payment decision I deliberately did **not** take | flag | `A1_LOCAL` | yes | standing grant | Making verification fail-closed changes live payment **acceptance**, which is not a change to make silently and not one to make without payment traffic to validate against. It exists as `PAYMENT_LOOKUP_REQUIRED`, **default off**, enabled only by an explicit truthy string, so the decision is a one-line env change for whoever owns it. Only a lookup that *actively contradicts* the webhook rejects today — unchanged behaviour. The exposure is at least visible now, which it was not |
| 244 | 2026-09-24 | "Five payment vocabularies" is two deliberate ones plus three that drifted | Lambda source | `A1_LOCAL` | yes | standing grant | Correcting the finding mattered as much as acting on it. `InvoicesTable` legitimately carries **both** `status` (the document's lifecycle: created → sent → paid) and `paymentStatus` (what the money did: captured) — two different facts about one row, and collapsing them onto one vocabulary would lose information. The storage migration stays deferred to 4f as recorded, because it touches the admin surface and the frontend. What is done is `payment_status.for_storage()`, which **raises** on an unmappable value: reads stay lenient because they run on provider input and must degrade rather than break webhook processing, writes refuse because we choose the value there — and letting an unrecognised one through is precisely how five accumulated |
| 245 | 2026-09-24 | **The fourth unguarded Flow writer had zero callers** | Lambda source | `A4_DESTRUCTIVE` | yes | standing grant, proven-dead surface | `whatsapp-business-api._save_flow_submission` was the last `FlowSubmissionTable` writer with no duplicate guard — a bare `put_item` with a random `submissionId`, so every call made a new row. `flow_completion` exists because one duplicated paid `submit_request` completion produced 2 submissions, 2 invoices and **2 payment links sent to the customer**, which is money and which nothing reported. Reachability checked first, as the item required: **zero callers**, verified with an AST pass over `amplify/` rather than a grep — the only other mentions are `flow_completion`'s own audit table and the *different* guarded function `flows/common.save_flow_submission` that the nine live flows use. Deleted rather than guarded: wiring it up would have been a two-line change reintroducing the exact bug, and a dead unsafe writer beside a live safe one is an invitation. Prior text at `956e3e6a`; `flow_completion`'s "four writers" docstring corrected |
| 246 | 2026-09-24 | A Python traceback was being rendered on customers' handsets | Lambda source | `A1_LOCAL` | yes | standing grant, tightens | `_handle_flow_data` put `f'Flow routing failed: {str(e)}'` into `data.error`, which the Flow renders on the phone — bad UX and an information disclosure to anyone who can open the flow. Now a generic apology plus a stable `error_code`, with the detail kept in the log line under the same `requestId`. A failed route also used to fall through into `flow_response_full` at **info** level, so a dropped submission read as an ordinary exchange to anyone reading logs — which is the property that let this survive an audit. The previously-silent `if not response_payload` branch logs too |
| 247 | 2026-09-24 | **STILL OPEN: `_handle_flow_data` answers 200 on failure** | — | `A0_READ` | n/a | needs one live QA send | Recorded rather than guessed at. If Meta retries only on a non-200, a failed `data_exchange` is never retried and the submission is silently lost. Changing the status is a live behaviour change on an **encrypted customer-facing** endpoint, and the retry semantics are not documented clearly enough to act on — the observable symptom of a non-200 is the handset showing "Endpoint did not return a successful response", which suggests the user sees an error rather than a retry happening. Settling it needs one deliberate test to the QA recipient `+918100640044`. Note the hard precondition is already met: **retrying is now safe** for the nine flows that claim through `flow_completion.claim_completion`. A test asserts the note stays in place |
| 248 | 2026-09-24 | A price could render as "abc" | Lambda source | `A1_LOCAL` | yes | standing grant | `_money_amount` ended in a bare `str(value)`, so `_money_amount('abc')` returned `'abc'` straight into the `price` field the admin UI renders. Previously **pinned rather than fixed**, on the correct grounds that it was pre-existing behaviour identical across all 64 golden cases and a build should not fail on an opinion. Fixed now for a stronger reason than taste: `''` is what every caller tests for when falling back to the next price source, so a truthy junk value **stopped the fallback chain on the junk** and never consulted the price range that may have held a real number. Validating repairs the fallback, it does not merely tidy the output. The test that pinned the old behaviour is rewritten to say why it changed |
| 249 | 2026-09-24 | **A failure count that was never zero** | local | `A1_LOCAL` | yes | standing grant | Every `deploy_all_lambdas.py` run ended `failed=1`, always for `wecare-customer-whatsapp-auth` — in the deploy map, source present, but never created, because first creation is owned by `provision_customer_whatsapp_auth.py` which has not been run. That is a defect in the **report**, not the deploy: a genuinely broken deploy would have arrived in a summary that already said `failed=1`, beside an entry everyone had learned to skip. `Spec` takes `provisioned_by` now and such functions count as `awaiting_provisioning` with the exact command to run. The default stays empty on purpose — a function with no provisioner that goes missing **is** a failure, and defaulting the other way would hide that. Verified: `failed=0 awaiting_provisioning=1` across all 60 |
| 250 | 2026-09-24 | A permanent warning on every invoice, for a dependency that was never installed | Lambda source | `A1_LOCAL` | yes | standing grant | `import qrcode` was in no requirements file and in no attached layer — the only layer on `wecare-invoice-engine` is `pillow-python312` — so it failed on **every** invoice render and logged at WARNING each time, for a condition that was permanent and not actionable. Adding the dependency would have been the wrong repair: the encoded value is the constant `https://wecare.digital/selfservice`, so every invoice would have produced a byte-identical image and the right artifact for a constant is a file. The pre-rendered `stream/media/m/qr-selfservice.png` is present (459 bytes, image/png, verified) and the text-URL fallback still carries the same information if it ever is not. Deploy-all import warnings: **1 → 0** |
| 251 | 2026-09-24 | `AIInteractionsTable` **does** have a writer — the gap note was wrong | measurement | `A0_READ` | n/a | standing grant | Carried as "no writer but the architecture page claims one". It has one: `_store_ai_interaction` in `inbound-whatsapp-handler`, a real `put_item`, called from two sites. The table holds **0 items**, which is what the note was probably reacting to, but an empty table and an unwritten one are different facts — the architecture page is correct and needed no change. Recorded so the claim is not re-derived from the item count next time |
| 252 | 2026-09-24 | **`VdmOptions.EngagementMetrics`: upgraded from unverified to verified** | measurement | `A0_READ` | n/a | needs owner decision | Carried as "still ENABLED, may inject a pixel into OTP mail, **unverified**". The chain is now measured end to end and it is real. The Cognito pool `us-east-1_cSx0RHCIR` has `EmailConfiguration.ConfigurationSet = wecare-digital` with `EmailSendingAccount: DEVELOPER`; that configuration set has `VdmOptions.DashboardOptions.EngagementMetrics = ENABLED`; and the `wecare.digital` identity names the same set as its **default**, so domain mail inherits it too. `TrackingOptions` is `null`, which means no *custom* redirect domain — not no tracking. So MFA codes, verification codes and password-reset links are sent through a tracking-enabled configuration set, and click tracking rewrites links through AWS's default `awstrack.me` domain: a reset URL proxied through a third-party-looking host, on a domain running `p=reject` where deliverability is unforgiving. **Not changed unilaterally** — the correct remedy is a second, untracked configuration set for Cognito only, so auth mail is clean while transactional mail keeps its telemetry; repointing the pool's email configuration is an auth-email behaviour change and `email-auth-dns.md` requires verification either side of it |
| 253 | 2026-09-24 | `rcsmenu` template id/version stays `UNVERIFIED`, and why that is not fixable here | — | `A0_READ` | n/a | blocked on provider access | Confirming the template exists and which version is live requires a call to the Sinch RCS API, which requires reading `wecare/sinch/rcs` — prohibited, and `SINCH_RCS_ENABLED` is off so nothing exercises the path. `NOTIF_RCS_TEMPLATE_NAME` defaults to `rcsmenu` in `notifications/policy.py` and `sinch_rcs.send_rcs_template` falls back to `card_message` on failure, so the unverified id degrades rather than breaking. Unblock action, stated exactly: the owner supplies Sinch RCS credentials or runs the template list themselves; it is not something to work around |
| 254 | 2026-09-24 | Gates after this work | — | `A0_READ` | n/a | standing grant | **3209 pytest** (was 3160, +49) · **208 vitest** (was 181, +27) · typecheck clean · build **125 sitemap URLs** · `check_design_drift.py --gate` OK · `check_ui_labels.py --gate` OK at every severity · `check-provider-policy.sh` OK · `verify_secret_hook.py` OK · `block_catastrophic.py --self-test` OK · `generate_integration_inventory.py --check` OK · `audit_data_model_drift.py --gate` OK (**new**) · `check_data_model_drift.py --gate` OK (**new**) · `deploy_all_lambdas.py --dry-run` 60 packaged, **failed=0**, **0 import warnings** |
| 255 | 2026-09-24 | One gate caught me, and I took the block rather than the exemption | CI | `A0_READ` | n/a | standing grant | Writing the retired table's environment-variable name in an explanatory **comment** tripped `check-provider-policy.sh`'s `airtel-runtime` rule, whose pattern matches the literal anywhere in runtime code. The rule cannot tell a comment from a client and should not try — a prohibition that can be talked around with a comment is not a prohibition. Reworded the comment to describe the variable without spelling it, and said so in the comment itself |
| 256 | 2026-09-24 | **Twelve functions deployed, alias moved, drift 0** | Lambda | `A3_PRODUCTION` | yes | standing grant | Rollback versions captured **first** into `docs/execution/snapshots/lambda-aliases-before-gap-sweep.txt`, then `update_function_code` + publish + move `live` for all twelve: razorpay-webhook v30→31, invoice-engine v22→23, inbound-whatsapp v47→48, whatsapp-business-api v37→38, voice-in-c2c v13→14, sms-aws v16→17, messages-read v13→14, system-cleanup v14→15, service-api v8→9, wix-store v18→19, product-image-gen v14→15, catalog-management v9→10. `updated=12 failed=0`, and alias-sha vs `$LATEST`-sha compared across all twelve afterwards: **drift 0**. The nine wix/payment functions needed republishing even where only a shared `lambda_utils` file changed, because the package embeds it |
| 257 | 2026-09-24 | The function is `wecare-inbound-whatsapp`, not `...-handler` | measurement | `A0_READ` | n/a | standing grant | Worth recording because of how it failed. `messaging/inbound-whatsapp-handler` is the **source directory**; the deployed function drops the suffix. Probing the wrong name returned `live=NO_ALIAS`, which reads exactly like "this function has no live alias" — a real condition for nine functions in this account — rather than "no such function". Caught only because the sha came back `ERR` in the same row. A snapshot that had silently recorded NO_ALIAS would have left that function with no rollback line |
| 258 | 2026-09-24 | Post-deploy health, and what it does **not** cover | measurement | `A0_READ` | n/a | standing grant | 20-minute window on real traffic: `wecare-razorpay-webhook` **24 invocations, 0 errors**, `wecare-whatsapp-business-api` **19, 0**, `wecare-inbound-whatsapp` **2, 0** — so the three functions carrying live traffic are healthy on the new code, including the one whose payment-verification path was rewritten. The other nine took **no invocations** in the window: deployed and drift-free, but **not traffic-verified**. Unauthenticated probes on `/prod` returned **401** for `/voice-in/c2c`, `/messages`, `/invoices` and `/contacts`, so handler-level auth survived |
| 259 | 2026-09-24 | Pushed to `stack`, rebased over PR #45, Amplify green | git + Amplify | `A2_REMOTE_CODE` | yes | standing grant | Two commits: `3c8e4f06` and `6f5f8277`. `origin/stack` had moved to `0c3e513c` (#45, per-page share previews); **zero file overlap** with either of mine, verified with `comm` over the two name lists before rebasing, and gates re-run after the rebase rather than assumed. Non-force, branch `stack`, every path staged individually. Amplify job **838 SUCCEED** on commit `6f5f8277` exactly |

## Not exercised

- No credential was read, written, rotated or revoked.
- No `GetSecretValue` from a shell or from `aws___run_script`.
- No WhatsApp registration, WABA, number or subscription was touched.
- No secret deletion scheduled or cancelled.
- No force push, no history rewrite, no broad staging.
- No live customer message or call was sent.
- `PSTN_BROWSER_ROUTING_ENABLED` and `PSTN_CONNECTED_NOTIFICATIONS_ENABLED`
  remain `false`.
| 225 | 2026-09-25 | **There is no CloudFront redirect layer — the hostname redirects live in Amplify** | AWS | `A0_READ` | n/a | standing grant | Asked what redirects exist in CloudFront before deciding where to move `stack.wecare.digital`. The account has **2** distributions and **neither redirects a hostname**: `ERCXSFDL0VM8X` serves `app`/`selfservice`/`selfcare` from S3 with `DefaultRootObject=error.html` and 403+404 → `/error.html` at **ResponseCode 200** (a catch-all error page, not a redirect), and `E1SZBXLQ4XNLJ7` serves the MTA-STS policy. `redirect-to-https` on both is a protocol upgrade, not a hostname rule. `signin.wecare.digital` points at `d2cjxje0em4hm6.cloudfront.net`, which is Cognito's managed distribution and **not in this account**. The real `www → apex` and `stack → apex` 301s are **Amplify custom rules**. So the premise "move it to CloudFront like the other redirects" had nothing to move to |
| 226 | 2026-09-25 | One orphan CloudFront Function, unattached and double-hopping | CloudFront | `A0_READ` | n/a | standing grant | `auth-wecare-redirect` exists at stage **DEVELOPMENT** and is associated with **no distribution**. Its body unconditionally 301s to `https://www.wecare.digital/selfcare` — a host that itself 301s to the apex, so even if wired it would cost two hops. Left in place: deleting it is not needed for this work and it affects nothing while unassociated |
| 227 | 2026-09-25 | **The subdomain served nothing of its own, which is why this was safe** | HTTP | `A0_READ` | n/a | standing grant | Measured rather than assumed, because the code claimed the opposite. `stack.wecare.digital/` → **301** to the apex; `/dashboard` → 301 preserving the path; `?code=…&state=…` **survives** the 301; `wecare.digital/dashboard/` → **200**. Meanwhile `_app.tsx` asserted "this app is served from the subdomain … the two are not interchangeable" and used the subdomain for the OAuth redirect URI, the breadcrumb `item` and `pageUrl`. Production had already collapsed that distinction, so pages served **on the apex** were emitting a breadcrumb pointing at a host that redirects away — one occurrence in the live HTML of every page |
| 228 | 2026-09-25 | The precondition that made teardown safe: the apex was already registered | Cognito | `A0_READ` | n/a | standing grant | `https://wecare.digital/` was **already** both a `CallbackURL` and a `LogoutURL` on client `1j8kbi48m4v2rped3n224rlevb`, verified against the live pool. That is what allowed the redirect URI to move before the hostname was removed without any window where sign-in could fail with `redirect_mismatch`. Read with `--query 'UserPoolClient.CallbackURLs'`; the unnarrowed form is refused by `block_catastrophic.py` because the response carries `ClientSecret`, and the guard was not worked around |
| 229 | 2026-09-25 | **69 references retired; the CORS fallback was the one where order mattered** | local | `A1_LOCAL` | yes | standing grant | `cors_headers()` reflects `ALLOWED_ORIGINS[0]` back for any **disallowed** origin, and that first entry was the subdomain — a redirecting host is useless in `Access-Control-Allow-Origin`, because the browser compares the header against the literal request origin and never follows it. Apex moved to first. Same removal in `waba-management` `CORS_CORE_ORIGINS`, `link-resources.ts` and `deploy_site_language.py`. **User-visible, so not cosmetic:** `inbound-whatsapp-handler` sent a CTA button to `https://stack.wecare.digital` in two places and the FAQ told people to visit it to install the PWA — both would have become dead links. `sw.js` matched the subdomain to focus an existing window, so every notification click would have opened a new tab. Native deep links were **already** broken: Apple fetches `apple-app-site-association` and Android `autoVerify` fetches `assetlinks.json` directly and **neither follows a redirect**, so an associated domain that 301s can never verify |
| 230 | 2026-09-25 | `NEXT_PUBLIC_APP_URL` → apex, and the whole map had to be resent | Amplify | `A3_PRODUCTION` | yes (snapshot committed) | standing grant | `UpdateBranch` **replaces** `environmentVariables` rather than merging, so all 17 were sent back with one substitution; restoring a single key later would have silently dropped the other sixteen, which is why `docs/execution/snapshots/amplify-branch-env-before-app-url-apex.json` records the full map. Re-read confirms 17 before / 17 after, that key the only difference, none added or lost. Read at **build** time, so it needed a rebuild — Amplify job **844 SUCCEED**. Proven in production by downloading all 16 chunks the live `/dashboard` references (1.43 MB): `redirectSignIn:["https://wecare.digital/"]`, `redirectSignOut` likewise, and **zero** occurrences of the old hostname |
| 231 | 2026-09-25 | Fleet redeploy, because the CORS list lives in the shared package | Lambda | `A3_PRODUCTION` | yes (alias repoint) | standing grant | `lambda_utils/response.py` is bundled into every function, so this was not a two-function change: **57 updated, 3 unchanged, 0 failed**, publisher moved **53** aliases, and `snapstart_publish.py --dry-run` then reported **56 current / 0 stale** — drift zero. `deploy_site_language.py` moved `wecare-site-language` `live` **v4 → v5** with `SITE_LANGUAGE_ALLOWED_ORIGINS` now apex+www only; rollback `--function-version 4`. `config/lambda-env-manifest.json` regenerated to match, which also absorbed **11 pre-existing unrelated drifts** — enumerated in the commit rather than left buried, since a regenerate is exactly where such things disappear |
| 232 | 2026-09-25 | **Cognito rewrite: 20 fields read individually so 18 could be preserved** | Cognito | `A3_PRODUCTION` | yes (URLs saved) | standing grant, tightens | `UpdateUserPoolClient` **replaces** the client configuration, so sending only the URL lists would have reset `ExplicitAuthFlows`, token validities, `AllowedOAuthScopes` and `PreventUserExistenceErrors` to defaults — a silent auth regression. Every field was read with its own narrowed `--query`, and `ReadAttributes`/`WriteAttributes`/`DefaultRedirectURI`/`AnalyticsConfiguration` were **asserted null** before being omitted. Applied via `--cli-input-json file://` so nothing reached a command line. Field-by-field re-read: **20 compared, 18 unchanged, exactly `CallbackURLs` and `LogoutURLs` changed**. API `zllr9lrg7j` CORS `AllowOrigins` **6 → 5**, `AllowHeaders`/`AllowMethods`/`MaxAge` untouched. Live preflight proof: apex, www and the Amplify branch default each get their own origin echoed back; the retired host now gets **no `access-control-allow-origin` header at all** |
| 233 | 2026-09-25 | `stack.d22dm4b0jn71jw.amplifyapp.com` is **not** stale — I nearly removed a live host | AWS | `A0_READ` | n/a | standing grant | I had recorded it as stale alongside the custom subdomain because both contain "stack". It is the Amplify **branch** default domain — the branch is still named `stack` — and it returns **200**. Checked before editing the CORS list, and **kept**. Two names sharing a substring are not the same resource, and the only thing that separated them here was a one-line probe |
| 234 | 2026-09-25 | **Teardown: Amplify removed the DNS record itself** | Amplify + Route 53 | `A4_DESTRUCTIVE` | yes (3 snapshots committed) | standing grant, owner-instructed | Subdomain `stack` dropped from the domain association (apex sent back as `""`, since the API reports the apex prefix as `null`), and its 301 custom rule removed — rules **23 → 22**, with the `www → apex` rule and the `/<*>` 404-200 SPA catch-all asserted present before writing. `domainStatus` `UPDATING → AVAILABLE`, certificate type `CUSTOM` so no re-issue was triggered. The manual Route 53 delete turned out to be a **no-op**: Amplify manages the record and removed `stack.wecare.digital CNAME` itself, confirmed by API — zone **43 → 42** records, delta exactly **-1**, no `stack` record remaining, all ten critical hostnames still present. Verified no wildcard CNAME existed first, so the name now fails to resolve rather than falling through. Snapshots: `amplify-domain-association-before-stack-removal.json`, `amplify-custom-rules-before-stack-removal.json`, `route53-stack-cname-before-removal.json` |
| 235 | 2026-09-25 | Email authentication re-verified, because this edited the mail zone | DNS | `A0_READ` | n/a | standing grant | `email-auth-dns.md` requires a check either side of any change to that zone, and both policies there are **fail-closed** — a mistake rejects mail outright rather than filtering it. `verify-email-auth.ps1` could not run (no `pwsh` on this machine), so the same records were read directly: MX `1 smtp.google.com`, SPF unchanged at 4/10 lookups, DMARC `p=reject; sp=reject`, `_mta-sts` id **2026082302 unchanged**, TLS-RPT present, and the policy still served at `mode: enforce`. Nothing disturbed — the removed record was a CNAME on an unrelated label — but "it should not have touched mail" is not evidence, so it was measured |
| 236 | 2026-09-25 | **The gate I shipped could not catch the mistake it was written for** | CI | `A0_READ` | n/a | standing grant | `verify_public_bundle_secrets.py` scans `out/`, which only contains a key if the build that produced it had the variable set — and `build-test.yml` sets **no** `NEXT_PUBLIC_GOOGLE_MAPS_KEY`, so it scanned an export that never held one and passed for free. The build that has the branch variables is **Amplify's**, whose `amplify.yml` ran `npm run build` and nothing else. So the guard was green, and the real path was unguarded. Found by asking where the variable actually exists rather than re-reading the script |
| 237 | 2026-09-25 | Which build spec ships, settled on evidence not documentation | Amplify | `A0_READ` | n/a | standing grant | Both a repo `amplify.yml` and an app-level `buildSpec` exist, and they differ — only the repo file carries `Referrer-Policy` and `Permissions-Policy`. `wecare.digital` serves **both** of those headers, so the repo file is what Amplify uses and is the one to edit. A precedence claim from memory would have been a coin flip; two headers made it measurable |
| 238 | 2026-09-25 | **`verify_deployed_headers.py` has been checking the wrong origin** | measurement | `A0_READ` | n/a | standing grant | It requests `https://app.wecare.digital/`, which is the S3/CloudFront **static assets** distribution (`ERCXSFDL0VM8X`), not the Amplify app. That distribution applies none of Amplify's `customHeaders`, so the script reports `Permissions-Policy (absent)`, `x-content-type-options (absent)`, `referrer-policy (absent)` and `RESULT: FAIL` — against a host that was never supposed to carry them. The same headers are all present and correct on `wecare.digital`. A security check pointed at the wrong host is worse than none, because its failure gets learned as noise. **Not fixed here** — flagged, because it is unrelated to the credential work and deserves its own change |
| 239 | 2026-09-25 | **The credential gate moved to where it can actually fire** | Amplify | `A3_PRODUCTION` | yes (revert `amplify.yml`) | standing grant, tightens | Added `python3 scripts/verify_public_bundle_secrets.py` to the `amplify.yml` build phase, after `npm run build`, so the unified Google key fails the Amplify build **before** anything deploys. `python3 --version` added to `preBuild` so a missing interpreter fails early and loudly instead of turning the gate into a silent skip — the failure mode recorded at the top of `snapstart_publish.py`. The CI step is kept and **relabelled a backstop**, since all it can catch is a credential committed into the tree. New `--amplify-env` mode fingerprints the live branch variables so the question can be answered without a build; it reports a public variable and a non-public one differently, because only the former is served to visitors |
| 240 | 2026-09-25 | **Proven on the real inlining path, not just in unit tests** | verification | `A0_READ` | n/a | standing grant | The whole gate rests on an assumption — that Next inlines a `NEXT_PUBLIC_*` value into a chunk in a form the scanner matches after minification. Tested it: a synthetic `AIza`-shaped value written to `.env.local` (never to a command line, which `block-inline-secrets` would refuse) appeared in **exactly one chunk**, was first reported as an allowed browser key with its fingerprint, and then, with that fingerprint declared server-side, failed with **exit 1** naming the chunk and printing no value. `.env.local` removed and the tree rebuilt clean afterwards: **0** files contain the probe. 5 more tests, boto3 stubbed, including one asserting the `amplify.yml` wiring itself so removing the step fails the suite. **3232 pytest** |
| 241 | 2026-09-25 | Closed as an owner action with the procedure written down | docs | `A1_LOCAL` | yes | standing grant | `CREDENTIAL-ROTATION-RUNBOOK.md` now carries the decision — the unified key never goes in Amplify — plus the exact steps: a **new** key, Application restrictions → HTTP referrers `wecare.digital/*` **and** `*.wecare.digital/*` (the apex alone misses www), API restrictions → Maps JavaScript API only, pasted in the Amplify console rather than on a command line, then a redeploy because the value is read at build time. Recorded so it is not re-litigated and so the next person reaching for the obvious value finds the reason it is wrong before they find the key |
