# Evidence index

Immutable evidence for requirement states. No credential values, no customer
identifiers, no secret contents.

| ID | Requirement IDs | Evidence | Timestamp | Environment |
|---|---|---|---|---|
| `EV-0001` | `SEC-BASE-001`, `SEC-HOOK-001` | Git: HEAD `4baf4236dbf78a784ec84f238e091b699f7f8cd4`, branch `stack`, `origin/stack` identical, `git rev-list --count 827cd614..HEAD` = **28**. Working tree: 2 deleted spec files + 2 untracked, none authored by this session. `.kiro/hooks/` holds `block-broad-git-staging`, `block-catastrophic`, `block-inline-secrets`, `heal-parallel-setup` | 2026-09-21 | local |
| `EV-0002` | `WA-INGRESS-001`, `WA-OUTBOUND-001`, `WAC-001`, `SMS-001`, `RCS-001`, `PAY-001`, `DEPLOY-001` | `lambda:ListFunctions` + `ListAliases` ×58, errorTotal **0**. 58 functions (57 Zip, 1 Image), all `python3.12`, `SnapStart.ApplyOn=None` on 58/58. 49 carry `live`. Measured live versions: `whatsapp-calling=12`, `inbound-whatsapp=37`, `outbound-whatsapp=17`, `plivo-answer=9`, `sms-aws=12`, `outbound-sms=9`, `rcs-send=7`, `rcs-dlr=6`, `razorpay-webhook=25`, `invoice-engine=17`, `whatsapp-business-api=31`, `meta-business-agent=20`. 9 with no alias: `ad-attribution`, `docs-scraper`, `marketing-ads`, `partner-onboarding`, `partner-token-refresh`, `push-notifications`, `seo-tools`, `sla-engine`, `url-shortener` | 2026-09-21 | AWS `775261844268` / `us-east-1` |
| `EV-0003` | `NOTIF-STORE-001`, `NOTIF-STORE-002`, `NOTIF-LEGACY-001` | `dynamodb:ListTables` = **66** tables. `CallNotificationsTable` **present**. `PstnNotificationDelivery` **absent**. `RcsMessagesTable` **absent** | 2026-09-21 | AWS |
| `EV-0004` | `SEC-CRED-001`, `PROV-PAYU-001`, `PROV-AIRTEL-001`, `PROV-SINCHSMS-001`, `RCS-001` | `secretsmanager:ListSecrets` (metadata only, **no `GetSecretValue`**) = 31 entries. 25 active including `wecare/sinch/rcs`, `wecare/razorpay/api`, `wecare/razorpay-webhook`, `wecare/plivo*`, `wecare/meta-system-user-token`. 6 scheduled: `airtel-iq`, `airtel/c2c`, `airtel/obd`, `airtel/sms`, `sinch/sms` (2026-09-20), `payu` (2026-08-26) | 2026-09-21 | AWS |
| `EV-0005` | `SEC-MFA-001`, `SEC-WAF-001`, `SEC-GD-001` | Cognito pool `us-east-1_cSx0RHCIR` / `WECARE.DIGITAL`; `GetUserPoolMfaConfig` → `MfaConfiguration=OFF`, SMS MFA config present, TOTP not reported enabled; clients `stack-wecare-digital-web`, `WECARE.DIGITAL`. `wafv2:ListWebACLs` REGIONAL = **0**, CLOUDFRONT = **0**. `guardduty:ListDetectors` = **0**. `amplify:ListApps` = 1 (`d22dm4b0jn71jw`) | 2026-09-21 | AWS |
| `EV-0006` | `PROV-PAYU-001` | No PayU Lambda, route, integration or table in the measured inventory. `wecare/payu` remains recoverable inside its window | 2026-09-21 | AWS |
| `EV-0007` | `SEC-ROUTE-001`, `SEC-ROUTE-003`, `SEC-ROUTE-004`, `PROV-AIRTEL-001`, `PROV-GATE-001`, `PSTN-TOKEN-001`, `PSTN-DIAL-001` | `docs/execution/route-auth-audit-20260921.txt`: 329 routes, 102 integrations, 171 handler dirs; gateway-authorized **0**; gateway NONE + handler authenticates **302**; handler source unresolvable **4**; **OPEN 2** = `GET /webhook/sinch-rcs`, `POST /webhook/sinch-rcs` → `wecare-rcs-dlr`. Unresolved: `DELETE/GET/POST /voice-in/cdr`, `POST /whatsapp/inbound`. The script scans only `zllr9lrg7j` | 2026-09-21 | AWS + repo |
| `EV-0008` | `SEC-ROUTE-002`, `WA-INGRESS-001` | `amplify/functions/messaging/inbound-whatsapp-handler/handler.py:539` `def handler(...)` consumes `event['Records'][*]['Sns']['Message']`; grep for `require_auth` returns **no hits**; grep for `X-Hub-Signature` returns **no hits** in this file (verification lives in `whatsapp-calling/handler.py:324` and `whatsapp-business-api/handler.py:5525`). Direct-invoke branch `event['action'] == 'create_invoice'` executes before any authorization | 2026-09-21 | repo |
| `EV-0009` | `PROV-SINCHSMS-001`, `PROV-AIRTEL-001`, `DEPLOY-002` | API `79g3bbufdh` (`wecare-api`), 3 routes, 0 authorizers, stage `prod` autoDeploy: `GET /webhook/sinch-dlr` → `wecare-sinch-dlr` (**function absent**), `POST /webhook/sinch-dlr` → same, `POST /ai/generate` → `wecare-ai-generate-response` (**unqualified `$LATEST`**). API `zllr9lrg7j`: `POST/GET/DELETE /voice-in/cdr` → `wecare-voice-in-cdr` (**function absent**) | 2026-09-21 | AWS |
| `EV-0010` | `NOTIF-OWN-001`, `NOTIF-LEGACY-001` | `wecare-elevenlabs-postcall-sms` **absent from the live fleet**. DLT key `ivr-default` referenced in 8 source files: `comms/__init__.py`, `comms/notify.py`, `comms/dlt.py`, `pstn/notifications.py`, `voice-in/obd/handler.py`, `voice-in/c2c/handler.py`, `whatsapp-calling/handler.py`, `plivo-answer/handler.py`. `CallNotificationsTable` referenced only by `whatsapp-calling/handler.py`. `rcs-dlr/handler.py:291` invokes `wecare-rcs-send` from `evaluate_rules` | 2026-09-21 | repo + AWS |
| `EV-0011` | `PSTN-FLAG-001` | `plivo-answer/handler.py:87` `PSTN_BROWSER_ROUTING_ENABLED` default `false`; `:228` raises when true without `PSTN_AGENT_ENDPOINT`. `pstn/notifications.py:81` `PSTN_CONNECTED_NOTIFICATIONS_ENABLED` default `false`. Deployed environment values **not yet measured** | 2026-09-21 | repo |

| `EV-0012` | `SEC-ROUTE-001`, `SEC-ROUTE-003`, `SEC-ROUTE-006` | `tests/test_public_webhook_auth.py`, 35 tests, all passing. Includes the signature vector published by Sinch (`6bpJoRmFoXVjfJIVglMoJzYXxnoxRujzR4k2GOXewOE=`). RED proof: the pre-fix `rcs-dlr` handler taken from git `HEAD` returned **200** and called `_process_delivery` on an unsigned forged report; post-fix returns **401** and does not | 2026-09-21 | local |
| `EV-0013` | `SEC-ROUTE-002` | Guard asserted at `inbound-whatsapp-handler/handler.py`: verification precedes the `create_invoice` branch, `create_invoice` is internal-only, both WABA app secrets are candidates, rejection is 401 | 2026-09-21 | local |
| `EV-0014` | `SEC-ROUTE-001`, `SEC-ROUTE-002`, `DEPLOY-001` | Deployed: `wecare-rcs-dlr` live **v7** sha `55FQyp+dAcVmHKqDoctzi1Cx7jzMyV6EeF3ZZqWlpAU=` `Active`; `wecare-inbound-whatsapp` live **v38** sha `5fjOzD6fOr6qk/X7SE/NYY6A+ZrwMBC9BAJk/VNLt9o=` `Active`. Both live shas equal `$LATEST`. Rollback: `rcs-dlr:live→6`, `inbound-whatsapp:live→37` | 2026-09-21 | AWS |
| `EV-0015` | `SEC-ROUTE-001`, `SEC-ROUTE-002` | Live probe of `https://api.wecare.digital`, unauthenticated, after deployment: `POST /whatsapp/inbound {}` → **401**; `POST /whatsapp/inbound {"action":"create_invoice"}` → **401**; `POST /webhook/sinch-rcs` with a MESSAGE_INBOUND body → **503**; `GET /webhook/sinch-rcs` → **200**. 4/4 as expected, re-runnable via `scripts/verify_public_webhook_auth.py --gate` | 2026-09-21 | production |
| `EV-0016` | `SEC-ROUTE-004`, `SEC-ROUTE-005`, `PROV-AIRTEL-001`, `PROV-SINCHSMS-001` | `docs/execution/route-auth-audit-20260921.txt` after deletion: 2 APIs, **327** routes, **OPEN 0**, **DANGLING 0**, **UNRESOLVED 0**. `--strict` still lists **31** routes on `zllr9lrg7j` plus `POST /ai/generate` relying on a weak marker | 2026-09-21 | AWS + repo |
| `EV-0017` | `SEC-ROUTE-006` | CloudWatch Logs Insights over `/aws/lambda/wecare-rcs-dlr`, 7 days to 2026-09-21: `rcs_dlr_received` by type = `MESSAGE_DELIVERY` **313**, and no other type. Function invocations 319 (7d). `wecare-inbound-whatsapp` 898 invocations (7d) | 2026-09-21 | AWS |
| `EV-0018` | `PROV-AIRTEL-001`, `PROV-SINCHSMS-001` | Deleted routes `iv4u4fs`, `nlktsl3` (`79g3bbufdh`), `569isr0`, `jeib1ii`, `wdyoeq8` (`zllr9lrg7j`) and integrations `qvupugf`, `cf9a7lp`. Integration refcounts measured as 2 and 3 beforehand, matching exactly the routes removed, so neither was shared. Routes 332 → **327**. Full export and restore commands in `docs/prohibited-provider-retirement.md` | 2026-09-21 | AWS |
| `EV-0019` | tooling | `~/.kiro/settings/permissions.yaml` rewritten by `apply_unattended_permissions.py --user`: 23 literal path entries → 19 glob entries, **0 literal**, six capabilities. Workspace file unchanged at 6 wildcard rules, hooks 3/3. `block_catastrophic.py` probed directly: pass (silent) for plain reads, `/tmp` redirects, venv pytest, AWS MCP reads, `apigatewayv2 delete-route` and `lambda update-function-code` | 2026-09-21 | local |
| `EV-0020` | `PROV-GATE-001`, `TEST-001` | Full suite **1207 passed** in 8.15s on the tree carrying the fixes. `scripts/check-provider-policy.sh` 8/8 ok, no violations | 2026-09-21 | local |
| `EV-0021` | `SEC-ROUTE-005` | Weak-marker triage. All seven handlers — `core/url-shortener`, `messaging/voice-in/c2c`, `messaging/voice-in/obd`, `ecommerce/product-image-gen`, `messaging/media-cleanup`, `operations/bulk-worker`, `ai/ai-generate-response` — contained **zero** occurrences of `require_auth`. Every `Authorization` match traced to `Access-Control-Allow-Headers: Content-Type,Authorization` or an outbound `Authorization: Bearer`; `product-image-gen`'s `api_key`/`X_API_KEY` is `WIX_API_KEY`, the outbound Wix credential. Conclusion: 30 routes genuinely unauthenticated, not merely weakly matched | 2026-09-21 | repo |
| `EV-0022` | `SEC-ROUTE-005` | Safety facts established before changing anything: `src/api/client.ts` attaches `Authorization: Bearer` from `fetchAuthSession()` (lines 50, 73, 195), as do `FloatingAgent.tsx` and `InternalChatTab.tsx`; `/voice-in/*` is documented and called as dashboard operations, not a provider webhook; no backend Python calls any of these routes over HTTP; `require_auth` returns `None` for events without an API Gateway context, so SQS, scheduled and internal invokes are unaffected | 2026-09-21 | repo |
| `EV-0023` | `SEC-ROUTE-005`, `TEST-001` | `tests/test_route_auth_enforcement.py`, 38 tests. Guard presence, failure propagation, and ordering before the first side effect for all seven handlers; behavioural tests proving url-shortener redirects stay anonymous while `/links` management requires auth; marker-policy tests asserting the weak markers are absent from the default set and every allowlisted route states a reason | 2026-09-21 | local |
| `EV-0024` | `SEC-ROUTE-005` | Gate proven, not assumed. Removing the guard from `media-cleanup` → **3 tests fail** (exit 1); restored → 38 pass. A seeded route `POST /seeded-hole` with an unauthenticated handler is classified **OPEN** by the live classifier | 2026-09-21 | local |
| `EV-0025` | `SEC-ROUTE-005`, `DEPLOY-001` | Deployed 8 functions, all `Active` with live sha == `$LATEST`, errorTotal 0: `wecare-media-cleanup` v8, `wecare-voice-in-c2c` v8, `wecare-voice-in-obd` v8, `wecare-ai-generate-response` v8, `wecare-bulk-worker` v8, `wecare-product-image-gen` v9, `stack-wecare-url-shortener` v4, `wecare-url-shortener` `$LATEST` (no alias). Rollback versions: 7, 7, 7, 7, 7, 8, 3 | 2026-09-21 | AWS |
| `EV-0026` | `SEC-ROUTE-005` | The deploy validator **refused** the first url-shortener upload: `handler.py:23 imports 'lambda_utils', not in package or layers`. It was `standalone=True` (handler.py only), so the import would have been a runtime ImportError. Resolved by dropping `standalone` for both twins and importing `require_auth` lazily inside the management branch, so the anonymous redirect path pays no import cost | 2026-09-21 | local |
| `EV-0027` | `SEC-ROUTE-005` | Live probe, **12/12** as expected via `scripts/verify_public_webhook_auth.py --gate`: 401 on `GET /links`, `GET /voice-in/obd`, `GET /voice-in/c2c`, `GET /bulk/worker`, `GET /store/preview-product-image`, `POST /ai/generate`, `POST /whatsapp/inbound` (×2); 503 on forged `POST /webhook/sinch-rcs`; 200 on `GET /webhook/sinch-rcs`; **302** on `GET /r/{code}` and `GET /{code}`, proving anonymous redirects still work | 2026-09-21 | production |
| `EV-0028` | `SEC-ROUTE-004`, `SEC-ROUTE-005` | `docs/execution/route-auth-audit-20260921.txt` under the strict default: 2 APIs, 327 routes, **OPEN 0**, DANGLING 0, UNRESOLVED 0, 301 handler-authenticated, 5 allowlisted with reasons | 2026-09-21 | AWS + repo |
| `EV-0029` | `OPS`, `TEST-001` | `.github/workflows/route-auth.yml`: blocking source gate on push/PR (guard assertions plus the full 1245-test suite, which previously ran in no workflow at all), and a scheduled live gate behind `vars.ROUTE_AUTH_ROLE_ARN` that skips rather than fails when the variable is absent | 2026-09-21 | repo |

| `EV-0030` | `NOTIF-STORE-001`..`004`, `PROV-AIRTEL-002` | `scripts/audit_data_model_drift.py`: 66 live tables, 6 live queues, 58 functions, 75 stack-prefixed literals in Python of which 7 are not tables. **MISSING 6**: `…-SmsAwsTable` (3 files), `…-AirtelC2CTable`, `…-AirtelSMSTable`, `…-PstnNotificationDelivery`, `…-SystemConfig`, `…-wix-store`. `ACCEPTED_ABSENT 1`: `…-RcsMessagesTable`. Deployed env read from live config: `wecare-voice-in-c2c` has `AIRTEL_C2C_TABLE` + `AIRTEL_C2C_SECRET_NAME=wecare/airtel/c2c`; `wecare-rcs-dlr` has `RCS_TABLE`; `wecare-sms-aws`, `wecare-system-cleanup`, `wecare-messages-read` have **no** table variables and run on source defaults | 2026-09-21 | AWS + repo |
| `EV-0031` | `DEPLOY-002` | API `79g3bbufdh` deleted: no domain mapped to it (both `api.wecare.digital` and `r.wecare.digital` map to `zllr9lrg7j`), **zero** request datapoints over 30 days against 84,301 for the live API, one route duplicating the live API's `POST /ai/generate` but pointing at unqualified `$LATEST`, CORS `AllowOrigins ["*"]`, `DisableExecuteApiEndpoint=false`. Deleted route `po5hjn4`, integration `qz7bhz2`, the API, and the stale Lambda permission `apigateway-invoke` for `79g3bbufdh/*/*`. After: **1 API, 326 routes**, and `wecare-ai-generate-response` grants invoke only to `zllr9lrg7j/*`. `wecare-waba-management` redeployed v9 → v10 to drop the deleted id from the `CORS_API_IDS` default | 2026-09-21 | AWS |

| `EV-0032` | `DEPLOY-003`, `OPS-001`..`003` | `scripts/generate_runtime_inventory.py` → `docs/execution/runtime-inventory.{json,md}`. 58 functions, 326 routes, 66 tables, 49 with a `live` alias, all joined to source path, deploy-map membership, aliases, routes, integration qualifier, env variable **names**, event sources, log retention and 24h/7d invocations and errors. Anomalies: **0 errors in 7 days**; **0** functions with zero 7-day invocations; **3** functions serve routes with no `live` alias (`partner-onboarding` 21 routes, `marketing-ads` 2, `seo-tools` 2) accounting for all **25** unqualified integrations; **9** log groups with no retention; **95** route paths no frontend file mentions; **2** live functions outside the deploy map, both documented exceptions (`docs-scraper` is an Image shipped by GitHub Actions, `seo-tools` has its own deploy script) | 2026-09-21 | AWS + repo |

## Not evidence

- Every Meta, Plivo, Razorpay, Google, Bing and Wix identifier in
  `docs/protected-resource-register.md` is **carried forward** from `bw-crm.md`.
  No provider API was called during Phase 0.
- No live HTTP request was sent to any production route. The two unauthenticated
  ingresses are proven from **configuration plus source**, not from exploitation.
- No test suite, lint, typecheck or build was run this phase.
